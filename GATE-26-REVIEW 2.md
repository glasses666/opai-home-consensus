# Gate 26 审阅：家装风格语料

## 结果

- 独立分支：`codex/cli-harness-style-dataset`；不合并 `main`，不修改网页编辑器。
- 语料包含 28 种风格，覆盖现代、奢华、历史、地域、亚洲与强表达方向。
- 每种风格包含格局、色彩、材料、墙地顶、家具、灯光、装修方法、避坑点和来源。
- 高争议或易过时风格使用 `polarizing / trend-sensitive`，不把“丑”写成客观事实。
- 11 维技术画像全部标记为 `curated_estimate`，用于相似度、追问和偏好对比，不是行业标准。
- 风水是默认关闭的文化约束层，不是风格、法规、安全标准或效果承诺；只在住户明确选择时询问。

## 研究路径

本轮按 Agent Reach 流程先检查后端，再使用 Exa 语义检索和 Jina Reader 读取公开页面。Exa 免费 MCP 中途触发 429，之后改用 Jina 直读与公开搜索页；没有申请或写入外部 API Key。核心来源优先采用 V&A、MoMA、The Met、NPS、UNESCO、丹麦公共站点，其次使用 Architectural Digest、House & Garden、Vogue 等专业媒体。社交平台搜索在当前 OpenCLI 会话没有返回可复用结果，因此未把小红书或 B站内容写进事实层。

## 数据边界

- 风格不会直接生成 `SceneCommand`；Gate 26 不改 Harness 或 Prompt。
- 风格不会提供净距、承重、消防、机电或施工数值。
- 相互重叠的标签保留为家装工作语言，而非互斥学术分类。
- 摩洛哥、热带和新中式属于策划翻译：来源负责历史 / 材料语境，落地字段是可审阅归纳。
- 真实欧派产品、报价、工期、BOM 和企业禁配仍为外部依赖。

## 下一 Gate 的进入条件

用户验收风格覆盖和字段后，Harness 才能读取这份语料，先做“识别偏好 → 找冲突 → 提两个方向 → 追问”，不能一收到风格词就批量改场景。

## 验证

- 风格语料测试：2 / 2 通过。
- 全仓测试：185 / 185 通过。
- 生产构建通过；仍只有既有的大 chunk 警告。
- `app/src/agent/harness.js` 与 `app/src/agent/prompt.js` 在本 Gate 无改动。
