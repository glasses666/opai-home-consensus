# 后端体验收口 · 2026-09-05

范围：收尾已有 DeepSeek Harness / 版本 / 单文件存储改动，不改前端、不部署、不 push、不接入 RAG。起点 `ab93f217b29ede5e7bb679f969492bb37b339342`，已有 10 个未提交后端文件；两个用户录屏不纳入。

## 完成

- 执行与澄清模式提供确定性工具 JSON 示例，保留 SceneCommand、意图匹配、规则校验和失败降级；这不是开放式设计能力提升的证明。
- 普通编辑输出预算 800 tokens；首版全屋生成独立 4096 tokens。关闭 thinking、温度 0.1。拒绝 `finish_reason=length`，即使截断内容恰好是合法 JSON。
- 默认 BFF 和首版链路保留 `DEEPSEEK_*` 错误，不再将 DeepSeek 错误重写为 Aily 错误；未改任何前端标签。
- 审核只接受当前且客户确认版本。带交接快照的相同审核请求可重放，且仍可重试同步，不重复状态转换。
- 无外部依赖的 `/api/health/live`；readiness 不排空 Base 队列。
- 保存前保留上一份文件备份；主文件损坏或缺失时加载经原有结构和回放校验的备份。损坏主文件仍隔离保存。

## 验证

- 全仓 `npm test`：283 / 283，0 失败。
- `npm run build`：通过；仍存在大于 500 kB 的 chunk 提示，未借本任务改前端分包。
- `git diff --check`：通过。
- 独立代码审查定向五套测试：86 / 86。
- 审核 HTTP 重试先复现 400，再修到 200；缺失主文件恢复和截断合法 JSON 两条先红后绿。
- 默认 DeepSeek 首版线路用 mock 上游、真实本地 HTTP 验证：三段分别 4096、完整合同校验成功、401 保留 `DEEPSEEK_AUTH_FAILED`。这是线路测试，不是模型调用。
- health 在真实非空 pending 队列上验证：两类探针均无同步调用，队列仍保留。

## 仍需保留的边界

- 本机 `.env.local` / 进程环境没有 DeepSeek key；没有 fresh live golden、首版质量或延迟证明。发布前需在有凭据的授权环境跑真实黄金路径。当前不部署。
- JSON store 仍限定单进程单写者；没有增加跨进程锁、fsync 或完整事务回滚。运行时外部修改主文件、主备双损坏、磁盘故障不属于完整容灾保证。
- Base 重试依赖后续业务请求，不再依赖 health；本次没有接后台定时队列。
- 离线 24 案例 RAG 种子库没有接入运行链。

## 经验

状态机 guard 必须沿存储、快照和 HTTP 重试一起验证，单层幂等测试不足；共享模型 adapter 的提速参数要区分短编辑与长首版生成；恢复测试必须同时覆盖损坏与缺失文件；探活测试必须放入真实 pending 事件。

下一步：视觉案例研究与用户选方向，暂不重构前端。回退仅使用本地提交的定向 revert，不能覆盖用户录屏或无关变更。
