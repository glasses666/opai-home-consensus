# 完整住宅 · Blender 视觉审阅

## 首页毛玻璃本地调节面板 · 2026-09-06

调节完成后按用户要求移除全部滑块、复制/重置控件及专用样式，保留确认参数 2px / 75% / 95% / 54%。

用户回传并确认的新默认值：模糊 2px / 横向 75% / 纵向 95% / 底色浓度 54%。默认和重置共用 DEFAULT_GLASS，开发预览与生产构建使用同一变量样式路径；调试控件仍仅开发环境可见。仅修改本地源码，未部署。

HomePreview 新增仅开发环境显示的可折叠滑块：模糊 0–24px、横向/纵向范围 10–150%、暖色底色浓度 0–100%。拖动即时预览，提供复制数值和重置；刷新恢复默认 3px / 58% / 55% / 88%，不改变线上样式。浏览器实测模糊从 3 改到 8 后读数与参数文本同步更新，再重置。生产构建、git diff --check 通过；未提交或推送。Vault 既有重叠改动不覆盖，记录暂存此处。

## V17 构件柔和入场与支撑等待

用户允许缩放/速度缓冲，要求解决空中突然出现。`soften-v17.py`由V16另存motion-v17，不修改相机、路径、墙体避让时点。每个构件以底部中心为支点，700ms五次平滑曲线从小到原尺寸（无超调），退出反向；不再一出现就是空中完整体量。Solid视口不显示材质透明过渡，因此必须有实际几何缓动，不能只延长alpha。

家具、玻璃/饰件保守等待本轮结构墙/楼板全部就位；屋顶也等待这些结构件完成，再交错入场。不是工程施工依赖图，仍属概念动画；完整尺寸及V04世界矩阵、中间缩放值和等待时间检查通过。相机及总长不改，无渲染，用户待审。源码未提交/推送，结果暂记README/PLAN，Vault旧冲突不覆盖。

## V16 遮挡构件延后

09.05.58反馈：不改V15相机，穿行和看树期间挡住路径/视线的背墙、玻璃等14组构件延迟至6.95秒或更晚搭建。判断不仅检查相机点穿墙，还检查相机至树的线段与构件包围盒，镜头穿出前保持隐藏。`--wall-gate`另存motion-v16，`verify-wall-v16.py`独立逐帧校验移动后实体包围盒、0.08m相机余量，以及遮挡组至137帧保持隐藏。模型最终几何保持，未渲染/提交/推送。该检查不等于整个视锥无遮挡；用户视口审阅仍必要。结果暂记此处和PLAN，Vault既有冲突不覆盖。

## V15 整个首段注视树

用户09.02.20截图圈出的是01至穿出段，不是开头1.1秒。修正0–6.8秒全部注视固定树中心，6.8–8.2秒才过渡整屋视线；移动路径保留。搭建热点保持独立的穿行目标轨迹，不能因锁树把所有搭建锁在树旁。--tree-lock另存motion-v15，无渲染/提交/推送。新增逐帧树注视与水平校验，不再只验末端一帧。上一版只改1.1秒的范围理解错误。

## V14 开场对树后退

用户要求第一段镜头对树沿自身后方退。`--tree-retreat` 仅把0–1.1秒改为注视固定树中心、沿视轴向后退2.5m，缓入缓出；之后沿用V13路径，不改变模型。另存motion-v14，无渲染/提交/推送。注意此改动只涉及开场段，后续仍是用户先前确认的穿行路线。

## V13 已明确确认：庭院向后穿越中间底层

用户确认08.53.23图的入口/出口：01朝树后方直进，穿过中间建筑底层，从背面出来，随后大弧线接03。V12沿侧面走的方向错误，不再沿用。`--through-rear` 生成motion-v13，x约1.2、y从-5.8经过0/5到10，约1.8m穿行，背面穿出后绕至右侧；树优先/世界Z锁水平保留。热点穿行时半径约2.2–3m，穿出后才扩大，避免提前搭完整栋。

原模型中间有封闭玻璃和背墙，因此不是现成贯通走廊：不切模型、不凭空改门洞，凡阻挡镜头路径的构件延后至镜头通过再搭建。此为概念搭建镜头，不宣称可实际步行穿过的建筑通道。完整展示几何仍校验保留，无渲染，旧稿保留，本地未提交/推送。经验：明确入口和出口后再改，不用二维草图反复猜测左右/高度；当前结果记README/PLAN，Vault旧冲突仍不覆盖。

## V12 底层穿行，锁定水平

用户明确纠正08.44.47红线含义：不是外绕或从屋顶上穿过，而是从房子底下穿行。V12由树前进入右侧有顶露台，约1.8m高度沿y=-3.8通道经过x=4.5至10，再从x=15处穿出抬升接03。原树优先与热点搭建保留，参数 `--underpass`，另存 `motion-v12/opai-underpass-v12.blend`。相机使用世界Z确定上方向，移除前版平行传输造成的累计侧倾。

独立重载 `verify-underpass-v12.py`：47帧位于右侧屋顶下通道，整段相机右方向竖直分量<1e-5（无roll）；按完整V04实体包围盒、相机点外扩0.08m逐帧检查入口至出口，无碰撞。此为点路径/包围盒检查，不代表近裁剪面、视锥或最终画面质量验收。无渲染，仍待用户Blender视口确认；源码未commit/push，Vault同页冲突不覆盖，经验：用户草图必须结合明确的“穿过/绕过”空间语义，不能凭二维投影猜测高度。

## V11 上拱连续弧线（08.36.14红线）

检查发现原固定上方向的look-at在近垂直俯视处单帧旋转约10.57°；修正为沿前帧相机上方向平行传输，保持视线目标不变，避免经过屋顶时突然扭转。路径光滑与相机朝向光滑必须分开检查。修改后重新载入验证朝向峰值小于10°/帧通过；未据此宣称成片审美通过。

用户进一步纠正路径形状：不是V10视图下方的外绕弯，而是从树前向上拱起、越过庭院/屋顶上方后接右侧。新增 `--smooth-arch` 候选，主要路点2秒(-1,-8,7)、4秒(4,0,16)、6秒(16,8,22)，8.2秒接既有右侧点；共享Hermite切向保证段间速度方向连续，树前停留结束从零速度启动，四元数朝向同号处理。树先搭建与热点机制不变；原V10保留，另存motion-v11，无渲染。路径与实际景别仍待用户确认，不因数学连续就宣称视觉通过。

## V10 第一段向右，中景看右侧住宅

依据用户 `截屏2026-09-06 08.31.43.heic` 红线修正：V09树正面开场保留；随后镜头从庭院向右前侧绕出，4秒位于(18,-9,9)、看向右侧立面(8,0,3.4)，6秒沿右侧抬升，再接既有远景。取消第一段向左绕背后的路径；同一视线轨迹重新驱动搭建热点，路线站点和时间轴中景标签同步改为RIGHT。复现 `camera-v08.py -- --right-medium`，另存 `motion-v10/opai-right-medium-v10.blend`，不覆盖V08/V09。无渲染，视口构图等待用户确认；本地未commit/push，读取Vault项目首页后确认原同页并行冲突仍在，暂记此处与PLAN。

## V09 树先搭建（用户V08反馈）

开场透视相机正对庭院树包围中心(-1.317,-4.271,2.592)，0–1.1秒固定观察树；树与花池模块0.12–0.62秒先落位，其余房屋模块最早1.2秒开始，线稿最早1.15秒启动，再接原左侧抬升/绕屋顶/右侧远景路线。总长仍15秒，无渲染。复现 `camera-v08.py -- --tree-first`，另存 `motion-v09/opai-tree-first-v09.blend`，V08保留。视口开启脚本从第1帧播放。仅本地未提交，Vault既有并行冲突不覆盖。

## V08 相机驱动搭建（仅Blender视口，等待路径确认）

用户要求近/中/远运镜、以镜头观察处为搭建热点，并提供桌面 `截屏2026-09-06 07.58.27.heic` 红线草图：1庭院起步，2沿左侧抬升绕屋顶，3从右侧拉远。以此替代最初正面左右扫动候选。`camera-v08.py` 由V07生成V08，不调用渲染。

- 32mm透视相机，连续Hermite位置/视线曲线、四元数朝向；左侧抬升→背上方→右侧→远景整屋，15秒时间轴。
- 搭建由相机look-at热点首次靠近各模块包围盒中心触发，随拉远扩大影响半径，增加小幅构造层次错位。不是沿用全屋统一的楼层计时，也不是相机所处空中的位置直接触发构件。当前99个语义模块仍按组落位，跨范围的大组后续可再拆细。
- 逐笔线稿按相同空间热点重新排序，退场边拆边收线。模型尺寸与V04完整展示矩阵校验保持；相机接回起点发生于末尾空白段。
- `motion-v08/opai-camera-hotspot-v08.blend` 保存原模型和非渲染辅助集合：CAMERA PATH相机路线、LOOK TARGET PATH视线轨迹、热点球、四个景别站点；左相机画面、右路线总览。`open-v08-review.py` 布置视口并启动播放。实体同步关键帧hide_viewport/hide_render，否则Solid视口会提前显示尚未装配的所有几何。
- 视口用Solid快速审动作，玻璃用线框显示，颜色/光照不作为成片依据；没有生成V08视频或渲染帧。原Logo未保存工程不覆盖，另开Blender进程加载此审阅文件。
- 目前仅实现并校验完整几何，运镜审美及热点半径仍待用户确认，不能当作最终运镜。未替换首页/提交/push/部署。读取AgentVault HOME及PRJ-2026-008项目首页，其首页/视觉记录仍有并行修改，暂停回写，结果暂记本页及PLAN。

## 当前审阅：V07 重叠编排（15秒）

用户认可V06动作方向，要求画线与搭建异步发生、退出同理并压缩等待。`retime-v07.py` 直接重定时V06保存场景，保留V04完整几何、22笔顺序描线、99个恒定尺寸位移模块；不重建模型，不修改V06原文件。

- 描线0.20–3.80秒，装配0.65–5.37秒；完整模型约5.37秒就位，庭院侧85°环绕约5.35–9.25秒。
- 拆解9.35–13.27秒，反向逐笔收线10.10–13.88秒，两者重叠；14.25秒后空白时复位相机，形成15秒循环。
- 已完成六个关键帧目视和独立重新加载状态检查：入场61帧、退场60帧同时存在正在绘制/擦除的笔画及全不透明移动构件；每时刻最多一笔变化，原V04完整展示矩阵与尺寸保持，头尾隐藏及相机闭环通过。不是单纯加速视频或同时缩放整屋。
- `motion-v07/` 保存blend、manifest、关键帧、状态检查与预览页；完整视频验证另见下方完成记录。保持960×540动作小样，未宣称最终画质。
- 完成记录：300张PNG已完整渲染，`house-overlap-v07.mp4`经ffprobe解码验证H.264、20fps、15.000秒、960×540、300帧；独立验证 `verification.json` 的 `media_verified=true`，首尾最大像素差约1/255。右侧本机8199预览已打开并观察到实际视频画面。此次未做丢帧率性能基准，不复用V06播放指标。
- 经验：用户所说“异步”指视觉阶段重叠，不是多任务并行；验收要同时检查实际笔画进度与可见刚性位移，不能只以总时长缩短代替。
- 未替换首页、commit、push、部署。AgentVault项目首页与视觉记录仍有并行未提交修改，未覆盖；持久记录暂存本说明与PLAN。

## V06 逐笔描线与逐件装配（方向认可，节奏由V07调整）

用户明确否定V05：整体压扁/拉伸不是“画出来、建起来”。该稿保留仅供对照，不作为已通过动画。V06重新从认可的V04模型建立动作，不改模型本体或网站。

- `animate-v06.py` 使用22条固定尺寸三维曲线，按笔画依次推进`bevel_factor_end`；同一时刻最多一条在绘制，不让全部线条同时延长，不缩放线框。
- 分离99个语义装配单元：地基/柱墙/室内/一层窗框/楼板/上层/屋顶/饰面。每个单元维持原尺寸，通过独立控制器位移落位；构件在全不透明状态下仍有可见移动。只在头尾几帧用淡化缓冲，不以淡入代替装配。
- 完成后庭院侧85°环绕；按装配次序倒放逐件移出，最后从屋顶笔画向地面笔画反向擦除。24秒/20fps/960×540/8采样是动作审阅稿，不是最终画质或工程施工顺序。
- `motion-v06/opai-house-assembly-v06.blend`、manifest、关键帧已保存。第一轮检查修正了“courtyard wall”窗框名称被错分为承重墙的分组规则，并验证材质透明度停留段不会被线性插值提前淡掉。原关键帧属于分组修正前的诊断稿，以最终frames/视频为准。
- `verify-motion-v06.py` 完整重载验证通过：22笔最多1笔同时绘制、99组全不透明时确实有位移、所有构件无scale/delta_scale动画、V04完整展示时所有世界矩阵保持、头尾实体隐藏、相机闭环、480张PNG尺寸有效、首尾画面最大差异≤1/255。`verification.json` 的 `media_verified=true`；早期 `state-verification.json` 仍保留为分阶段证据。
- `motion-v06/house-assembly-v06.mp4` 已编码并经ffprobe完整解码计数：H.264/yuv420p、480帧、20fps、24.000秒、960×540、2,444,757字节；12时点接触表已目视。前240帧的 `assembly-intro-v06.mp4` 是12秒先行审阅段。前54帧为分组修正前渲染但只有线稿、全部实体隐藏，线稿/相机参数未变，55帧起使用修正后的模型。
- `motion-v06/index.html` 提供逐笔描线/构件装配/环绕/拆解/收线五个跳转入口；右侧已打开本机 `http://127.0.0.1:8198/` 预览。用户仍需评价动作节奏，不以机器通过替代审美验收。
- 隔离浏览器实际播放观察25秒：视频24秒、readyState4、paused=false、loop=true，观察到一次循环回绕，累计解码1307帧、丢帧0、media error为空；见 `motion-v06/playback-verification.json`。该性能观测只代表本机小样，不外推线上设备。
- 可复用教训：视觉动词必须落实为具体操作，“绘制”用曲线弧长推进，“装配”用恒定尺寸刚性位移；不能以好看的整体缩放替代。动态验收须额外证明描线并发数和构件全不透明时的真实位移，不能只验关键帧画面。
- 未修改首页、commit、push或部署。AgentVault既有项目首页/视觉记录仍有并行未提交工作，未覆盖，经验与结果暂记本说明及PLAN。

## V05 黑线生长循环（用户否定动作逻辑）

用户认可V04内饰可用，授权参考原视频的“线稿生成—环绕—退回地面”逻辑试一版。继续用Blender真实模型/相机，不改网站，不重新生成建筑外形。V04源blend保留。

- `animate-v05.py` 加载V04，以主要楼板/屋顶轮廓、墙和窗框轴线生成稀疏黑色骨架，不给家具加密集网格线。
- 16秒、20fps、960×540、Cycles 12采样动作小样：先地面起线→线框向上生长→实体向上展开→庭院侧100°环绕→实体整体向下收回→线框降回平面→线条擦除。这里是整体竖向收拢，尚非逐构件分解；环绕也不是360°。该选择供用户判断动作语言，不预先认定终版。
- `motion-v05/house-growth-loop-v05.mp4` 为H.264/yuv420p/faststart、320帧、16.000秒、约2.7MB；`motion-v05/opai-house-motion-v05.blend` 保存可直接复现的关键帧动画；同目录manifest、逐帧PNG、关键帧和contact-sheet保留。
- 首轮关键帧目视后加粗线条，并让室内灯随模型收放，避免屋体尚未长到灯具高度时出现不合位置的亮斑。完整输出已做八时点接触表目视、ffprobe解码帧数检查。
- `verify-motion-v05.py` 重载blend验证4247个实体在各阶段的显隐、完整展示段比例1、320张PNG、首尾相机一致；空白端点最大像素差为1/255以内。不能用PNG文件二进制相等判定循环（元数据可能不同），也不把低采样渲染一个8-bit级别的差异写成像素完全一致。
- 仅为动作节奏稿，非最终画质；未替换首页、commit、push或部署。临时预览仅绑定本机127.0.0.1:8197。AgentVault项目首页仍有并行未提交修改，遵守既有冲突闸门，结果暂记本说明与PLAN。

```sh
/Applications/Blender.app/Contents/MacOS/Blender --background --python-exit-code 1 --python design-lab/opai-house-blender/animate-v05.py -- --motion-render
ffmpeg -framerate 20 -i design-lab/opai-house-blender/motion-v05/frames/frame-%04d.png -c:v libx264 -preset slow -crf 18 -pix_fmt yuv420p -movflags +faststart design-lab/opai-house-blender/motion-v05/house-growth-loop-v05.mp4
/Applications/Blender.app/Contents/MacOS/Blender --background --python-exit-code 1 --python design-lab/opai-house-blender/verify-motion-v05.py
```

## V04 内饰重建（用户已认可）

用户认为V03内饰仍粗糙，本轮从V03保存模型重建室内家具，保留完整外壳、建筑体量与主镜头。仅增加表面纹理/滚边不足以改善近景，因此实际替换家具轮廓，并用室内人眼高度相机复核，不以整栋远景代替内饰验收。

- 四个室内会客/阅读区改为连续圆润软包、独立坐垫与靠垫、弧形扶手、木质底座、曲面单椅；替换柜体为有板厚、层板、书本的开放书柜和分缝矮柜，补石质椭圆茶几、空心陶器、织物地毯与吊灯。
- 室外廊下原家具保留；玻璃粗糙度由0.055降至0.009，让隔玻璃观察的室内更清楚。没有隐藏屋顶/墙体拍摄，没有使用景深或后期锐化掩盖模型。
- 第一张预览发现西侧单椅与高窄体量立柱穿插，移动到立柱前方并重渲染；最终用求值网格世界坐标检查，最近立柱AABB间隔约0.293m。该单点回归不是全屋动线/碰撞/建筑规范审查。
- `interiors-v04.py` 可从V03复现；`opai-interiors-v04.blend` 含完整模型和三台审阅相机；`manifest-v04.json` 记录来源、相机、受保护几何和输出。
- `house-interiors-v04.png` 2560×1440；`living-room-v04.png`、`reading-room-v04.png` 各1920×1080。Cycles Metal 192采样，三张均已逐张目视；先做1280×720/48采样近景预览。
- `verify-v04.py` 重新加载blend，268个受保护外壳组件顶点/变换及主相机保持不变；4287对象、1019个V04对象、三张PNG/相机、无动画/景深、立柱净空专项检查通过。曲线求值包围盒可能不可用，验证改用`to_mesh()`的实际求值顶点，避免-1占位包围盒误报。
- 本轮未修改首页、制作动画、commit、push或部署。上下文沿用AgentVault欧派项目首页；该首页与视觉记录仍有并行未提交修改，Vault归档继续暂停，未覆盖或混提。结果及本次经验暂记录于本说明和PLAN。

```sh
/Applications/Blender.app/Contents/MacOS/Blender --background --python-exit-code 1 --python design-lab/opai-house-blender/interiors-v04.py
/Applications/Blender.app/Contents/MacOS/Blender --background --python-exit-code 1 --python design-lab/opai-house-blender/verify-v04.py
```

## V03 细节精修

用户认可V02体量后，授权只细化细节。本轮从保存的V02模型追加精修，不改布局、主相机或建筑比例，保留V01/V02产物；未替换首页，不加运镜或部署。

- 石材和铺装补细微材质变化、微表面凹凸和真实接缝；屋顶大面仅加疏朗分缝。
- 护栏边框减细，玻璃门增加把手/轨道，选定窗侧增加织物褶皱；没有挡住主要透视面。
- 布艺增加织物、滚边和支脚，茶几增加少量书本与陶瓷；仍为原创概念家具，非欧派目录。
- 粗略树叶换为2470片共享曲面叶网格，保留枝干/花池位置，地表增加卵石。
- `refine-v03.py` 可从原V02重复构建；`opai-refined-house-v03.blend` 含主相机和庭院细节相机。
- `house-refined-v03.png` 为2560×1440整栋图；`house-courtyard-detail-v03.png` 为1920×1080独立近景渲染。Cycles Metal 160采样，先1600×900/48采样测试并目视，再出成图；未用锐化或景深隐藏细节。
- `verify-v03.py` 重新读取保存场景与PNG，验证76个主体组件顶点/变换、主相机矩阵与缩放不变，3415对象、2470叶片、两张PNG尺寸和细节相机保存、无动画通过。两张成图均已目视；不等于建筑规范或网站E2E验证。
- 本地精细化已完成，未commit/push。沿用AgentVault项目首页的上下文；因同页并行简历改动，暂停Vault更新/提交，已向用户请求branch或overwrite选择，记录暂存在此与PLAN。

```sh
/Applications/Blender.app/Contents/MacOS/Blender --background --python-exit-code 1 --python design-lab/opai-house-blender/refine-v03.py
/Applications/Blender.app/Contents/MacOS/Blender --background --python-exit-code 1 --python design-lab/opai-house-blender/verify-v03.py
```

## V02 体量基线（用户已认可）

用户否定V01面积/比例太小，不接受只将其整体放大。V02对照首页实际媒体 `app/public/assets/hero/villa-hero-loop.mp4` 的10.8秒完整建筑帧，重建宽幅双翼、中央庭院、左退台/高窄格栅体量、后连接廊和右悬挑/廊下会客区。取消V01自行添加的水池；V01源与产物保留，不再作为当前比例基线。

- 当前模型 `opai-video-house-v02.blend`、静帧 `house-video-match-v02.png`，1920×1080、Cycles Metal 96 samples。
- `build-v02.py` 与 `house_kit.py` 复现V02；`verify-v02.py` 独立重新读取检查，`manifest-v02.json` 记录来源/边界，`verification-v02.json` 为验证结果。
- `reference/video-contact-sheet.png` 从左上按顺序为视频0、4.004、8.008、12.012、15.015秒，末格空白；`reference/video-10p8s.png` 为完整建筑参考。抽帧不修改原视频，未调用生成模型重画参考。
- 模型里的米制尺寸是为保持家具/建筑比例采用的建模假设，并非从视频测得的真实面积。背面和隐藏结构只能合理推定；这是可见体量的近似复刻，不宣称像素级/施工级还原。
- 目视迭代：第一稿镜头偏东使长侧墙主导，改西南侧观察；左高窄体量和后翼屋顶原本矩形重叠产生多余框线，改成单一封闭凹多边形屋顶和连续外缘。源视频自身有生成过程/消隐，不把半建成帧误当最终几何。
- 尚未做运镜或替换首页；源文件未commit/push。AgentVault项目首页有并行简历更新，已暂停该处写入/归档提交，V02暂仅记录于本说明与PLAN，避免覆盖或混提。

```sh
/Applications/Blender.app/Contents/MacOS/Blender --background --python-exit-code 1 --python design-lab/opai-house-blender/build-v02.py
/Applications/Blender.app/Contents/MacOS/Blender --background --python-exit-code 1 --python design-lab/opai-house-blender/verify-v02.py
```

---

用户希望首页呈现一栋完整、高完成度的房子，替代模糊的 AI 房屋视频。**不是开放剖面或工作台模型，也未替换首页**。当前阶段只审阅建筑气质、构图、玻璃和材质，不做动画或部署。

## 本版

- 两层退台住宅，完整屋顶、女儿墙、外墙、玻璃幕墙、木格栅、露台、庭院树及反射水池。
- 玻璃后有原创简化家具和室内灯光；所有几何及材质程序化自建，没有下载模型或使用欧派商品资产。
- 暖中性背景，主体偏右，斜上方观察；留左侧作为首页文案区域。关闭景深，避免用模糊掩盖细节。
- 原始 Eevee 测试玻璃显示为不透明灰板，实际目视后改用 Cycles / Metal；保留测试图作对照。
- 这是概念建筑，不是经过结构、动线、节能或防水审查的施工设计。

## 文件

- `house-hero-v01.png`：1920×1080、Cycles 128 samples 审阅静帧。
- `opai-complete-house-v01.blend`：可编辑完整场景，统一建筑根节点和 Hero 相机。
- `build.py`：可复现构建和渲染；`verify.py`：保存文件、尺寸、组件及模型状态检查。
- `manifest.json` / `verification.json`：来源、配置与机器验证。
- `house-preview.png`：1600×900、64 samples 中间预览；`house-eevee-check.png` 为弃用渲染测试。

## 复现（本机 Blender 5.1.2 / Metal）

```sh
/Applications/Blender.app/Contents/MacOS/Blender --background --python-exit-code 1 --python design-lab/opai-house-blender/build.py
/Applications/Blender.app/Contents/MacOS/Blender --background --python-exit-code 1 --python design-lab/opai-house-blender/verify.py
```

构建会覆盖本目录的 V01 场景和静帧；修改前另存用户手工编辑版本。代码按节点类型识别 Principled/Background，避免中文 Blender 默认节点名失配；5.1 使用 `BLENDER_EEVEE` 引擎标识。

## 下一步

用户审阅静帧后调整建筑比例及材质，再设计缓慢靠近、沿外立面移动的运镜。完整建筑必须成为主要停留画面；是否引入搭建过程和如何循环尚待视觉确认。未写入产品 canonical scene、不改前后端、不 push 或部署。
# V18 — 开头直接后退，树与墙板延后

`refine-v18.py` 从 V17 保留模型及后续运镜，去掉树前方开头绕行，从树靠房屋一侧直接后退进入中央通道。树在 0.35 秒后用现有 0.7 秒缓冲出现；Central limestone return 墙板延至 6.95 秒。仅保存 Blender 预览，不渲染、不部署。机器校验完整模型矩阵及树、墙隐藏时段；镜头构图仍待用户预览确认。原版本保留。AgentVault 因既有冲突暂停同步。
# V19 — 树单独再延后 2.5 秒

基于 V18，只将树入场从 0.35 秒移到 2.85 秒，原 0.7 秒缓冲及退场、运镜均保留。复现：`tree-delay-v19.py`。不渲染、不部署。
# V20 — 收稳后退场，微推近

基于已接受的 V19：保留树延后及前段运镜；10–13.9 秒减速收稳，停留 0.4 秒，14.3 秒开始退场。退场镜头以五次平滑曲线推进 2 米，18.05 秒结束；空场后回位，19.2 秒循环。`settle-exit-v20.py` 可复现。验证停留时房屋完整、退场结束隐藏及推进距离；未渲染，视觉待用户确认。旧窗口通过系统正常退出请求关闭，不强杀、不删除工程文件；原始可能未保存窗口保留。
# V21 — 按红线拉近退场终点

依据用户 09.34.19 截图，将退场推进由 2 米加长至远景到房屋焦点距离的一半，仍留屋外余量；时间、树、组装和前段运镜不动。`dolly-v21.py` 可复现，路径辅助线同步延长；无渲染。终点是截图方向的空间近似，待用户预览确认。
# V20 获准渲染

用户确认并授权整段渲染。`render-v20.py` 单 Metal GPU、1080p、Cycles 32 samples、20fps、384 帧；先检查完整房屋试帧，再输出可续渲 PNG。`encode-v20.sh` 只在完成标记存在后合成 H.264 MP4 并 ffprobe 检查。当前为渲染中，不能视为成片已验证。保留原场景，不替换网站。输出 `motion-v20/render-1080/`。
# V22 — 线框缓出与 120fps

用户指出 7–8 秒线框硬切，并授权提高资源占用渲染。根因：`camera-v08.py` 的 `8<t<10.2` 直接隐藏。新候选 `render-v22.py` 从已确认 V20 独立生成，线稿以 0.8 秒五次缓动反向收线，长度趋零才隐藏；末段重新画线后也缓慢收回。重映射所有动画时间到 120fps，19.2 秒共 2304 帧，非视频补帧。1080p/Cycles32/单MetalGPU/14CPU线程。逐线半收回状态检查通过，并生成7.8/8.2/8.6秒试帧检查。`render-encode-v22.sh` 渲染完自动编码与探测。目前全片尚待完成验证。旧版保留，不部署，不提交。Vault 既有冲突未覆盖。
# V23 — 用户改为 60fps

保留 V22 线框缓出及 19.2 秒时长，时间轴等比例改为 60fps/1152 帧。停止旧 120fps 渲染，旧文件保留；按时间一一对应复用其完整奇数帧（PNG结束标记校验），剩余继续 Metal GPU 渲染。`render-encode-v23.sh` 自动合成 1080p60 MP4。尚在渲染，未宣称成片验证通过；不部署、不提交。
# V23 接入本地首页预留位

成片 ffprobe 验证 1920×1080、60fps、1152帧、19.2秒。用户要求先放进网页试看：复制到 `app/public/assets/hero/house-blender-v23.mp4`，14秒附近提取匹配封面 `house-blender-v23.jpg`，仅替换 HomePreview 的预留背景媒体，保留毛玻璃/排版/静音自动循环与减少动态效果规则。浏览器本地5180实际显示新房屋线稿动画；原视频保留，未部署或提交。Vault既有冲突仍不覆盖。
# 用户剪映导出调色版接入

采用用户手动导出的根目录 `9月6日.mp4`，HEVC/1080p/59.94fps/19.2017秒。保留源文件，不再调色，仅转 H.264 去音轨网页兼容版 `app/public/assets/hero/house-graded-v24.mp4`，配套同源封面。HomePreview 本地预留位切换，浏览器实际画面确认；不发布线上、不提交。此 V24 为网页媒体标识，不是新增建模版本。
# 用户新版导出替换（网页媒体 V25）

按用户要求采用 `9月6日(1).mp4`，1080p/59.94fps/18.435秒；仅转H.264网页兼容版本并提取同源封面，使用 `house-graded-v25` 资源名避免旧缓存。旧媒体和源视频保留，本地替换，不部署、不提交。
# 首页清晰度收尾

用户授权缩小遮罩影响：home-glass.css 将全高横向毛玻璃改为左下局部径向遮罩，模糊5px降至3px；移动端只保留下半部文案保护。视频及排版不变。本地浏览器确认房屋主体清晰、文案可辨，diff检查通过；未部署提交。后续重心转其他前端模型，本轮不扩展建模。
