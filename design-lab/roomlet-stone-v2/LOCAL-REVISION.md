# Local shell revision · 2026-09-06

Pro handoff package: `deliverables/roomlet-approved-source-2026-09-06.zip` (44 files, 5,284,713 bytes). Allowlist includes current editable data/model sources, offline preview, GLBs/STEP, final sRGB sway video/poster, React integration example, minimal original asset baseline for preservation, updated Chinese README, and freshly run model/motion reports. Excludes old README/QA/screenshots, stale checksums, local operational history, credentials and caches. ZIP CRC and all 43 payload SHA256 checks pass after extraction. Motion audit: 3,937 samples, zero issues; three GLBs validate, external trimesh importer not installed. Browser security policy blocked opening the extracted file:// preview; no workaround attempted, so this package's fresh extracted GUI launch is not claimed. Previously verified source preview is bundled byte-for-byte. Capture CLI now accepts portable --output-dir/--browser options; no full CAD regeneration was performed during packaging. Local only, no commit/push; prior owning Vault conflict remains untouched.

Subtle parallax follow-up: homepage capture camera now orbits +/-2 degrees around accepted left-offset yaw over one 13.133s cycle. Cosine easing gives zero angular velocity at turnaround and loop boundary; same half-frame reversal keeps furniture motion unchanged. No flat CSS wobble, geometry change, or lighting change. Export remains 60fps with verified sRGB transfer tags. Homepage source cache version `sway-5`. Local-only record; owning Vault home remains dirty and was not overwritten.

Background seam correction: untagged H.264 displayed a lifted green rectangle despite source RGB matching CSS #353d32. Merely adding encoder colour flags did not preserve transfer/primaries in this FFmpeg build. Working export uses explicit BT.709 matrix plus sRGB transfer in BOTH H.264 VUI and MP4 colr, verified by ffprobe (bt709 / iec61966-2-1 / bt709). Homepage now uses `roomlet-living-srgb.mp4`; browser playback screenshot confirms the formerly visible rectangular boundary is no longer discernible. No CSS tint, blend or model/light/camera changes. Original local MP4 retained; reproducible remux added to capture script. Project home read but owning Vault notes still overlap dirty work; no Vault overwrite, commit or push.

Homepage lighting follow-up: user found white studio illumination detached from the green page. Capture-only rig now reduces ambient and direct intensity, uses restrained warm key plus green-grey environment/fill, widens shadow penumbra, and reduces exposure to 0.94. Camera yaw shifts left by 0.18 radians (~10 degrees), keeping original pitch/target. Geometry, materials, motion and standalone interactive preview remain untouched. Reviewed a low-cost still before exporting the 960×960/60fps loop and poster; browser video URL cache version bumped. Reuse rule: calibrate the scene against its final page background before exporting the loop, not by tinting the whole page/video. Vault project home was read and still conflicts with existing uncommitted work, so this record stays local; no commit or push.

Homepage integration: replaced only the first story tab's floor-plan image with a locally captured looping H.264 video and poster. `source/capture_home.py` renders the accepted local geometry/motion at 960×960, 60fps, 13.133s (1.25× original pacing), about 0.9MB, with a dark green backdrop. Frontend lives in `app/src/RoomletStoryMedia.jsx` and `roomlet-story.css`; pauses offscreen/hidden and respects reduced motion, with poster fallback. Other two tabs remain unchanged. Production build and diff whitespace check pass (existing build size/browser-module warnings remain). In-app browser visually verified two different furniture poses in the actual story slot. Slight encoded-background colour difference remains visible at the media rectangle. Existing homepage anchor links were observed redirecting to the editor; not changed in this scoped integration. Local changes only, no commit/push/deployment. AgentVault owning home/visual-reference notes remain blocked by overlapping uncommitted work; this note is the temporary source-side record.

Motion follow-up: user rejected visible hovering. Small-object transit altitudes raised from approximately 2.8 to 10.0–10.3, keeping original stagger, easing, 16.4s duration, layouts and large-object paths. Default camera preview checked at 3.5s (small pieces outside frame) and 7.3s (all landed). Arbitrary user orbit/zoom-out can still reveal physical objects; this is camera framing, not visibility toggling. Motion audit: 3,937 samples, zero issues, loop endpoints match, large pieces remain grounded. Rebuilt animation GLB and standalone preview. Original ZIP untouched; local only, Vault conflict unchanged.

Floor-edge follow-up (user screenshot 19.29.48): kept the rectangular usable interior, replaced the exposed slab edges with a gently irregular timber apron tapering from y=0.080 to below the stone top. Smooth normals and continuous UVs prevent a hard raised rim; no alpha blending. Browser inspected at original layout. Exact comparison with delivery confirms only shell and floor geometry differ; furniture nodes, layouts, animation, materials and textures remain identical. STEP represents the stone shell only. GLBs and standalone preview rebuilt. Vault conflict remains untouched; no commit/push/deployment.

Follow-up: user found the first envelope too flat. Increased vertical envelope radius from 2.50 to 3.50, lowered the base from -1.40 to -2.40 and added lower facet rings. Overall height increases about 28%; room opening and horizontal radii stay unchanged. Rebuilt CAD/GLBs/offline preview with original-assets preservation; CAD solid valid, shell 200 triangles, browser inspected. Previous 168-triangle value below describes the first revision.

Source: user-provided `roomlet-stone-v2-delivery.zip`, original ZIP untouched.

Only requested change: recover a spherical overall silhouette while retaining grey low-poly facets. Replaced the upright point rings in `source/build_scene.py` with a faceted spherical envelope; retained the original Boolean room opening. Rectangular floor, two walls, all furniture, camera and animation remain unchanged.

Rebuild with the dependencies already declared in requirements.txt: run `source/build_scene.py`, then `source/preserve_original_assets.py <original ZIP>`, then `source/bundle_preview.py`. The preservation pass prevents library-version floating-point and texture regeneration differences from touching accepted assets.

Verification: valid CAD solid; 168 shell triangles; exact comparison against original delivery passes for all nodes, non-shell geometries, materials, textures, layouts, camera and animation samples. Existing motion audit reports zero issues over 3,937 samples (conservative furniture bounds, not full stone-shell collision proof). Local browser preview visually inspected. Three GLBs, STEP and standalone preview regenerated. Bundled samples and earlier QA documents remain ORIGINAL reference outputs, not evidence of this revision; original SHA256SUMS is no longer applicable.

Preview: http://127.0.0.1:8203/ . No homepage integration, deployment, commit or push. AgentVault project home and visual-reference note have overlapping uncommitted changes, so they were read but not overwritten; revision recorded here pending safe Vault synchronization.
