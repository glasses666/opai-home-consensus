# Gate 9 Review：家庭共享、意见冲突与共同确认

状态：已实现、已验证，等待用户验收。

## 本 Gate 构建了什么

- `/project/demo` 的右侧工作区增加“家庭”模式；房屋仍是主界面，没有新建房间卡片页或第二套 scene。
- 固定三名演示成员：业主、伴侣、长辈。每条意见记录成员、立场、对象和 `SceneVersion`，手动保存也记录当前成员来源。
- 成员可对同一房间、对象或版本表达支持、反对、补充和不可妥协；冲突仅在同一对象、同一版本存在相反立场时成立。
- 共识助手只提出经过现有 `SceneCommand` 与规则引擎校验的方向；选择方向会真实修改 canonical scene、创建新版本，并保留基准版本用于差异与影响查看。
- 三名成员分别确认同一个结果版本后，才把该 `SceneVersion` 标为 `customer_confirmed`。Agent 不代替任何成员确认。
- 家庭状态使用经严格校验的 `localStorage` 演示缓存；损坏、跨版本或篡改成员/法定人数的数据会被丢弃并安全重建。

## 三住户真实链路

- 业主在沙发 V1 上表达“支持保留完整三人沙发，会客时座位不能少”。
- 伴侣对同一对象表达“主通道一定要更清爽，这是我不可妥协的要求”。
- 长辈补充“夜里经过客厅时不能容易绊倒”。
- 系统识别同一对象、同一 V1 上的支持与不可妥协冲突，生成两个经过规则校验的真实方向：向东 200 mm、向北 200 mm。
- 长辈选择“向东 200 mm”；沙发 x 从 2200 mm 变为 2400 mm，生成 V2，来源为 `household:member-parent`。
- 业主、伴侣、长辈依次确认 V2；第三次确认后 V2 进入 `customer_confirmed`。刷新页面后意见、确认、V2 和沙发位置均保持一致。

## 验证证据

- 自动化：`npm test`，110 passed / 0 failed。
- 构建：`npm run build` 通过；仅保留既有大 chunk 警告。
- 后台 Chrome 全程按真实坐标点击、输入、切换成员和刷新，不抢占用户前台；控制台无未处理异常。
- 代码审查发现并修复了一个 P1：篡改缓存曾可把三人家庭缩减为一人并绕过共同确认。恢复数据现必须精确匹配三名演示成员，并有空成员 / 单成员回归测试。
- 长辈独立住户测试通过；未发现 P0/P1。成员标签较紧凑导致一次误点，记录为 P2，不阻塞功能。
- 截图：
  - `.omx/audits/gate9/01-initial.png`
  - `.omx/audits/gate9/03-conflict.png`
  - `.omx/audits/gate9/04-directions.png`
  - `.omx/audits/gate9/05-v2-decision.png`
  - `.omx/audits/gate9/07-consensus-confirmed.png`
  - `.omx/audits/gate9/persona-parent/final.png`

## 已知边界

- 当前用顺序身份切换模拟一家人共创，页面已明确标记 `Demo`；未实现账号、权限、实时多人光标或并发编辑。
- 当前冲突识别是确定性的立场 / 对象 / 版本匹配；Aily 当前为 `missing_scope`，不能把它描述为已上线的语义冲突理解。
- V1 只保留一个当前共同方向；已选择结果由 `SceneVersion` 链追溯，不额外建立一套决策历史。
- 家庭共识仍是本地演示缓存；Base 统一留痕和设计师复核属于 Gate 11。
