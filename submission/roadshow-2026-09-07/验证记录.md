# 路演材料制作与核验记录

日期：2026-09-07。归属：元界视创欧派 AI 家装共识工作台。

## 22时上线材料更新

- 已读取用户引用任务“查看最新3D页面进展 (2)”最新六轮，核对发布源码8706a2a及记录57f1c1c。下方“已完成/未完成”保留初稿阶段记录；当前状态以本节及06号说明为准。
- 本轮更新方案Markdown/Word/PDF、讲稿、旧对照的时间说明与导航，补入实际飞书交接入口、发布时295项测试及6/6真实模型验证。没有重跑这些发布测试。
- 公网首页哈希匹配；22:16两次health返回飞书auth_failed/user_token_invalid。内置浏览器两次加载超时，未完成本轮GUI验收，不以HTTP请求代替交互证明。
- 新版Word与Markdown去格式正文3730字符完全一致。PDF仍为五页，逐页检查并压缩第5页过长段落，最终无缺字、截断、重叠或额外空白页。README本地文件链接通过。
- 当前Git HEAD为57f1c1c，源码已由发布任务提交。本任务仅改仍未跟踪的路演目录，没有提交、推送或再部署。AgentVault项目008主页仍有重叠未提交修改，未覆盖；新的验证经验与状态存于06号说明。

## 已完成

- 找回旧提交包、飞书提交链接和早期报名研究；核对 Git 时间，区分 2026-08-15 录屏脚本与 2026-08-17 提交正文。
- 通过飞书用户身份读取官方强化文档正文，document_id 为 U81PdHTqNooZjhxYhGZcoya8nTf，revision 1320。只读，没有发消息、更新飞书材料或安装文档中推荐的 Skill。
- 核对固定演示分支、20 秒阶段等待、本地场景命令和共识秘书职责；未读取凭据。
- 使用现有 `app/tests/recording-scenarios.test.mjs` 做针对性复验，6 项通过、0 项失败。命令为 `node --test app/tests/recording-scenarios.test.mjs`，Node 来自 Codex 工作区依赖运行时。
- 生成方案 Markdown、可编辑 Word、五页 PDF、七分钟讲稿及修改对照。讲稿包含画面和备忘，不等于真人七分钟试讲已经验收。
- 校验方案 Markdown 与 DOCX 正文去除格式和空白后完全一致；PDF 为五页，每页均可提取中文；逐页目视没有缺字、截断、表格重叠或异常空白页。
- 文案经过一次独立审阅，收敛了重复的限制说明，并补充实际餐桌与座席变化，保留证据范围。

## 渲染修正及复用边界

初次用 PingFang SC 导出时，bundled LibreOffice 的字体环境没有正确呈现中文，且默认 Word 样式带有标题下横线。改用本机已有 Arial Unicode MS，通过临时 FONTCONFIG_FILE 只为本次渲染加入系统 Supplemental 字体目录，清除样式段落边框；没有安装字体、修改系统字体或改动渲染器。

自动倍数行距导致章节末段落单独溢出。保留 11 磅正文，使用 17 磅行距、合理段间距，表格用 10.5 磅字与 16 磅行距，移除文档网格后重导出。最终五页全部目视检查通过。字体配置仅用于渲染；跨机器编辑仍须复查字体与分页，PDF 可用于固定版式审阅。

生成脚本为 `build_review_docx.py`，从本目录 Markdown 构建 Word，不调用原提交包的封面或生成逻辑。渲染使用 `load_workspace_dependencies` 返回的 Python 与 bundled LibreOffice；没有使用桌面安装版 LibreOffice。

## 未完成的工作

当前线上非预设 AI 设计、确认、Aily 共识摘要与 Base 留痕的同次完整实录尚未补齐。6 项测试是确定性场景检查，不是 GUI、在线模型、生产可靠性或企业收益证明。原产品入口、旧视频及提交链接本轮未逐一打开验证。

没有新制作 PPT 或视频，没有替团队完成真人试讲，没有与顾问沟通或提交官方表单。本次交付是可继续打磨的材料审阅包。

## Git 与 Obsidian

本轮新增范围仅为 `submission/roadshow-2026-09-07/`。原提交包和代码未被本轮改写；本地 Git 新增文件未提交，不 push、不部署。起点 HEAD 为 `2f7b7be4020368638226f661b06fe39be6dc6070`。

检索使用 AgentVault 项目 008 的 `00_Project_Home.md`、`03_Deliverables.md`、`08_12_Strong_Roadshow_Guidance.md`。项目首页与沟通笔记已有其他未提交改动，因此没有覆盖，也没有启动 Vault 写入阶段。本轮新发现的版本判定、预设与在线证据区分，以及渲染修正已保存在本目录 Markdown；Vault 经验同步因重叠改动跳过。

## 精简版更新

按用户要求，方案由五页压缩为两页，围绕痛点、解决方式、演示、成果和企业价值；讲稿同步重写。技术机制、指标方法及答辩移至07号附件。保留必要的企业数据和当前授权边界。Word与Markdown正文一致，两页PDF均目视通过，README导航通过。仅改路演目录，未提交；Vault重叠改动不覆盖，经验记录保存在本目录。

## Opus5 完整正文与教师材料包

- 通过本机现有 Opus5 文案通道生成 `01-老师提交正文-Opus5完整版.md`，输入包含原 40 强正文、精简优化稿、案例/行业证据、远端更新、技术答辩附件和历史审阅边界；返回 HTTP 200，模型标识 `claude-opus-5`，正常结束。
- 对生成稿做了事实边界复核：有限面积表述、行业调查的地域边界、演示级规则范围和三口之家/祖父母席位问题均已收敛；未加入未验证的 SKU、BOM、价格、生产或全国统计结论。
- `01-老师提交正文-Opus5完整版.docx` 由 Markdown 生成，封面日期更新为 2026 年 9 月 7 日；对应 PDF 共 20 页（A4）。重新渲染后逐页检查，封面、目录、表格和正文无缺字、截断、重叠或额外空白页。
- 新增 `00-老师交付说明.md`，明确教师阅读顺序、附件用途、体验入口、视频和在线验证边界。教师包选取主演示 `欧派.mp4` 与最新 3D 片段 `9月6日.mp4`；9 月 5 日长录屏保留在工作区，未放入压缩包以控制体积。
- 压缩包将在 `submission/元界视创-欧派AI家装共识工作台-老师材料包-2026-09-07.zip` 生成，并用 `unzip -tq` 校验；包内不含凭据、API Key、源代码或 Opus5 原始响应。

## 教师材料表达修订

按用户反馈，将 Harness 历史进度、阻塞、DeepSeek 切换和飞书职责集中为正文第六节的简短说明。教师 ZIP 排除内部运维、测试流水、历史重复稿和技术验证记录；真实能力边界仍保留。完整 PDF 17 页、简版 2 页，重新渲染检查；ZIP 内容与完整性检查通过。新版目录为 teacher-package-2026-09-07-v2。原始内部证据保留，不改变线上系统。Obsidian 同主题既有未提交修改未覆盖，经验暂存在本记录。
