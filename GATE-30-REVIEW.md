# Gate 30 验收：Harness 对话稳定性

## 结果

Gate 30 已实现，等待用户验收后提交。Harness 现在以本地解析的唯一写意图约束 Aily：工具名正确但对象、数值、材质或额外参数不一致时，转入安全本地降级；所有场景写入仍由 `SceneCommand` 和确定性规则执行。

## 关闭的失败模式

- “其他地方先别动”误伤同句明确修改。
- “越界就不要改了”吞掉下一个层板需求。
- 同一句含沙发和餐桌时猜错写入目标。
- Aily 使用正确工具名但偷换对象或移动量。
- Aily 在本地规则拒绝后仍宣称“已完成”。
- 真实评测内部工具失败，总报告却误标绿色。

## 证据

- `npm test`：全量通过，无失败。
- `npm run build`：通过；仅保留已知大 chunk 警告。
- `npm run eval:agent:live:golden`：6 / 6 真实 Aily 用例通过；查明并修复一处澄清选项假绿后，单例再次通过。
- `node --env-file-if-exists=.env.local scripts/live_resident_session.mjs`：通过；真实 Aily 3 / 5，安全降级 2 / 5，越界原子回滚，澄清轮无写入，最终沙发 `x=2400, z=5600`，南墙 `mat-wall-oak-panel`，累计 2 条 `SceneCommand`。

## 已知边界

- 本地解析为 fail-closed：未能唯一理解的口语会转为澄清或安全降级，不会猜测写入。
- Aily 仍存在约 20–50 秒延迟及偶发结构漂移；这是上游能力边界，不影响本地安全与数据一致性，但仍未达到生产 SLA。
- 本 Gate 不声称 Aily 已会完整室内设计；它只证明对话合同、工具边界、回滚和降级稳定。
