# 欧派 AI 家装共识工作台：产品层级校正与 V1.1 Gate 计划

> “欧派 AI 家装共识工作台”是当前工作名，不是已确认的最终品牌名称。

最后更新：2026-08-12
当前状态：**Gate 0–Gate 18 已实现并提交，现有 V1 技术底座全部保留；当前问题是消费者产品层级偏向“装修编辑器”，不是底层能力或数据架构失效**
当前 Gate：**Gate 23 已完成并进入 Gate 24；用户已授权连续完成 Gate 19–Gate 24，起床后统一验收**
当前后端批次：**B1–B4 已实现、验证并提交；不改变 Gate 3 验收状态**

> 2026-08-11 用户已授权 Gate 16–Gate 18 连续实施、逐 Gate 验证和提交，并在全部完成后统一验收；本次授权覆盖本文件原有的逐 Gate 等待要求，但不放宽真实数据、生产接口和外部发布边界。

> 2026-08-12 用户再次授权 Gate 19–Gate 24 连续实施。每个 Gate 仍须独立验证、复核 Agent-first 产品精神并建立安全提交点，但不等待中途人工验收；所有候选风格保留到用户起床后统一选择。

## 0. 为什么继续规划，而不是推倒重来

Gate 0–Gate 18 已经建立了可复用且经过验证的产品底座：同源 2D / 3D、canonical scene、Pascal Editor、SceneCommand、确定性规则、版本、家庭共识、Aily / 本地 Agent、飞书 Base、设计师复核和企业交接适配器。用户于 2026-08-12 明确确认：**当前版本不需要废弃，主要问题只是产品层级把“装修编辑器”放得太靠前。**

因此 V1.1 只校正消费者体验的主次关系：

- **Agent 设计会话是产品主流程**：Agent 先理解、先尝试、展示结果、解释取舍，再追问一个当前最重要的问题。
- **实时 3D 是共同语境**：住户和 Agent 围绕同一住宅、对象、表面和版本交流，所有设计建议必须真实作用于 scene。
- **住户调整是辅助能力**：对象级快捷调整按需出现；完整 Pascal Editor 继续保留，但降为明确进入的高级编辑层。
- **2D、版本、家庭意见、复核和交接是阶段性工具**：需要时出现，不与 Agent 和 3D 长期争夺主画面。
- **不改正确地基**：不建立第二套 scene、规则、版本、Agent Harness 或编辑器；不迁移框架，不重写已通过的后端闭环。
- 本文件继续是唯一实施计划；Gate 19 起重新按用户审阅推进。

## 1. 已确认的产品合同

以下内容不再反复向用户询问：

1. **2D 户型是空间契约**：房间、墙体、门窗、尺寸和家具位置来自结构化户型数据。
2. **真实 3D 是消费者主界面**：3D 不是方块示意，也不是独立于户型的图片。
3. **2D 与 3D 同源但职责不同**：2D 只展示当前户型和装修布局的同步俯视总览；3D 是主要浏览与编辑界面，两者不存在第二套图片版本状态。
4. **房间导航是两段式 3D 镜头**：用户先从整屋立体视图点击房间，镜头平滑飞到该房间的 3D 俯视总览；进入房间后再通过视角按钮切换入口、主功能面或自由视角。2D 只同步高亮当前位置。
5. **Home Assistant 只作为交互参照**：借鉴数字住宅的整屋浏览与对象点选，不继承其旧式界面、模型质感或设备控制逻辑。
6. **3D 模型采用原创北欧家居目录风格**：接近 IKEA 当代空间的自然光、真实比例、浅木与暖中性色，但不复制品牌、商品、配色或页面结构；禁止低模方块、白模和老旧可视化质感成为产品画面。
7. **全部可移动家具都可编辑**：可选择、移动、旋转、替换、删除及在允许范围内改材质或尺寸；固定建筑对象按类型锁定。
8. **规则始终兜底**：建筑边界、碰撞、通行、门扇、人体工学、产品组合、施工和已确认对象共同约束编辑。
9. **B + A 交互**：AI 先生成合理方案并解释，用户仍能在规则内自由修订；不固定成某个 80/20 比例。
10. **Agent 操作真实项目**：自然语言和直接操作最终调用同一套场景命令，不能只返回文案或预设图片。
11. **家庭共享共创**：家庭成员在同一公开设计空间表达意见；AI 同时承担设计解释与冲突调解。
12. **效果图是当前 3D 场景的渲染结果**：可以有不同质量或相机预设，但对象、材质和版本必须与实时场景一致。
13. **确认后才交接**：每次变更可追溯、可比较、可回退；家庭确认后由设计师复核，再进入报价、BOM 或生产适配层。
14. **不冒充企业数据**：没有欧派真实 SKU、规则、报价或接口时，必须标记 `demo` / `estimate`。

### 1.1 V1.1 产品层级

消费者主工作空间固定为以下四层，默认只展开第一层：

| 层级 | 默认状态 | 负责什么 | 不负责什么 |
|---|---|---|---|
| 1. Agent 设计会话 | 默认展开 | 场景理解、自然表达、方案尝试、关键解释、单问题追问 | 展示完整专业编辑器或长篇问卷 |
| 2. 3D 方案与影响 | 默认主画布 | 浏览住宅、点选对象、预览 Agent 改动、比较取舍 | 充当静态效果图或第二套状态 |
| 3. 上下文微调 | 点选对象后按需出现 | 移动、旋转、替换、材质等当前对象允许的少量调整 | 暴露全量工具、目录和属性树 |
| 4. 高级编辑与交接 | 用户明确进入或流程到达时出现 | Pascal 完整编辑、版本、家庭共识、设计师复核、下游导出 | 成为消费者首屏或默认工作方式 |

层级校正不降低任何既有能力。Gate 18 已有功能必须继续可达、可回放、可验证，只改变默认曝光、任务顺序和信息密度。

## 2. Gate 共同规则

每个 Gate 都严格执行：

1. 开始前记录 Git 分支、HEAD、状态和本 Gate 精确改动路径；当前默认不 commit、不建 remote、不 push。
2. 只实现本 Gate 内容，不提前放置下一 Gate 的假按钮、占位卡或“即将上线”伪功能。
3. 每个 Gate 都交付一个审阅包：
   - 本 Gate 构建了什么、没有构建什么；
   - 可直接操作的本地页面；
   - 同一状态的截图或短录屏；
   - 自动化测试与浏览器控制台结果；
   - 已知问题、下一 Gate 依赖和实际 Git diff。
4. 用户可以要求修改、退回或废弃当前 Gate；默认只有用户明确验收后才进入下一 Gate。Gate 19–24 已获得一次性连续实施授权，改为每 Gate 自动复核并提交、最终统一验收。
5. 若地基能力达不到视觉、性能或交互标准，在当前 Gate 换方案；不得靠后续 UI 遮盖。
6. 任何新增依赖、外部 API、付费资产、授权或企业数据，都在对应 Gate 单独说明用途和退出方案。
7. 接受 Gate 后若用户另行授权，可为该 Gate 创建一个本地 Lore commit；未授权则继续保持未提交状态。

## 3. 页面总图

V1.1 不新增消费者页面，也不把现有能力拆成多个应用。`/project/demo` 仍是唯一家庭主工作空间，但默认任务从“打开编辑器”改为“继续一次 AI 设计会话”：

| 页面 / 视图 | 用途 | 首次出现 |
|---|---|---|
| `/lab/scene` | 临时技术试验页，验证渲染、同源数据和镜头，不是产品页面 | Gate 1 |
| `/project/demo` | Agent 主导的家庭共创空间：实时 3D、自然表达、方案预览、按需微调 | Gate 3 建立，Gate 19 校正层级 |
| Pascal 高级编辑层 | 保留完整装修模拟器能力，仅在用户明确进入时加载和展开 | Gate 15 建立，Gate 21 校正入口 |
| 版本与影响抽屉 | 当前版本、对象差异、影响、撤回与确认 | Gate 7 |
| 家庭共识面板 | 成员意见、冲突、未决项与共同确认 | Gate 9 |
| `/review/:projectId` | 设计师只读复核与退回，不与客户模式混在一个开关里 | Gate 11 |
| `/handoff/:versionId` | 共识单、来源、未决项和下游 JSON 导出 | Gate 11 |

`/project/demo` 的默认顺序固定为：

`进入已布置住宅 → Agent 给出场景化观察 → 用户说 / 点 / 选 → Agent 生成合法预览 → 展示原因与影响 → 必要时微调 → 保存版本 → 家庭确认`

用户必须能够在不打开 Pascal 高级编辑层的情况下完成整条演示黄金路径。

## 4. 串行 Gate

### Gate 0：产品合同、页面骨架与技术边界

**状态：已验收（2026-08-08）。**

审阅包：[GATE-0-PRODUCT-CONTRACT.md](./GATE-0-PRODUCT-CONTRACT.md)

**构建什么**

- 把本计划中的产品合同、页面总图和主流程锁定为后续唯一依据。
- 定义首个演示住宅、家庭成员、房间范围和核心故事，但不创建产品 UI。
- 定义对象能力矩阵：什么可移动、什么可替换、什么只能改材质、什么必须锁定。
- 定义最小数据合同名称：`FloorPlan`、`Room`、`Surface`、`Opening`、`SceneObject`、`Material`、`SceneCommand`、`DesignRule`、`SceneVersion`、`HouseholdMember`、`Confirmation`。
- 决定旧 V1 的隔离方式；未决定前不删除、不搬移现有代码和资产。

**页面**

- 无运行页面；只提供主流程图、低保真页面结构和对象能力表供审阅。

**验收证据**

- 用户可以沿着一条完整旅程逐步指出每一步看见什么、能做什么。
- 页面、数据和角色之间不存在两套互相矛盾的来源。
- 所有尚未决定的事项被明确列为开放问题，而不是被 Codex 默认决定。

**本 Gate 明确不做**

- 不写产品代码，不生成新效果图，不接 AI，不改飞书数据。

**进入 Gate 1 的条件**

- 用户确认本计划的 Gate 顺序和 Gate 0 产品合同。

---

### Gate 1：Canonical Scene 与 2D 户型地基

**状态：已验收（2026-08-08）。**

审阅包：[GATE-1-REVIEW.md](./GATE-1-REVIEW.md)

**目的**

先建立唯一的房屋几何、对象与命令来源。后续 3D、编辑、Agent、版本和导出都只能读取或修改它。

**构建什么**

- 建立可序列化的 `FloorPlan`、`SceneGraph` 和 `DesignRule` 引用结构。
- 使用 11,000 × 8,000 mm 的七空间合成住宅验证整屋关系，包含主卧、卫生间、次卧 / 书房、过厅、开放客餐厅、厨房和玄关。
- 建立唯一 `SceneCommand` 入口；所有未来的手动编辑和 AI 工具都必须调用它，禁止直接散改状态。
- 先用 2D 投影验证房间边界、表面、opening、对象 ID、transform、尺寸和房间归属。
- 将 2D 拆成同源的 `CAD` 与 `media` 两层：CAD 负责外 / 内墙厚度、门窗符号、尺寸链、房间面积和家具足迹；media 使用原创透明家具俯视资产表达装修排布。
- 页面固定提供 `CAD`、`家具俯视`、`叠加` 三种显示模式，使用画布右侧竖向图标工具条；这是 Gate 1 的最终模式数量。
- 家具与叠加模式按 floor material 显示原创浅橡木或暖灰砖纹理；纯 CAD 模式保持无纹理图纸表达。
- 两层只从同一 `SceneObject` 的毫米坐标、尺寸和旋转投影，禁止 UI 手工维护另一套摆放坐标。
- 完成 schema 输入校验、保存、加载、确定性 round-trip 和非法引用测试。

**页面**

- `/lab/scene`：整屋 CAD / 家具媒体同源投影、三种显示模式、对象选择、只读 scene JSON 和校验结果；不伪装成最终产品。

**验收证据**

- 修改模型中的一个房间、墙、opening 或家具后，2D 只从模型重新投影，不存在手工热点坐标。
- CAD 与家具媒体层可单独显示或叠加；媒体资产的锚点、宽深、旋转和 polygon 必须与对应 CAD 家具足迹一致。
- 保存再加载后，墙、门窗、家具、材质引用和对象 ID 不漂移。
- 自动化测试阻止重复 ID、悬空 room / surface 引用和绕过 `SceneCommand` 的状态变更。
- 用户能够审阅 2D 是否正确表达真实户型和空间关系。

**明确不做**

- 不做真实 3D、镜头、家具拖拽、Agent、版本或漂亮数据卡。

---

### Gate 2：真实 3D 视觉上限、资产管线与同源证明

**状态：已验收并以 `080a3b2` 提交（2026-08-09）。**

审阅包：[GATE-2-REVIEW.md](./GATE-2-REVIEW.md)

**目的**

让 Gate 1 的同一场景模型生成真实 3D，并在搭建正式页面前证明 Web 端能达到“不廉价”的家庭渲染质量。

**构建什么**

- 先检查可用的企业 / 商业 3D API、SDK 和素材来源，再决定是否继续使用 Three.js。
- 用 Gate 1 的 scene 在 `/lab/scene` 渲染一个真实客餐厅局部：墙、地面、门窗、灯光和至少 4 件真实网格家具。
- 使用 PBR 材质、正确色彩空间、自然光、柔和阴影和可复现的相机预设；模型、软装与材质统一为原创的当代北欧家居目录质感。
- 验证 glTF / GLB 资产规范、贴图体积、加载时间、帧率、LOD / 缓存策略和授权来源。
- 实现由 `Room` / `Surface` ID 驱动的两段式镜头证明：整屋立体视图点击房间后先飞到该房间的 3D 俯视总览，再切换到入口或指定功能面的预设视角。
- 镜头状态与切换事件借鉴 Shapespark Viewer API 的 `switchToView`、切换开始 / 完成回调和节点点击模式，但只复用交互思想；相机仍属于本项目 canonical scene，不嵌入 Shapespark 页面或复制其视觉。
- 大角度镜头切换使用“观察点平移 + 最短球面圆弧 + 四元数朝向”插值；采用零速度起止缓动并按旋转角自动延长，俯视 heading 与主入口对齐。墙体在所有落点保持 canonical 2800 mm 真高；摄像机接近或穿过某一墙面时只淡化该 surface，离开 550 mm 后恢复实体。
- 2D 与 3D 都从同一对象 ID、transform、尺寸、材质和房间归属生成。

**页面**

- `/lab/scene` 增加 2D / 3D 联动调试视图、相机预设、性能读数和资产来源。

**验收证据**

- 用户先审阅 1440×900 实际浏览器画面和镜头录屏，并确认视觉方向具备当代北欧家居目录质感，不像旧式 Home Assistant 可视化或低模演示。
- 所有可见家具是 3D mesh，不是图片平面或静态背景。
- 修改一个对象 transform 后，2D 与 3D 在同一帧显示同一结果，选中 ID 一致。
- 目标设备持续交互无明显卡顿；控制台无错误；资源来源可追溯。
- 若质量不合格，本 Gate 继续更换渲染或资产方案，后续全部暂停。

**明确不做**

- 不做正式页面、家具拖拽、Agent、版本、飞书或完整房屋。

---

### 夜间后端包 N1：命令事务、Agent Harness 与飞书能力门

**状态：已完成并完成真实 Aily / Base 验收（2026-08-09）；审阅包见 [BACKEND-N1-REVIEW.md](./BACKEND-N1-REVIEW.md)。**

**目的**

在不改变已验收 2D / 3D 页面和不提前进入可视 Gate 的前提下，先建立手动编辑与 Agent 都会复用的确定性后端地基，并把已授权的 Aily / Base 能力接在浏览器之外。

**构建什么**

- 将现有 `SceneCommand` 扩展为可回放的原子事务，支持撤销、重做、失败不留脏状态和分支后清空 redo。
- 使用同一 scene 校验器补充最小的对象碰撞与受保护净距检查；只声明后端地基，不冒充 Gate 6 的完整设计规范。
- 建立工具注册表与 Agent Harness：模型只提出结构化工具调用，本地 planner 校验并执行，禁止模型直接写 scene JSON。
- 提供确定性本地 provider、Aily provider 边界、一次重试后降级、脱敏 trace 和固定输入回放。
- 使用 Node 标准库提供薄 BFF；浏览器不读取飞书 token，项目不新增运行时依赖。
- 验证当前用户的 Aily 项目 scope 和 Base 读写能力；创建并发布开发后台自定义智能体，开启 OpenAPI 渠道，留下真实 turn 与 Base 回读证据。

**验收证据**

- 同一命令序列重复回放得到字节一致 scene；undo / redo、失败原子性和 redo 分支清空均有自动化测试。
- 合法移动能落盘，越界、对象碰撞和侵占硬净距不能提交；所有结果返回稳定错误码和家庭可读解释。
- 至少三种自然语言输入通过 Harness 产生真实 `SceneCommand`；provider 返回非法结构或超时时自动降级且不污染场景。
- `/api/health` 只返回真实 capability 状态；Aily / Base 未经真实调用不得标记 `ready`。
- 已验证一次 `provider → SceneCommand → 规则引擎 → Base` 完整链路，health 为 `Aily ready / real_turn_verified` 与 `Base ready / write_read_verified`。
- `npm test` 与 `npm run build` 通过，Gate 2 技术页无视觉回归。

**明确不做**

- 不修改 `/lab/scene` 或创建 `/project/demo`，不实现拖拽 UI、正式版本面板、家庭共识或设计师复核。
- 不创建欧派真实 SKU、报价、BOM 或施工规则，不发送群消息，不部署或公开分享。

---

### 夜间后端包 N2：合成组件目录与 Prompt 安全边界

**状态：已完成并通过本地与真实 Aily 验证（2026-08-09）；审阅包见 [BACKEND-N2-REVIEW.md](./BACKEND-N2-REVIEW.md)。**

**目的**

让 Agent 不再把装修理解成“只移动家具”，同时在欧派真实 API 尚未提供时，用可替换的合成目录验证墙面、地面、架体、隔断、柜体、门、台面、吊顶与五金的选择边界。

**构建什么**

- 接入 15 项、10 类的本地合成组件目录；所有目录项标记 `source: demo`，价格和工期标记 `source: estimate`。
- 表面饰面可通过既有 `SceneCommand` 真正落到 canonical scene；架体、隔断、门、柜体等缺少安装几何规则的组件只允许检索和澄清，不能假装安装。
- Aily Prompt v2.1 只发送当前意图需要的房间、对象、表面、目录项与工具，避免重复传输完整场景。
- 本地 Harness 校验回复中的无依据数字和高风险施工做法；越界、超时或格式不稳定时确定性降级，不污染 scene。
- Provider 工具调用严格受本轮 allowlist 限制；只读意图会移除全部写工具，本地与 Aily 路径都不能绕过。
- BFF 增加只读目录查询接口，目录 provider 可由后续真实欧派 API 替换。
- Coach 数据请求已按 maintainer 风格整理；用户于 2026-08-09 12:45 私聊罗梓威发送三段核心请求，飞书回读确认成功。详细字段清单尚未作为附件或消息送达；完整记录见 [COACH-ENTERPRISE-DATA-REQUEST-DRAFT.md](./COACH-ENTERPRISE-DATA-REQUEST-DRAFT.md)。

**验收证据**

- 真实 Aily 已成功完成木饰面应用、层板方向澄清与沙发移动；后续单次 provider 波动也能安全降级。
- `npm test`、`npm run build` 与 `npm run eval:agent:live` 提供本地、构建和真实 provider 证据。
- 墙板写入生成真实 `surface.setMaterial`；`sceneReady=false` 的层板不能生成场景写入。

**明确不做**

- 不把合成目录称为欧派真实产品库，不提供真实报价、BOM、施工做法或生产接口。
- 不创建层板 / 隔断 / 柜体的新 3D 几何，不修改 `/lab/scene`，不进入 Gate 3。
- Codex 没有发送 Coach 或群聊消息；用户已亲自私聊发送核心请求。后续不得自动补发、追问或改发群聊。

**企业接入下一步**

- 等待 Coach 对脱敏端到端样例请求的回复；如对方需要字段级说明，再由用户决定是否补发详细清单。
- 一次申请测试环境中的项目、目录、规则、设计版本、报价 / BOM 预览和导入导出权限；明确排除真实客户隐私、生产下单和生产环境写入。
- 若 API 暂不可开放，先接受字段字典、JSON / CSV / Excel 和现有系统导出文件，不让企业接口进度阻塞产品 Gate。

---

### 后端 B1–B4：可恢复状态、评测、规则影响与企业数据入口

**状态：已实现、通过验证并提交（2026-08-09）；仅改后端与测试，不改变 Gate 3 页面，也不视为 Gate 3 验收。**

审阅包：[BACKEND-B1-B4-REVIEW.md](./BACKEND-B1-B4-REVIEW.md)

**B1：项目与版本持久化**

- 使用 Node 标准库将项目、`SceneVersion`、命令记录和 Base 待同步事件原子落盘。
- 服务重启后恢复现场；事件 ID 幂等，`expectedVersionId` 阻止旧版本覆盖新版本。
- 覆盖临时文件替换、损坏文件隔离 / 恢复与无部分写入测试；V1 不引入数据库。

**B2：Agent 自动评测**

- 建立 20–30 个固定离线场景，覆盖工具选择、越权修改、硬约束阻止、回复依据和 provider 降级。
- 输出可比较 JSON 报告、逐案例 trace 和延迟统计；真实 Aily 评测继续作为独立可选证据。

**B3：确定性设计规则与影响计算**

- 覆盖房间边界、对象碰撞、门扇开启、主通道与家具使用净距。
- 统一输出 `blocked | warning | passed`，并计算变更前后的净距、收纳等可由场景数据解释的影响；未知项标记 `unresolved`。

**B4：企业数据导入适配器**

- 支持 JSON / CSV 目录导入，校验毫米单位、尺寸、来源、模块 ID、组合与禁配约束。
- 校验失败整体拒绝，不部分导入；真实 Coach 数据以后只替换 provider，不修改 Agent 的 catalog 工具合同。

**本批明确不做**

- 不修改 `/project/demo`、不进入 Gate 4、不新增数据库 / Web 框架、不伪造欧派 SKU、报价、BOM 或生产能力。
- 不调用真实生产接口、不发送飞书消息、不部署或公开分享。

**验证证据**

- `npm run test:backend`：62/62 通过；`npm test`：88/88 通过；`npm run build` 通过。
- B2 离线固定评测 24/24 通过；真实 Aily 冒烟 3/3 完成，其中 1 个不合规 provider 响应按设计安全降级。
- B1 双进程重启冒烟确认当前版本可从 JSON store 恢复；损坏恢复、事件幂等、并发冲突与 Base 待同步持久化均有自动化测试。

---

### Gate 3：家庭主工作空间与 3D 数字住宅导航

**状态：已验收（2026-08-10）。两轮 5 人模拟住户黑盒测试已完成；第一轮 2 个镜头 P1、对象定位、第二轮小屏 / 过渡状态 P1、双墙剖切与玻璃归属均已修复并复验。**

审阅包：[GATE-3-REVIEW.md](./GATE-3-REVIEW.md)

住户黑盒报告：[GATE-3-RESIDENT-TEST.md](./GATE-3-RESIDENT-TEST.md)

**目的**

先验证用户能否在一个完整立体住宅里完成“看整屋 → 点房间 → 飞到房间 3D 俯视图 → 切换预设视角 → 点选对象”的连续体验；2D 只负责说明当前全屋布局。

**构建什么**

- 建立 `/project/demo` 的最小消费者页面。
- 2D 只展示当前户型和家具布局的俯视总览；3D 占据主要画布并承担浏览、点选和后续编辑。
- 初始状态是可旋转、缩放和平移的 3D 整屋透视 / 剖切住宅视图。
- 点击房间后，镜头先平滑飞到该房间的 3D 俯视总览，保留墙体高度、家具体积和空间关系，不切换成 2D 图片。
- 进入房间后才显示房间级视角按钮；首版至少提供 `俯视`、`入口`、`主功能面`、`自由视角`，不可用的预设按房间数据省略。
- 切换视角只改变同一 3D Scene 的相机，不加载另一张效果图；选中对象和编辑状态在视角间保持。
- 3D 当前房间、目标表面、选中对象和 2D 同步高亮保持一致。
- 提供明确的“返回整屋”动作，并支持浏览器前进 / 后退恢复房间与相机预设状态。
- 编辑信息架构借鉴 Blueprint3D Modern 的 2D / 3D 同源、对象选择、上下文属性面板和按需资产抽屉；不复制其低质 3D 视觉，也不引入第二套 scene 数据。

**页面**

- `/project/demo`：只包含 2D 户型、3D 画布、返回整屋和房间级视角按钮。

**验收证据**

- 用户在整屋 3D 中连续进入至少三个房间；每次都先到达正确的房间俯视总览，且不会穿墙、迷失或跳到错误房间。
- 每个已进入房间都能在 `俯视`、至少两个房间预设与 `自由视角` 间切换；镜头过渡平滑，对象选择不丢失。
- 1024×768 与 1440×900 都能使用，没有六空间缩略图栏和静态图片热点。
- 2D 始终正确反映当前装修俯视布局和 3D 当前所在空间。
- 用户可以明确说出“我现在在哪里、怎样切换这个房间的观察视角、怎样回到整屋透视并进入另一个房间”。
- 第二轮 P1 复验：1024×768、1366×768 与 1440×900 的主画布、2D 总览和位置卡均在一屏内；2D 房间名在 1024 宽视口约为 10 px，主要按钮热区不小于 44 px。
- 第二轮 P1 复验：进入房间或返回整屋时，URL 和镜头请求可先更新，但面包屑、标题、视角和选择摘要只在镜头 `done` 后统一切换；飞行中明确显示目标视角。
- 自由视角的剖切上限为最近两块不同实体墙面；玻璃不单独参与遮挡排序、不占用退让名额，但会随宿主墙面沿同一曲线退让和恢复。
- 视角胶囊提供“自动剖切”开关；关闭后所有墙面沿同一曲线恢复实体，重新开启后只退让实际遮挡视线的实体墙面。

**明确不做**

- 不做家具编辑、Agent、版本或影响卡。

---

### Gate 4：完整客餐厅真实 3D 垂直切片

**状态：已实现、验证并提交（2026-08-10）。审阅见 [GATE-4-REVIEW.md](./GATE-4-REVIEW.md)。**

**目的**

把 Gate 1 的局部质量扩展成一个可用房间，先验证真实资产、灯光、材质、加载和选中精度，再写编辑器。

**构建什么**

- 完整开放客餐厅建筑壳、主要固定构件和生活化家具。
- 所有可见家具登记为真实 `SceneObject`，带类别、尺寸、移动能力、SKU / demo 来源和包围盒。
- 2D 图形、3D mesh、对象名称和属性面板共用同一对象。
- 加载进度、失败占位和资源缓存必须真实可用。

**页面**

- `/project/demo` 的客餐厅视图；增加轻量对象信息面板，但还不能编辑。

**验收证据**

- 画面达到 Gate 1 已确认的视觉标准。
- 点选每件可见家具都命中正确对象，不使用人工百分比热点。
- 对象数量、资源体积、首次加载和稳定帧率有实测记录。

**明确不做**

- 不做拖拽、AI、静态版本图或其他房间的伪预览。

---

### Gate 5：全部家具的直接编辑与撤销

**状态：已实现、验证并完成本地提交（2026-08-10）。审阅见 [GATE-5-REVIEW.md](./GATE-5-REVIEW.md)。**

**目的**

先让用户真正能够修改自己的房间，再允许 Agent 代替用户操作。

**构建什么**

- 3D 中所有 movable 家具都能直接命中和选择；2D 只同步显示选中对象与当前位置。
- 所有标记为 movable 的家具支持移动、旋转；允许的对象支持替换、删除、复制、改材质和参数尺寸。
- 家具的直接拖动、旋转和修改发生在 3D；每个动作写入 `SceneCommand`，2D 俯视图即时同步结果但不承担 CAD 式编辑。
- 建立 undo / redo、取消拖动、键盘操作、对齐与吸附。
- 固定结构和禁止动作给出明确原因，不显示失效按钮。

**页面**

- `/project/demo` 增加对象工具条与属性抽屉，不增加专业 CAD 式工具海洋。

**验收证据**

- 任意可移动家具都能完成选择 → 移动 / 旋转 → 2D / 3D 同步 → 撤销 → 重做。
- 每次操作产生可检查的 `SceneCommand`，没有直接改 mesh 的旁路。
- 键盘与指针均能完成基本编辑；浏览器控制台无错误。

**明确不做**

- 本 Gate 只有防止数据损坏的边界与碰撞底线，不宣称已经满足完整设计规范。

---

### Gate 6：设计规范与受约束编辑

**目的**

把“全部家具可移动”与“不能破坏合理性”真正连接起来。

**构建什么**

- `DesignRule` 分为 hard block、warning、recommendation。
- 首批规则覆盖：房间边界、对象碰撞、门扇开启、主要通道、家具使用净距、防倾倒 / 儿童安全、电视观看距离和固定设备关系。
- 拖动时展示合法区、临界区和不可落点；硬违规允许预览但不能提交，warning 可以由用户确认后保留。
- 规则解释使用普通家庭能理解的语言，并提供合法吸附点或替代位置。
- 每条公开规范记录来源和适用边界；欧派专属规则仍标记待企业数据。

**页面**

- 主工作空间增加就地反馈、规则解释小面板和可视化净距，不增加独立规则后台页。

**验收证据**

- 至少 8 个确定性案例覆盖合法、警告和阻止三类结果。
- 2D、3D、手动编辑和未来 Agent 调用得到相同判定。
- 用户可以理解为什么失败以及下一步可怎样调整。

**明确不做**

- 不用 LLM 决定几何与安全规则，不冒充施工图审查。

---

### Gate 7：版本、真实差异、渲染、影响与确认

**状态：已实现并验证，等待用户验收。**

**目的**

让每次调整成为可追溯的真实场景版本，而不是换一张图和几行数字。

**构建什么**

- `SceneVersion` 保存父版本、命令列表、快照摘要和来源。
- 版本对比展示对象增加 / 删除、transform、尺寸、材质和约束状态差异。
- 高质量效果视图由当前 scene、材质、灯光和相机生成；版本缩略图也从对应版本重建。
- 影响数据先只计算能从几何和 demo catalog 可靠推导的内容；未知报价、工期继续标记 estimate / unresolved。
- 支持保存草稿、撤回旧版、客户确认，以及确认后再改进入 `changed_after_confirm`。

**页面**

- 主工作空间增加版本 / 影响抽屉和按需版本对比层。

**验收证据**

- 移动、替换、改材质后生成 V2，旧版仍可完整重建。
- 对比画面、对象 diff、约束和影响来自同一场景变化。
- 确认后修改不会覆盖已确认版本。

**明确不做**

- 不生成无法从数据解释的精确价格、工期或业务收益。

---

### Gate 8：真实 Agent 工具调用与 Designer Skill

**状态：已实现并验证，等待用户验收；当前 capability gate 为 Aily `missing_scope` / Base `ready`，页面明确使用本地降级。审阅证据见 [GATE-8-REVIEW.md](./GATE-8-REVIEW.md)。**

**目的**

在手动编辑和规则已可信后，让轻量 Agent 使用同一套能力操作真实房间。

**构建什么**

- 先通过 capability gate 选择可用的真实模型 / Aily API；需要授权时只请求最小权限。
- Agent 工具只包装现有 `SceneCommand`、规则检查、catalog、版本和影响能力。
- 支持：读取房间、选择对象、移动 / 替换 / 删除 / 改材质、检查规则、比较版本、请求确认。
- 实现“懂 → 做 → 变 → 解释 → 问”和渐进式信息获取；能做就先做，只在缺失信息阻碍当前设计时问一个主要问题。
- Designer Skill 提供设计流程与解释策略，确定性规则仍是最终裁决者。
- API 失败时明确显示降级状态，不用预写提案冒充真实 Agent。

**页面**

- 主工作空间增加轻量 Agent sidecar；它辅助场景，不成为覆盖画布的聊天产品。

**验收证据**

- 至少 5 个自然语言任务真实改变 scene，并留下工具调用、规则与版本证据。
- Agent 能看到用户刚完成的手动修改；用户也能继续编辑 Agent 的结果。
- 硬约束违规时 Agent 不产生非法版本，而是解释并给出可执行替代。

**明确不做**

- 不让 LLM 直接写原始 scene JSON，不做万能 RAG 平台，不训练自有视觉模型。

---

### Gate 9：家庭共享、意见冲突与共识

**状态：已实现并验证，等待用户验收。审阅证据见 [GATE-9-REVIEW.md](./GATE-9-REVIEW.md)。**

**目的**

实现产品区别于普通 3D 配置器的核心：家庭在同一空间共同形成方案。

**构建什么**

- 家庭成员身份、意见、修改归属、偏好和确认状态。
- 同一个共享方案，不创建互相不可见的秘密版本。
- 成员可以针对房间、对象或版本表达支持、反对、补充和不可妥协项。
- Agent 识别冲突，说明功能、视觉与规则代价，提出多个完整可行方向供家庭选择。
- 家庭共识状态与 `SceneVersion` 绑定。

**页面**

- 主工作空间增加按需家庭面板与活动记录；默认仍以房屋为主。

**验收证据**

- 三名成员完成一次有冲突的真实流程：分别表达 → AI 识别冲突 → 修改场景 → 查看代价 → 形成共同确认。
- 每个意见和最终决定都能追溯到成员、对象和版本。

**明确不做**

- V1 不做复杂权限系统、成员私密方案或大规模实时协同基础设施；若只做顺序身份切换，必须明确标注演示边界。

---

### Gate 10：完整住宅与多空间扩展

**目的**

在一个客餐厅的完整纵向闭环通过后，再证明架构能够覆盖真实住宅，而不是复制几张不同房间图片。

**构建什么**

- 将同一户型扩展为完整住宅建筑壳和房间导航。
- 每个进入演示的房间必须使用同一 scene、真实 mesh、完整选择和规则；不得出现只读伪空间。
- 至少覆盖三种不同设计问题：
  - 开放客餐厅：动线、会客与跨空间收纳；
  - 主卧：衣柜、床侧净距、梳妆 / 储物；
  - 儿童房 / 书房：成长、活动、安全与未来调整。
- 玄关、厨房等是否进入 V1，由 Gate 10 开始前按资产和规则成熟度决定；只展示就必须可真实导航，显示家具就必须可选择和受约束编辑。

**页面**

- 仍是同一个 `/project/demo`，通过 2D 户型与镜头进入不同空间，不新增房间卡片墙。

**子 Gate**

- Gate 10A：主卧；用户单独验收。
- Gate 10B：儿童房 / 书房；用户单独验收。
- Gate 10C：其余住宅空间；范围在 10A / 10B 通过后再决定。

**验收证据**

- 点击所有纳入范围的房间都能正确飞入。
- 所有可见可移动家具都能选中、编辑、校验和版本化。
- 不同空间使用不同规则与故事，而不是更换文案复用同一提案模板。

**明确不做**

- 不为满足“六个入口”指标制造不可操作的预览房间。

---

### Gate 11：飞书、设计师复核与下游交接

**目的**

产品闭环稳定后，再连接真实协作与企业适配层，避免飞书或后台结构反过来决定消费者体验。

**构建什么**

- Aily 可用时承担自然语言与解释；Base 保存项目、空间、版本、活动和确认事件。
- 设计师在独立 `/review/:projectId` 查看结构化需求、场景 diff、规则告警、来源和未决项，并批准或退回。
- `/handoff/:versionId` 输出脱敏共识单和机器可读 JSON。
- Base 写入使用事件 ID 幂等补写；失败保留本地状态并明确显示未同步。
- 报价、BOM、生产接口仍是 adapter；未获得真实企业数据时不伪装接通。

**页面**

- 设计师复核页、下游交接页；消费者主界面不增加“设计师模式”开关。

**验收证据**

- 一条真实链路可回查：家庭表达 → 场景变更 → 版本 → 家庭确认 → 设计师复核 → Base 留痕 → JSON 导出。
- UI 精确显示 `Live Aily + Live Base`、`Local / Other Agent + Live Base` 或 `Offline Demo`。

**明确不做**

- 不发送群消息、不联系 Coach、不创建真实订单、不生产下发、不公网分享。

---

### Gate 12：商业化视觉、质量验证与比赛交付

**当前状态：V1 Demo 已实现并通过 134 项全量测试、28 案例 Agent 评测、三视口浏览器 QA 和住户黑盒完整链路，等待用户最终验收。审阅包见 [GATE-12-REVIEW.md](./GATE-12-REVIEW.md)，演示脚本见 [DEMO-SCRIPT.md](./DEMO-SCRIPT.md)，设计 QA 见 [design-qa.md](./design-qa.md)。**

**目的**

只有核心场景和因果链全部真实后，才进行最终视觉 polish、性能优化和演示包装。

**构建什么**

- 使用 Product Design 对真实产品页面做 audit / image-to-code polish，而不是让视觉稿重新定义产品结构。
- 完成桌面响应式、键盘可达性、加载 / 错误 / 空状态、资源优化和性能预算。
- 删除 `/lab/scene` 或将其从交付构建中排除。
- 更新 README、产品边界、数据来源、5 分钟演示脚本、设计 QA 和飞书证据。
- 装配比赛文档，但公网部署、团队分享和正式提交仍需单独授权。

**页面**

- 最终消费者工作空间、版本 / 家庭面板、设计师复核与交接页。

**验收证据**

- `npm test`、生产构建、Sites 测试和核心浏览器流程全部通过。
- 1440×900、1366×768、1024×768 可用，控制台无未处理错误。
- 5 分钟内走完：查看 2D 全屋总览 → 在 3D 整屋中点房间并飞入 → 点选家具并手动编辑 → 规则反馈 → Agent 修改 → 家庭共识 → 版本确认 → 设计师复核 → 飞书留痕 / 导出。
- 最终 `design-qa.md` 为 `passed`，但 QA 只证明已确认产品的视觉实现，不再替代产品验收。

---

### Gate 13：空间层级、安装锚点、碰撞代理与可替换 3D 资产合同

**当前状态：已验收并以 `ce6d917` 提交（2026-08-11）。**

审阅包：[GATE-13-REVIEW.md](./GATE-13-REVIEW.md)

**目的**

在进入墙面、固定系统与水电点位前，先让所有现有对象具备稳定的房间层级、安装宿主、确定性碰撞代理和可替换模型槽。后续 AI 生成或企业提供 GLB 时，只替换资产，不重建对象 ID、空间关系、版本或规则。

**构建什么**

- 在现有 `SceneObject` 上补齐 `hierarchy`、`placement`、`collision`、`review` 与可追溯 `model3D` 元数据；不创建第二套 asset scene。
- 家具与固定安装件都归属真实房间；固定安装件必须有合法 surface 宿主，不能靠视觉位置冒充安装关系。
- 设计规则使用独立的简化碰撞代理，渲染仍使用 GLB 与真实对象尺寸；未来更换 AI 模型不会改变碰撞和净距结果。
- 新增受 capability 约束的模型替换 `SceneCommand`，保留对象 ID、room、transform、placement 与模型槽，只递增资产 revision。
- 2D、3D、版本比较、设计师复核和交接导出继续读取同一 canonical scene；3D 在模型路径变更时正确卸载并重新加载。
- 当前页面只展示必要的层级、碰撞、模型槽与专业复核信息；不提前做最终视觉 polish。

**验收证据**

- 缺少层级、宿主、碰撞、模型追溯或专业复核合同的对象不能通过 scene validation。
- 碰撞代理与渲染尺寸可独立存在；手动编辑和 Agent 已有命令仍经过同一规则总线。
- 模型替换命令可撤销、重做和字节一致回放；不可替换对象和远程模型 URL 被拒绝。
- 模型替换后 3D 仍沿用同一对象选中 ID、位置与房间，并触发资产重载。
- `npm test`、后端测试、Agent eval 与生产构建通过。

**明确不做**

- 不在本 Gate 接 AI 生成服务、不生成新 GLB、不做 mesh 级物理引擎。
- 不在本 Gate 新增墙体拆改、固定系统、水电管线或施工计算；这些能力只复用本 Gate 的层级、宿主、碰撞与复核合同继续实现。

---

### Gate 14：墙面、地面与顶面饰面

**当前状态：已实现、完成自动化与后台住户黑盒复测，并以 `9d268f1` 保存（2026-08-11）。用户已授权进入开源前端迁移。**

**目的**

在同一 canonical scene 的 `Surface` / `Material` 合同上补齐墙面、地面与顶面饰面，让用户和 Agent 修改的不再只有家具，同时保留后续欧派目录与 AI 生成资产的替换边界。

**构建什么**

- 每个房间都由 canonical scene 持有可校验的 floor、wall 与 ceiling surface；顶面只表示饰面，不冒充吊顶结构或灯光工程。
- 所有饰面修改继续只经过既有 `surface.setMaterial`，并在命令层校验材质适用面，禁止墙面材质写到地面或顶面。
- 3D 房间内可直接选择表面；属性区只展示该表面可用的 demo 饰面。2D、3D、版本、Agent 与导出读取同一个 materialId。
- 顶面在室内视角可见，在整屋与俯视镜头中平滑淡出，避免遮挡空间浏览。
- 版本比较、设计师复核和共识导出明确列出表面饰面变化与 demo 来源。
- 本地 Agent 支持在已选墙面、地面或顶面上应用合法饰面；未选目标时先澄清，不猜测整屋批量修改。

**本 Gate 精确路径**

- `PLAN.md`、`GATE-14-REVIEW.md`
- `app/src/domain/demo-scene.js`、`scene.js`、`projection.js`、`design-version.js`、`handoff.js`
- `app/src/catalog/demo-catalog.js`、`app/src/agent/harness.js`
- `app/src/Scene3D.jsx`、`app/src/App.jsx`、`app/src/styles.css`
- `app/tests/gate14-surface-finishes.test.mjs` 及受影响的既有回归测试

**验收证据**

- 墙、地、顶三类表面都可选择并通过同一命令修改；非法材质组合、锁定表面和缺失目标都稳定拒绝且不污染 scene。
- undo / redo、保存 / 恢复和命令重放后，表面 materialId 字节一致。
- 3D 与 2D 投影读取相同 surface 数据；顶面不遮挡整屋或房间俯视。
- 版本差异、设计师复核和导出包含表面变化与 `source: demo`，不宣称真实欧派 SKU、报价或施工。
- 领域测试、Agent 测试、站点测试、生产构建与浏览器核心流程通过。

**明确不做**

- 不做墙体拆改、吊顶造型、灯槽、灯具、电气或施工计算。
- 不在本 Gate 增加固定柜、层板、隔断和背景墙对象；这些属于 Gate 16。
- 不接入或宣称真实欧派产品、报价、BOM、工期或生产数据。

---

### Gate 15：Pascal Editor 开源装修模拟器迁移

**当前状态：已验收并以 `059c560` 提交（2026-08-11）。迁移前 V1 已以 tag `v1-pre-pascal-20260811` 保存；审阅证据见 [GATE-15-REVIEW.md](./GATE-15-REVIEW.md)。**

**目的**

停止继续从零补齐通用装修编辑器能力，把时间集中到 Agent、家庭共识、规则和企业接入；以 MIT 开源的 Pascal Editor 1.0 beta 作为嵌入式 2D / 3D 编辑前端。

**构建什么**

- 将完整 Pascal Editor 组件嵌入 `/project/demo`，复用其 2D / 3D、对象选择、移动、旋转、材质、墙体与空间编辑界面。
- canonical scene 仍是唯一业务真相；Pascal 只保存可随时重建的界面投影，不成为第二套版本或数据库。
- 建立 `canonical scene → Pascal SceneGraph` 单向投影，保留房间、表面、opening、家具的 canonical ID 映射。
- Pascal 中的本地操作必须先翻译为现有 `SceneCommand`，通过规则引擎后才回写并重新投影；拒绝动作不污染 canonical scene。
- Agent、undo / redo、版本、家庭共识、设计师复核、飞书留痕和交接继续使用现有合同，不直接依赖 Pascal store。
- 保留本地 shim 与第三方许可证；不把项目迁移到 Next.js，不 fork 或复制整套 Pascal 源码。

**验收证据**

- 七个房间、全部对象、表面和 opening 能从同一 canonical scene 导入；2D / 3D 返回同一 canonical ID。
- 家具移动、旋转、对象材质和墙地顶饰面都生成现有 `SceneCommand`，规则拒绝、undo / redo、版本 replay 仍有效。
- Safari 与 Tailnet 页面不白屏；生产构建、全量测试、后端测试和 Agent 评测通过。
- 开源前端可以移除后回到保存 tag；迁移不改变真实欧派数据仍为 pending 的边界。

**明确不做**

- 不让 Pascal IndexedDB 或 undo history 取代 canonical scene / version history。
- 不让 Agent 直接写 Pascal scene，不建立双向自动同步。
- 本 Gate 不增加固定柜、层板、隔断、背景墙或真实欧派目录；这些顺延到 Gate 16。

**后续资源优化门槛（不阻塞当前迁移）**

- 将本次多个 3D 渲染器与测试浏览器叠加造成的高 CPU 占用作为性能回归场景；单一项目页不得长时间占用多个 CPU 核心。
- 宿主只保留一个活跃渲染实例；标签隐藏或窗口失去可见性时暂停渲染，卸载时释放 geometry、material、texture、BVH 和 WebGL / WebGPU 上下文。
- 浏览器自动化与视觉 QA 每轮只启动一个实例，验证后必须关闭并检查无遗留渲染进程。
- 移动端不直接承诺完整桌面编辑器：默认 2D 或房间级轻量 3D，按设备能力降低 DPR、阴影、材质、模型 LOD 和同时加载范围。
- 后续性能 Gate 需用真实移动设备验收首帧、稳态 CPU / GPU、内存峰值、隐藏页占用与持续操作温升；桌面构建通过不能替代设备证据。

---

### Gate 16：固定装修组件与可替换 3D 资产槽

**状态：已实现并验证（2026-08-11）。**

审阅包：[GATE-16-REVIEW.md](./GATE-16-REVIEW.md)

**目的**

让装修范围从活动家具与表面饰面扩展到固定柜、层板、隔断和背景墙，同时沿用 Gate 13 的层级、安装宿主、碰撞代理与模型替换合同，为后续 AI 生成 GLB 或欧派真实资产留出稳定入口。

**构建什么**

- 在 canonical scene 中加入演示级固定柜、墙面层板、空间隔断和背景墙组件；每个组件必须归属房间并引用合法 surface / floor 宿主。
- 固定组件使用独立的碰撞代理与净距规则，渲染资产和未来 AI 生成模型只替换 `model3D` 槽，不改变对象 ID、宿主、transform、规则、版本或共识引用。
- Pascal 只投影并编辑 canonical 允许的属性；不可移动固定组件不能通过拖拽绕过宿主关系，允许的材质、尺寸或模型替换继续翻译为现有 `SceneCommand`。
- 合成目录补充与上述组件对应的 `source: demo` 项，并保持企业目录 provider / import adapter 可替换；未获得欧派数据时不出现真实 SKU、报价、BOM 或施工结论。
- 版本差异、Agent、设计师复核与交接导出能识别固定组件的新增、材质 / 尺寸 / 模型变化和来源。

**页面**

- 仍使用 `/project/demo`；固定组件进入 Pascal 资产与对象层级，不新增第二个装修器或独立组件页面。

**验收证据**

- 固定组件缺少房间、宿主、碰撞代理或模型追溯时 scene validation 失败。
- Pascal 与 Agent 对固定组件的合法操作生成同形 `SceneCommand`；非法移动、越界、碰撞和不兼容材质不污染 scene。
- `object.setModelAsset` 保留组件 ID、宿主与碰撞代理，可撤销、重做和确定性回放。
- 全量领域测试、后端测试、Agent 评测与生产构建通过。

**明确不做**

- 不实现 AI 3D 生成服务、mesh 级物理、墙体拆改、水电、施工计算或真实生产下发。
- 不为未来接口搭建第二套资产数据库；企业或生成式资产只通过现有 provider / `model3D` 槽接入。

---

### Gate 17：性能、资源生命周期与移动设备降级

**状态：已实现并验证（2026-08-11）。**

审阅包：[GATE-17-REVIEW.md](./GATE-17-REVIEW.md)

**目的**

把 Pascal 迁移后的高 CPU / GPU 风险变成可验证的资源合同，避免桌面多渲染器叠加与移动设备直接加载完整编辑器。

**构建什么**

- `/project/demo` 同一时刻只保留一个 Pascal 渲染实例；组件卸载、route 切换和可见性变化时复用上游生命周期能力暂停或释放资源。
- 隐藏标签页暂停高频渲染；恢复可见时不丢 canonical 选择、视图和编辑状态。
- 使用已有懒加载边界拆分 Pascal 编辑器大包；主壳、设计师复核和交接页不提前加载 3D 编辑器。
- 依据视口、精细指针和浏览器公开设备能力选择 `full` / `light` 能力档；移动端默认 2D / 轻量浏览，不承诺桌面级自由编辑。
- 自动化和视觉 QA 每轮只启动一个浏览器实例并在结束后关闭；保留包体、首屏与运行时资源基线。

**页面**

- `/project/demo` 增加非侵入的轻量模式说明与手动进入 3D 的入口；桌面完整模式不改变 canonical 能力。

**验收证据**

- 隐藏 / 恢复、mount / unmount 和 route 切换不产生重复渲染实例或未处理错误。
- 桌面、1024 宽与移动尺寸都无横向溢出；移动端不自动启动完整 3D。
- 生产构建输出记录 Pascal chunk 与主壳 chunk；复核 / 交接路由不请求 Pascal chunk。
- 全量测试与生产构建通过；浏览器完成一次隐藏 / 恢复与移动轻量模式回归。

**明确不做**

- 不宣称未实测的真机温升、电池、GPU 或 Safari iOS 指标；这些保留为正式设备验收。
- 不 fork Pascal 渲染内核或新写渲染调度器。

---

### Gate 18：最终信息层级、视觉收口与 V1 交付

**状态：已实现并验证（2026-08-11）。**

> Gate 18 完成的是开源编辑器、业务面板和既有闭环的工程整合。用户于 2026-08-12 确认这些能力继续保留，但消费者默认体验仍偏向编辑器；后续 Gate 19–23 只校正主次层级，不否定 Gate 18 的实现与验证。

审阅包：[GATE-18-REVIEW.md](./GATE-18-REVIEW.md)

**目的**

在功能与性能边界稳定后，统一主工作台、Pascal 编辑区、Agent、版本 / 家庭 / 复核 / 交接的信息层级，使 V1 看起来像一个以空间为主角的当代家装产品，而不是开源编辑器与业务面板的拼接。

**构建什么**

- 使用已选欧美北欧家居目录概念图作为视觉目标，保留暖白、浅木、克制酒红和真实空间主画面；不复制 IKEA 品牌、蓝黄配色、商品图或页面结构。
- 收紧顶栏、编辑器工具、上下文属性区与 Agent 的优先级；同一时刻只展示当前任务需要的一个主要侧栏，避免 Pascal 与宿主重复 inspector。
- 中文化宿主可控的关键导航、状态和操作；第三方编辑器无法安全覆盖的文本列为明确边界，不做脆弱 DOM hack。
- 完成 1440×900、1366×768、1024×768 和移动轻量视图；补齐焦点、键盘、错误、空状态和 reduced-motion 基础。
- 使用 Product Design 当前轮截图审计与 Build Web Apps 浏览器回归，更新 `design-qa.md`、README、5 分钟演示脚本和最终审阅包。
- 用后台住户角色走完整链路：浏览空间 → 选择 / 修改 → 规则反馈 → Agent → 版本 → 家庭共识 → 设计师复核 → 交接导出。

**页面**

- `/project/demo`、`/review/:projectId`、`/handoff/:versionId`；`/lab/scene` 继续只作为开发技术页。

**验收证据**

- 当前轮截图、DOM、控制台与核心交互证据齐全；参考图和最终截图在同一 QA 轮通过 `view_image` 对照。
- `design-qa.md` 记录至少五项具体对照点、修复结果、文字差异和保留边界，最终状态为 `passed`。
- 全量测试、后端测试、Agent 评测与生产构建全部通过；浏览器无框架错误层或未处理业务错误。
- 每个 Gate 有独立 Lore commit，最终工作树干净；不创建 remote、不 push、不公网部署。

**明确不做**

- 不以静态效果图替代实时 scene，不承诺照片级渲染、手机端完整编辑或企业生产可用性。
- 不在视觉收口阶段重写 Agent、规则、版本或 Pascal 数据边界。

---

### Gate 19：Agent-first 主工作空间与首个 30 秒

**状态：已完成。默认 Agent 侧栏、场景化开场、三层任务入口和按需 Pascal 高级编辑已落地；证据见 `GATE-19-REVIEW.md`。**

**目的**

只调整 `/project/demo` 的默认信息层级，让住户进入后首先感受到“Agent 正在和我一起设计这套住宅”，而不是“我需要学习一个装修编辑器”。

**主要改动路径**

- `app/src/App.jsx`：主工作空间状态、默认视图、按需面板和首屏任务顺序。
- `app/src/styles.css`：3D、Agent、上下文操作和次级抽屉的视觉优先级。
- `app/src/PascalStage.jsx`：只调整宿主进入 / 收起合同，不改 Pascal 内核。
- `app/tests/`：新增产品层级和首屏可达性回归。

**构建什么**

- 默认首屏继续显示已合理布置的真实 3D 住宅，不出现空白项目、长问卷、全量资产目录或展开的 Pascal 工具区。
- Agent 显示与当前房间、选中对象和版本有关的简短开场；没有选中对象时从整屋与家庭目标开始，不使用通用欢迎词。
- 主画布只长期保留 3D、轻量当前位置和一个自然表达入口；2D、版本、家庭意见和完整编辑器通过明确动作按需打开。
- 点选对象后只出现三个任务入口：`让 Agent 调整`、`快速微调`、`高级编辑`。每个入口继续使用既有 canonical ID 和命令边界。
- 高级编辑入口继续加载现有 Pascal；退出后回到原房间、相机、选择和版本，不创建第二套工作流。

**验收证据**

- 新住户在不打开高级编辑器的情况下，能在 30 秒内看懂当前住宅、选择房间或对象并提交一句需求。
- 首屏没有同时展开 Pascal inspector、Agent、家庭和版本四套侧栏；同一时刻最多一个主要侧栏。
- `2D`、`版本`、`家庭意见`、`高级编辑`全部仍可在两次明确操作内到达，并能无损返回主会话。
- URL 中的 room / view / select、浏览器前进后退、移动轻量档和当前 trackpad 行为不回归。
- 全量测试、生产构建、1440×900、1366×768、1024×768 浏览器流程通过。

**明确不做**

- 不修改 Agent 推理、prompt、规则、版本域或企业 adapter。
- 不删除 Pascal、不重写 2D / 3D、不重做视觉资产。

**进入 Gate 20 的条件**

- 用户确认默认页面已经从“编辑器主导”变成“Agent 与住宅主导”，且既有能力没有丢失。

---

### Gate 20：自然设计回合、渐进画像与方案预览

**状态：已完成。自然设计回合、渐进画像、合法预览与用户确认边界已落地；证据见 `GATE-20-REVIEW.md`。**

**目的**

把现有受约束 Agent 从“命令解析侧栏”提升为真正推进设计的轻量设计师：先理解并尝试，再解释一个关键判断，只追问一个当前最重要的问题。

**主要改动路径**

- `app/src/agent/harness.js`、`app/src/agent/prompt.js`：回合协议、场景化表达、工具与只读边界。
- `app/src/App.jsx`：提案预览、影响反馈、追问和接受 / 退回流程。
- `app/server/project-store.mjs`：最小设计简报持久化；不把对话历史塞进 scene。
- `app/tests/agent-harness.test.mjs`、`app/tests/agent-eval.test.mjs`、`app/tests/project-store.test.mjs`：固定回归与恢复证据。

**构建什么**

- 新增项目级 `DesignBrief`，只保存 `goals`、`hardConstraints`、`softPreferences`、`confirmedObjects` 和 `unresolvedIssues`；它引用 canonical ID，但不复制几何、家具或版本状态。
- 每个回合采用固定节奏：`理解 → 能做则生成合法预览 → 展示变化与影响 → 解释一个关键判断 → 最多追问一个问题`。
- 住户通过短句、对象点选和快捷目标进入同一 Harness；不新增第二套聊天或规则实现。
- 默认给出一个明确推荐方向；只有在约束冲突或存在真正不同的合法取舍时，才给出两个可比较方向，不为凑数生成提案卡。
- Agent 写操作继续先形成可撤销预览，经规则检查和用户接受后才保存版本；“先看看”“给方向”“不要改”等表达保持只读。
- 真实 Aily、Local Agent 和降级状态继续如实显示。支持的演示指令必须先给即时处理反馈；Aily 超时或异常不能冻结场景、输入或版本。
- 回复删除口气词、泛化赞美和无依据承诺；不超过一个主要问题，不输出未被 scene、规则或 catalog 支持的数字和施工结论。

**验收证据**

- 固定黄金路径“主卧太满但收纳别少”无需打开编辑器即可形成合法预览、影响说明和一个后续问题。
- 信息不足时场景不变，只问一个能解除阻塞的问题；只读意图、provider 越权和失败回合均保持 scene / version 字节一致。
- 同一项目刷新后恢复 DesignBrief、当前版本和未决项；旧项目没有 brief 时安全建立空简报，不破坏历史版本。
- Agent 固定评测新增自然表达、约束冲突、单问题、无口气词、无写意图和降级案例；全部通过。
- 本地黄金路径从提交到可见处理反馈不超过 300 ms；确定性支持路径的合法预览目标不超过 2 秒。真实 Aily 延迟单独记录，不伪装成本地时延。

**明确不做**

- 不让 LLM 直接写 scene、版本或 DesignBrief 原始 JSON。
- 不在本 Gate 接企业 RAG、参考图视觉理解、真实报价或生成式 3D。

---

### Gate 21：住户微调与 Pascal 高级编辑分层

**状态：已完成。住户快速微调与 Pascal 高级编辑已按能力分层，退出后由 Agent 汇总差异；证据见 `GATE-21-REVIEW.md`。**

**目的**

保留当前成熟编辑能力，同时把普通住户的默认操作收敛为针对当前方案的少量修正，避免再次把设计责任推回用户。

**主要改动路径**

- `app/src/App.jsx`、`app/src/PascalStage.jsx`、`app/src/styles.css`：上下文操作、进入 / 退出高级编辑和影响回顾。
- `app/src/pascal/pascal-to-command.js`、`app/src/pascal/trackpad-navigation.js`：仅在现有桥接需要时修正，不新建编辑协议。
- `app/tests/pascal-command-bridge.test.mjs`、`app/tests/pascal-trackpad.test.mjs`、`app/tests/transform-controls.test.mjs`：能力不丢失与输入回归。

**构建什么**

- 快速微调只显示当前对象 capabilities 允许的操作；固定组件、opening 和表面不获得伪移动能力。
- 完整移动、旋转、尺寸、替换、材质、墙体与空间编辑继续由 Pascal 提供，但必须由用户明确进入高级编辑。
- 从高级编辑返回时，Agent 用确定性差异总结“改了什么、触发了什么规则、是否需要保存版本”，不要求用户自己阅读属性树。
- 手动和 Agent 修改继续共用 `SceneCommand`、规则、undo / redo、版本和影响，不增加同步器或旁路存储。

**验收证据**

- 住户通过快速微调完成一次合法家具调整，不看 Pascal inspector；需要完整操作时可进入 Pascal 并返回原上下文。
- 进入 / 退出高级编辑十次不丢 room / view / select、不重复挂载渲染器、不产生第二份 scene。
- 触控板平移、缩放、旋转和拖拽稳定；操作结束后手柄不会因无关重渲染失效。
- hard block、warning 预览、undo / redo、保存、恢复和版本 replay 全部继续通过现有回归。

**明确不做**

- 不删减 Pascal 的专业能力，不 fork 上游编辑器，不重写 transform controls。
- 不让快速微调演变成另一套轻量编辑器。

---

### Gate 22：把家庭共识、版本和确认放回设计流程

**状态：已完成。版本保存、家庭分歧、共同确认与设计师复核已由 Agent 串成连续下一步；证据见 `GATE-22-REVIEW.md`。**

**目的**

让家庭意见、冲突、版本和确认成为 Agent 设计回合自然产生的阶段，而不是与主体验并列的一组后台面板。

**主要改动路径**

- `app/src/App.jsx`：方向比较、家庭意见、版本保存和确认的会话式编排。
- `app/src/domain/household-consensus.js`、`app/src/domain/design-version.js`、`app/src/domain/design-impact.js`：优先复用现有合同，只补缺失验证。
- `app/tests/household-consensus.test.mjs`、`app/tests/design-version.test.mjs`、`app/tests/design-impact.test.mjs`、`app/tests/handoff.test.mjs`。

**构建什么**

- Agent 在检测到家庭目标冲突时明确指出冲突和代价，提供少数完整方向；不替家庭投票、不把意见平均成模糊折中。
- 方案接受后才提示保存版本；版本保存后才进入家庭意见与确认；确认后修改继续进入 `changed_after_confirm`。
- 版本、家庭与影响抽屉保留，但默认从当前设计回合的结果卡进入，不长期占据主画面。
- 客户确认、设计师复核、飞书 Base 留痕和精确 handoff versionId 继续使用现有后端闭环。

**验收证据**

- 三个家庭成员可在同一版本表达不同意见，Agent 生成两个真实合法方向，并保留原始分歧。
- 用户可从 Agent 结果完成 `保存 → 家庭逐人确认 → 客户确认 → 设计师复核 → handoff`，全程不进入高级编辑器。
- 确认后修改、旧版恢复、拒绝复核、Base pending 和精确版本导出没有回归。
- 所有价格、产品、工期和企业字段继续带 `demo` / `estimate` / `pending` 来源。

**明确不做**

- 不实现真正同时在线的多人协同、账号权限或通知泛滥。
- 不重写现有版本与家庭共识领域模型。

---

### Gate 23：住户黑盒复测、视觉收口与 V1.1 交付

**状态：已完成。五类住户黑盒任务、加载态修复、家庭表达收口、相机录屏和 V1.1 交付证据已完成；见 `GATE-23-REVIEW.md`。**

**目的**

证明层级校正后的产品能够被普通住户理解和完成，而不仅是功能仍然存在；最后再统一视觉、触控板、资源和比赛演示。

**主要改动路径**

- `app/src/styles.css` 与必要的宿主组件：只修复经过截图和住户任务证明的问题。
- `design-qa.md`、`README.md`、`DEMO-SCRIPT.md` 和新的 Gate 23 审阅包。
- 现有站点、Agent、领域、后端与性能测试。

**构建什么**

- 使用至少五个独立模拟住户画像进行后台黑盒任务，禁止读取源码 / DOM 语义捷径，禁止抢占用户前台；分别覆盖首次装修、忙碌家庭、老年住户、预算敏感和远程家庭成员。
- 核心任务固定为：看懂住宅 → 表达需求 → 查看 Agent 改版 → 理解影响 → 必要微调 → 家庭确认 → 设计师复核 → 交接。
- 对 3D 旋转、平移、缩放和房间飞行录制一段受控视频并抽帧检查抖动、遮挡、穿模和资源泄漏。
- 完成 1440×900、1366×768、1024×768、触控板和移动轻量模式 QA；同一轮只启动一个后台浏览器实例，结束后确认没有遗留渲染进程。
- 最后使用 Product Design / Sites / Build Web Apps 审计当前截图，只修 P0 / P1 / P2，不以视觉重写掩盖流程问题。

**验收证据**

- 至少五名模拟住户均可在不打开高级编辑器的情况下完成黄金路径；报告只写任务证据，不外推真实用户完成率。
- 首屏 30 秒任务、一个完整 5 分钟演示、触控板操作和视频抽帧均有可复查证据。
- `npm test`、后端测试、Agent 固定评测、站点测试和 `npm run build` 全部通过；控制台无未处理错误。
- `design-qa.md` 最终为 `passed`，且明确保留客户级写实、真实欧派数据、移动端完整编辑和生产系统尚未完成的边界。
- Gate 19–23 各自拥有独立审阅证据；只有用户授权后才创建对应 Lore commit，不建 remote、不 push、不公网部署。

**明确不做**

- 不在交付 Gate 引入新框架、第二个 Agent、第二套 scene 或新的通用编辑器。
- 不以模拟住户测试冒充真实用户研究或业务 KPI。

---

### Gate 24：设计方向导航、动态体验样机与最终风格选择

**状态：实施中。研究与四套方向合同已完成，正在实现导航页与可操作样机。**

**目的**

在不改变 Gate 19–23 产品层级的前提下，为导航页和后续消费者体验建立四套可真实操作的原创设计方向。参考来自 Agent Reach 检索到的优秀建筑、家居、AI 画布和 3D configurator 网站；只提取布局、节奏、动效和信息层级，不复制品牌、素材、代码或商业页面结构。

**研究输入与借鉴边界**

| 方向 | 研究参考 | 提取什么 | 不复制什么 |
|---|---|---|---|
| `calm-catalog` 静谧目录 | Vitra configurator、欧美家居目录 | 大留白、浅木与暖白、动态 modal、材质比较、产品信息克制出现 | Vitra / IKEA 品牌、商品图、图标和页面结构 |
| `spatial-cinema` 空间电影 | smart / BMW 3D configurator | 3D 为绝对主角、镜头驱动导航、状态之间的连续转场、轻量说明 | 汽车视觉、广告镜头、品牌字体和素材 |
| `agent-canvas` 智能画布 | Ruumix、dMaya | Agent 与可编辑结果共处、方案并排、上下文动作、对话不吞掉画布 | 对方产品文案、无限画布实现和卡片布局复刻 |
| `architect-index` 建筑索引 | Garonzi、Mobius 建筑网站 | 纵向 / 横向导航、强排版、渐进揭示、页面位置感 | 项目照片、作品集内容、GSAP / Lenis 技术栈复制 |

其中 `spatial-cinema` 与 `architect-index` 必须提供真实动态体验；`agent-canvas` 也应包含状态响应动效。所有动效使用现有 React、CSS 和浏览器能力实现，不新增动画依赖，并尊重 `prefers-reduced-motion`。

**主要改动路径**

- `app/src/App.jsx`：导航入口、方向页路由、同一产品体验的 style 参数与返回路径。
- `app/src/styles.css`：四套局部 design tokens、方向页布局和动效；业务组件不复制四份。
- 可选新增 `app/src/DesignDirections.jsx`：只承载方向元数据与展示结构，不拥有 scene、Agent、版本或编辑状态。
- `app/tests/`：路由、键盘、reduced-motion、风格隔离和黄金路径回归。
- `GATE-24-REVIEW.md`、`design-qa.md`：来源、截图、动态证据、偏差检查与用户选择记录。

**构建什么**

- 新增一个轻量导航首页，直接说明产品是“AI 先设计、家庭再调整”的家装共识环境，并提供 `继续当前项目`、`查看四个设计方向`、`高级编辑`三个不同优先级入口；高级编辑不得成为主 CTA。
- 新增四方向审阅页。每个方向都使用同一套真实产品文案、当前项目状态和 Agent-first 旅程，不使用 lorem ipsum、静态假按钮或独立业务状态。
- 每个方向均可进入同一个 `/project/demo`，通过 URL 中的展示参数应用外壳风格；canonical scene、room / view / select、Agent、版本和家庭状态完全共享。
- `calm-catalog`：以安静的编辑排版、材质标签和低噪声导航展示真实住宅。
- `spatial-cinema`：以 3D 舞台、轻量景深层次、镜头状态转场和进入房间的连续动效展示住宅；禁止自动长动画阻塞操作。
- `agent-canvas`：把 Agent 当前判断、可见场景变化和两种设计方向组织成同一工作画布；编辑器仍按需进入。
- `architect-index`：用清晰的房间 / 阶段索引、横向方向切换与位置反馈组织项目；动效只帮助定位，不制造滚动劫持。
- 方向选择仅保存展示偏好，不进入 canonical scene 或版本，不影响导出；用户未选择时使用稳定默认风格。

**每个方向必须通过的产品精神检查**

- 首屏第一主角仍是住宅与 Agent，不是工具栏、风格选择器或营销动画。
- 住户可在不进入高级编辑器的情况下提交需求、查看预览、理解影响和继续确认。
- 编辑入口存在但视觉权重低于 `让 Agent 设计 / 继续设计`。
- 动效不延迟输入、不遮挡对象点选、不改变相机 / scene 状态、不在移动轻量档强制运行。
- 任何风格切换后，room / view / select、未保存修改、版本和 Agent 上下文保持一致。

**验收证据**

- 四个方向各有 1440×900 与 1024×768 同状态截图；至少两个方向有 8–12 秒受控录屏和抽帧接触表。
- 四个方向均可完成 `进入项目 → Agent 表达 → 预览 → 快速微调 → 保存版本`，且高级编辑不是必经步骤。
- 键盘、触控板、reduced-motion、浏览器前进后退和移动轻量档通过；无横向页面溢出、滚动劫持或焦点陷阱。
- `npm test`、站点测试、Agent 评测和 `npm run build` 全部通过；控制台无未处理错误。
- `GATE-24-REVIEW.md` 对每个方向分别记录：参考来源、原创变化、Agent-first 检查、住户操作摩擦、性能边界和推荐使用场景。
- 用户起床后可以只通过导航页在四个方向间切换并选择继续打磨的方向；未选择前不删除其余方向。

**明确不做**

- 不复制参考网站代码、图片、视频、字体文件、品牌元素或受保护商品资产。
- 不为四个方向复制四套业务组件、路由状态、scene、Agent 或版本系统。
- 不引入 GSAP、Lenis、Framer Motion 或新的 UI 框架；现有 CSS / Web Animations 足以支撑本 Gate。
- 不在用户选定方向前把某一套样机散布到复核页、交接页或企业后台。

## 5. 旧实现处理结果

- 用户于 2026-08-08 验收 Gate 0，并明确要求丢弃此前错误版本、修复 Git 状态并 commit。
- 旧 `app/`、旧截图、旧演示脚本、旧视觉 QA、旧 README 和旧 `.omx` 计划指针已整体移至 `/Users/dracoglasser/.Trash/oppein-legacy-v1-pre-gate1-20260808/`，可以恢复但不留在新工作区。
- 历史 `FEISHU_EVIDENCE.md` 继续保留，只证明当时的 Base / Aily 能力状态，不作为新产品实现。
- 旧代码中的 BFF、幂等记录、构建或测试思想只能在对应 Gate 重新实现和验证；不得从废纸篓直接复制整套旧模块。
- AgentVault 中的项目首页、决策、交付物和待确认事项已同步到当前三模式与 floor-material Gate 1 路线，对应本地 commit `595cda0`。

## 6. 下一步

1. 用户先审阅本次产品层级校正以及 Gate 19 的首屏与默认任务；未验收前不修改产品代码。
2. Gate 19 只调整默认曝光和页面任务顺序，证明现有能力能够从“编辑器主导”切换为“Agent 设计会话主导”；验收后再进入 Agent 回合改造。
3. Gate 20–Gate 22 依次完成自然设计回合、渐进画像、微调 / 高级编辑分层以及家庭共识流程编排；不并行重写共同文件 `app/src/App.jsx`。
4. Gate 23 进行多住户后台黑盒测试、录屏抽帧、触控板复验和基础 UI polish；测试不得抢占用户前台。
5. Gate 24 再用 Agent Reach 研究结果制作四套共享业务内核的方向样机，用户起床后从导航页统一验收和选择，不提前删除候选。
6. 真实欧派数据到达后按 [BACKEND-B1-B4-REVIEW.md](./BACKEND-B1-B4-REVIEW.md) 的目录适配边界接入，不改 Agent catalog 工具合同。
7. AI 生成 3D 与企业 GLB 只替换 Gate 13 / 16 的 `model3D` 槽；真实报价、BOM、工期和生产写入继续保持 `pending`，直到端到端验证。

## 7. 本次计划校正的停止条件

满足以下条件后，V1.1 不再继续扩 Gate：

- 普通住户无需学习 Pascal 即可完成完整黄金路径；
- Agent 的每轮设计都对应真实 scene 预览、确定性影响和最多一个主要问题；
- 快速微调和高级编辑都保留，但不再控制默认产品心智；
- 家庭意见、版本、确认、复核与交接从设计结果自然进入；
- 现有 canonical scene、SceneCommand、规则、版本、Aily / local fallback、Base 和企业 adapter 没有被复制或绕过；
- 所有自动化与浏览器证据通过，仍未获得的企业能力继续诚实标记为 `pending`。
- 四套设计方向共用同一产品和业务状态，至少两套动态方向通过 reduced-motion 与性能回归，用户可以从统一导航页直接比较。
