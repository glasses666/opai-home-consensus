# Gate 23 审阅：住户黑盒复测与 V1.1 收口

## 黑盒住户任务

同一个后台 Chromium 依次执行五个独立干净会话，避免并发 3D 占用：

1. 首次装修：模糊表达 → 单一追问 → 主卧合法预览 → 保存 → 家庭下一步。
2. 忙碌家庭：两位成员相反意见 → 保留原话 → 两个合法方向。
3. 年长住户：1024×768 快速微调；无横向溢出、无尺寸 / 删除误操作。
4. 预算敏感：预算偏好、暖白材质与收纳硬约束进入同一回合；不伪造降价。
5. 远程成员：意见绑定成员、对象和 V1，可在共享活动中回看。

五个独立评审代理只查看各自的运行截图和任务结果，不读取源码。报告不冒充真实用户研究或完成率。

## 根据反馈修复

- P0：Pascal 载入前画布空白。现以同源 2D 户型和家具作可操作加载态，3D 完成后原位替换。
- P1：家庭方向使用工程方位、原话不显眼。现直接展示成员原话，主标签改为位置方案 A / B，毫米信息退为说明。
- P1：预算请求容易被误解为已省钱。现明确“已记录预算偏好，但真实成本需欧派报价 API”。
- P1：内部编号、SceneCommand / geometry JSON 暴露给住户。快速层已改为生活化标签，技术边界改为可读说明。
- P1：实时同源易被误解为实时多人。家庭标题处明确“Demo 顺序切换，不是实时多人同步”。

## 产品精神复核

- Agent-first：通过。五条任务均从自然表达或 Agent 结果推进，微调不是必经路径。
- 用户编辑为辅：通过。住户 Pascal 微调只有移动、旋转与宽深高。
- 操作方便：通过。1024×768 无横向溢出；预览、撤销、保存和家庭下一步均在当前上下文出现。
- 诚实边界：通过。真实多人、报价、SKU、BOM、施工与客户级写实均未冒充完成。

## 验证

- `npm test`：175 / 175。
- `npm run test:backend`：81 / 81。
- `npm run eval:agent`：28 / 28，`passed: true`。
- `npm run build`：通过；保留既有大 chunk 警告。
- 五类后台黑盒会话与修复复验：控制台错误 0。
- 旋转 / 缩放视频：`.omx/audits/gate23/camera-orbit-zoom.webm`；12 帧抽检见 `camera-contact-sheet.png`，未见黑帧、视角抽搐或重复 canvas。

## 保留边界

- 当前仍是风格化实时 3D，不是客户级离线写实渲染。
- 模拟住户只能证明任务可运行，不能替代真实用户研究。
- Gate 24 只封装导航和体验方向，不再重写 Agent、scene、Pascal 或家庭流程。
