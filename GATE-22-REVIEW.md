# Gate 22 审阅：家庭共识回到设计流程

## 已完成

- Agent 预览保存后直接提示进入家庭共识，并明确不会替家庭投票。
- 手动保存的新版本也进入同一家庭流程，不创建旁路版本。
- 检测到同一对象上的相反立场时，Agent 保留原始意见并提示生成少数合法方向。
- 应用方向后形成真实新版本；三位成员逐人确认后，Agent 再引导到版本影响与设计师复核。
- 报价、BOM、施工与企业字段继续保留 `pending` / `estimate` 边界。

## 产品精神复核

- Agent-first：通过。Agent 负责组织下一步、解释冲突和边界，不替人做最终决定。
- 用户编辑为辅：通过。完整家庭闭环不要求进入高级编辑器。
- 操作方便：通过。结果消息提供单一上下文动作，住户无需猜测下一面板。
- 共识可信：通过。意见绑定成员、对象和版本；冲突不被平均成模糊折中。

## 验证

- `npm test`：175 / 175 通过；既有家庭、版本、复核和 handoff 合同全部通过。
- 后台 Chromium 完整路径：Agent 预览 → 保存 V2 → 两位成员相反意见 → 两套合法方向 → V3 → 3 / 3 确认 → 版本与交接。
- 1366×768 降级路径复验无控制台错误；Aily 异常时由本地 Harness 完成同一流程。
- 截图：`.omx/audits/gate22/agent-to-household-to-version-1440x900.png`。

## 保留边界

- 顺序身份切换仍是 Demo，不声明真实多人在线、账号权限或推送通知。
- Gate 23 负责黑盒住户复测、可用性修复和 V1.1 交付证据。
