# Gate 27 审阅：多案例风格 RAG 种子集

## 结果

- 独立分支：`codex/cli-harness-style-dataset`；不合并 `main`，不改网页编辑器。
- Gate 26 的 28 种风格继续作为分类与需求澄清地图。
- Gate 27 为北欧、日式北欧、极简、当代、中世纪现代、静奢、新中式和工业风各增加 10 个住宅案例，共 80 张结构化案例卡。
- 案例按空间、装修系统、家具、灯光、适用性和风险投影为 480 个原子 chunk；本 Gate 未引入向量库或更改 Harness。
- 新增独立评测集，包含小户型、家庭收纳、气候、预算、跨风格冲突以及拒绝精确报价 / 结构安全越界声明。

## 数据边界

- 原页只作为 `reference_only` 证据；未新增或复制第三方图片。
- 案例文本是短摘要和产品化翻译，不复制来源长段落。
- 预算档位、风格强度和适用性标注为 `curated_estimate`；不写入未公开价格、工期、法规数值或商业效果声明。
- `sourceFacts / curatedInferences / unknowns` 分开存储；家庭、尺寸或软装无可验证证据时保留为 `unknown`。
- 语料仍是 RAG 种子集，不代表全球风格全覆盖；不训练模型，不声称解决了统计学意义上的过拟合。

## 多样性证据

- 每个风格恰好 10 个案例，80 个 URL 不重复。
- 验证器会拒绝某一风格 10 个案例全部落在同一房间、户型、强度和地域的假覆盖。
- 当前每个风格包含至少 7 个地域标签、2 种户型和 2 种强度；房间标签从 20 到 46 个不等。
- 北欧、日式北欧、中世纪现代和工业风都含明确非刻板案例；例如深色北欧、潮湿气候 Japandi、小户型中世纪与家庭化工业 loft。

## 验证

- 契约测试：5 / 5 通过。
- 契约证明：80 个案例、480 个 chunk、ID / URL 唯一、8 风格每个 10 个案例。
- 全仓测试：190 / 190 通过；生产构建通过，仅保留既有大 chunk 警告。
- 独立验证 Agent 复查通过，无 P0 / P1 / P2；剩余风险是来源仍偏二级建筑媒体，因此只能作为 `reference_only` RAG 种子证据。
- `app/src/agent/harness.js`、`app/src/agent/prompt.js` 和网页 UI 在本 Gate 无改动。

## 下一 Gate 进入条件

先对这 80 张案例做人工抽检和检索评测；只有当 Top-k 能区分户型、家庭、风格强度和风险边界时，才让 CLI Harness 读取检索结果。Aily 仍不得因一个风格词直接修改 scene。
