# Gate 20 审阅：自然设计回合与方案预览

## 已完成

- 新增最小 `DesignBrief`，只保存目标、硬约束、软偏好、已确认对象和未决项；不复制 scene 几何或聊天记录。
- 住户短句、对象点选和快捷目标进入同一 Harness；信息不足时只追问一个问题，场景保持不变。
- 固定黄金路径“主卧太满但收纳别少”会保留收纳、移动双人床 100 mm、检查规则，并形成可撤销预览。
- Agent 写操作不会直接保存版本；用户必须选择“保留并保存”或“撤销预览”。
- DesignBrief 随项目持久化，旧项目缺少该字段时安全回落为空简报。

## 产品精神复核

- Agent-first：通过。住户只需表达目标，Harness 负责理解、规划、校验和解释。
- 用户编辑为辅：通过。黄金路径无需打开 Pascal；手动工具不是 Agent 失败后的默认建议。
- 操作方便：通过。每轮只有一个建议、一个关键判断和至多一个后续问题。
- 数据可信：通过。LLM 不直接写 scene、版本或 DesignBrief；所有写操作仍经确定性工具与规则。

## 验证

- `npm test`：175 / 175 通过。
- `npm run test:backend`：通过。
- `npm run eval:agent`：通过。
- `npm run build`：通过；仅保留现有大 chunk 警告。
- 后台 Chromium 黄金路径：`.omx/audits/gate20/bedroom-agent-preview-1440x900.png`；无控制台错误，预览、简报计数与 Agent 回复均符合预期。

## 保留边界

- 本 Gate 不接企业 RAG、真实报价、施工结论或生成式 3D。
- 住户快速微调与 Pascal 高级编辑的进一步分层进入 Gate 21。
