# Coach 企业资料与 API 权限请求记录

日期：2026-08-09
状态：**核心短消息已由用户私聊发送；详细清单尚未作为附件或消息发送；群聊未发送**

## 1. 这次请求的目标

当前项目已经跑通同源 2D / 3D 场景、Aily 结构化工具调用、场景规则校验、版本留痕和一套合成组件目录。下一步不是索取欧派全部数据库，而是拿到一套可在测试环境跑通的**脱敏端到端样例**，把当前 `demo` / `estimate` 适配器替换成真实企业适配器。

最有用的交付不是一句“有 API”，而是同一组稳定 ID 能串起来的一条链路：

`量房 / 户型 → 设计版本 → 产品与材质 → 组合校验 → 报价 → BOM → 现有设计或生产系统导出`

我们不需要真实客户隐私，不申请真实下单、排产、支付或生产修改权限。

## 2. 已发送给 Coach 的短消息

> 罗老师您好，我们这边已经跑通 2D / 3D 同源场景、Aily 修改链路和合成组件目录，现在准备接企业真实数据。为了后面不反复补问，想请您帮忙把下面这份“最小接入清单”转给产品库、设计系统和报价 / BOM 对应同事确认。
>
> 最优先需要的是一套脱敏的端到端样例：量房 / 户型、产品模块与材质、组合校验、报价 / BOM，以及现有系统的导入导出结果；同时给到接口文档和测试环境权限即可。如果暂时不能开放 API，JSON、CSV、Excel 或现有系统导出文件也可以，我们先按文件适配。
>
> 我们不需要真实客户数据，也不申请生产下单权限。烦请相关同事按附件标一下哪些能提供、需要什么权限、由谁对接，最好能给一套能互相对应的样例 ID。

## 3. P0：最小可运行接入包

以下内容优先级最高。若一次只能准备一部分，请先准备这一组。

### A. 测试环境与接口说明

- 测试 / 沙箱环境地址和 API 版本。
- 鉴权方式，以及应用注册、凭证申请或 OAuth 授权流程。
- OpenAPI、Swagger、Postman Collection 或最小接口清单。
- 限流、请求体大小、超时、异步任务 / 回调方式、错误码和版本冲突规则。
- 是否需要 IP 白名单、VPN 或指定网络环境。
- 一位技术接口人，以及产品库、设计系统、报价 / BOM 各自的业务接口人。

### B. 一套脱敏端到端样例

请尽量让下列内容来自**同一个脱敏项目**，并保留可关联的稳定 ID：

- 一个量房或户型项目及其结构化数据。
- 至少一个初始设计版本和一个修改后版本。
- 版本中实际使用的产品模块、SKU、材质、颜色和五金。
- 一次组合 / 设计规则校验结果。
- 一份报价预览和一份 BOM 预览。
- 一份可导入或导出到欧派现有设计 / 报价 / 生产系统的文件或请求样例。
- 项目、空间、对象、产品、材质、版本、报价、BOM 之间的 ID 映射说明。

### C. 量房与户型数据合同

- 单位、坐标原点、坐标轴方向、楼层和版本号。
- 房间 ID、名称、用途、边界 polygon 和面积。
- 墙体中心线或边界、厚度、高度、内外墙属性。
- 门、窗和洞口的位置、宽高、离地高度、开启方向。
- 梁、柱、台阶、层高、吊顶、地面高差等建筑约束。
- 不可移动结构，以及插座、开关、给排水、燃气、暖通等点位（若现有系统有）。
- 量房数据的状态：草测 / 已复核、修订版本、误差或置信度（若有）。

### D. 产品与装修组件目录

家具之外也请覆盖墙地顶和固定装修组件。建议至少包含：活动家具、定制柜体、墙面 / 护墙板、地面、层板 / 架体、隔断、门、台面、吊顶、五金，以及现有系统管理的水电设备。

每类至少需要以下字段：

- 产品、模块、SKU、系列、品类、空间用途和有效状态。
- 固定尺寸或可调范围、步进值、单位、安装方向和锚点。
- 材质、饰面、颜色、纹理、五金和可选配置组。
- 可替换组、可组合项、禁配项和适用产品线 / 区域。
- 安装依附面、必要净距、门扇 / 抽屉开启空间等放置条件。
- 生命周期、版本、生效区域 / 门店，以及数据更新时间。
- 对应 2D / 3D 资产 ID 和缩略图 ID。
- 数据来源字段，便于区分正式产品、历史产品、估算或仅展示资产。

### E. 2D / 3D 几何与材质资产

- 2D 家具足迹、CAD 图块或参数化平面表达。
- 3D 模型或参数化模型；请注明实际支持的格式，例如 GLB / glTF、FBX、OBJ、DWG、DXF 或企业自有格式。
- 统一的单位、原点 / pivot、朝向、包围盒和缩放约定。
- 材质槽、纹理贴图、颜色空间和产品材质 ID 的映射。
- 模型版本与产品 / 模块 ID 的对应关系。
- 可否将少量脱敏资产用于本次比赛 Demo，以及允许展示到什么范围。

### F. 欧派专属设计、组合与生产规则

通用建筑和人体工学规则我们可以先自行实现；更需要欧派内部规则的机器可读样例：

- 规则 ID、名称、版本、适用产品线 / 地区和生效时间。
- 输入对象、触发条件、计算表达式或校验接口。
- 严重级别：禁止、警告、需人工复核。
- 给设计师 / 客户看的原因说明和可替代方案。
- 模块组合 / 禁配、尺寸跨度与支撑、门板 / 抽屉开启、安装面、净距、收口、五金适配、制造可行性等约束。
- 厨卫、防潮、防火、水电点位等规则若属于现有系统，也请说明可提供范围。

### G. 报价、BOM 与工期

- 报价预览接口或导出样例，以及必填输入字段。
- 价格口径：区域、门店、渠道、币种、税、折扣、有效期和非标加价。
- 产品、材质、五金、人工、安装、运输等是否分项，以及不可提供的边界。
- BOM 行项目 ID、产品 / 模块 / 材质 / 五金、数量、单位和损耗字段。
- 设计版本、报价版本与 BOM 版本如何关联和锁定。
- 工期 / 交期的来源、更新时间和置信范围。
- 返回结果属于估算、可签约报价还是可生产结果，以及进入下一状态所需的人工复核。

### H. 设计系统与下游交接

- 读取、创建、修改测试设计，以及保存 / 读取版本的方式。
- 设计 / 组合校验、报价预览、BOM 预览接口。
- 欧派现有设计系统支持的导入 / 导出结构和必填字段。
- 客户确认、设计师复核、未决项、退回原因和版本状态如何表达。
- 异步任务的轮询或 webhook、幂等键、重试和版本冲突处理方式。
- 下游接收共识 JSON 时必须保留的项目、空间、对象和版本字段。

## 4. P1：有条件时一并提供

### I. 官方案例与风格体系

- 可用于测试的官方案例 ID、图片、空间、风格、系列和标签。
- 案例图背后实际使用的产品、模块、材质和颜色映射，而不只是图片链接。
- 企业内部风格 / 系列分类和推荐逻辑。
- 少量脱敏案例用于比赛 Demo 的展示许可范围。

### J. 真实业务流程与指标口径

- 量房、导购沟通、设计、报价、客户确认、下单、生产、安装之间的实际角色和交接步骤。
- 哪些节点必须人工审批，哪些状态可以自动流转。
- “首次确认轮次、报价复核时长、确认后重大变更率、版本错用”等指标的内部定义；如可分享，再提供脱敏基线。
- 这里优先要**口径和流程**，不强求敏感经营数据。

## 5. 建议一次申请的测试权限

只申请测试 / 沙箱环境：

| 能力 | 权限 | 优先级 |
|---|---|---:|
| 项目、量房与户型 | 读取 | P0 |
| 产品、模块、材质与几何资产 | 读取 / 下载脱敏样例 | P0 |
| 设计、组合与生产规则 | 读取或执行校验 | P0 |
| 测试设计 | 创建、读取、修改、保存版本 | P0 |
| 报价 | 预览，不生成真实订单 | P0 |
| BOM | 预览 / 导出，不生产下发 | P0 |
| 现有设计系统 | 测试导入 / 导出 | P0 |
| 异步任务 | 查询状态；必要时订阅测试 webhook | P1 |
| 官方案例与风格库 | 读取脱敏子集 | P1 |

明确不申请：

- 真实生产下单、排产或生产数据修改。
- 支付、退款和合同签署。
- 真实客户姓名、手机号、地址、身份证或完整订单数据。
- 全库批量导出和未经批准的对外发布权。
- 生产环境写权限。

## 6. 暂时没有 API 时的替代交付

API 暂时不可开放，不会阻塞试点。按优先顺序可替代为：

1. 字段字典 + 脱敏 JSON 样例 + 现有系统导出文件。
2. CSV / Excel 产品表、规则表、报价表和 BOM 表。
3. 少量 GLB / FBX / DWG / DXF 或企业实际使用的几何资产。
4. 只能在企业环境查看时，可提供测试租户或安排一次受控联调；机器可读样例仍优先于截图。

最低可用替代包：**一个脱敏户型、一条设计版本链、一组跨品类真实组件、一份校验结果、一份报价、一份 BOM 和一份下游导出**。

## 7. 方便对方一次回复的回执格式

请对应同事在每项后标注：`可提供 / 需脱敏 / 仅测试环境 / 暂不可提供`，并填写：

| 系统或资料 | API / 文件格式 | 测试或生产 | 所需权限 / 前置条件 | 样例能否提供 | 对接人 | 预计时间 | 使用限制 |
|---|---|---|---|---|---|---|---|
| 量房 / 户型 |  |  |  |  |  |  |  |
| 产品 / 材质 / 资产 |  |  |  |  |  |  |  |
| 规则校验 |  |  |  |  |  |  |  |
| 设计版本 |  |  |  |  |  |  |  |
| 报价 |  |  |  |  |  |  |  |
| BOM |  |  |  |  |  |  |  |
| 下游导入 / 导出 |  |  |  |  |  |  |  |
| 案例 / 风格库 |  |  |  |  |  |  |  |

## 8. 发送边界

- 本文件不代表欧派已承诺提供任何接口或数据。
- 2026-08-09 12:45，用户在罗梓威私聊中分三段发送了第 2 节正文；消息 ID 为 `om_x100b68b41194d8a0ddc200e062ccfd9`、`om_x100b68b4110caca8c4c70967bc74e9c`、`om_x100b68b41135bca4c077435f387a56b`。
- 12:46 用户另发送握手表情；群聊没有发送任何请求。
- 当前会话未检测到附件或第 3—7 节详细清单，因此准确状态是“核心请求已发送，详细字段清单待需要时补充”，不是完整请求包已送达。
- 未经用户再次明确授权，不自动补发、追问或转发到群聊。
