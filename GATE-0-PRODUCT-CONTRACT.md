# Gate 0：产品合同与技术边界

状态：**已验收（2026-08-08）**

日期：2026-08-08

上位计划：[PLAN.md](./PLAN.md)

本文件是 Gate 0 的审阅包，不是第二份实施计划。`PLAN.md` 决定 Gate 顺序；本文件只把进入 Gate 1 前必须统一的产品事实、演示假设、数据所有权和开放问题写清楚。

## 0. 标记规则

- **已锁定**：用户已经明确确认，后续不再自行改写。
- **演示假设**：为了能开始构建而选定的可替换测试夹具，不代表真实客户或欧派业务事实。
- **开放**：尚未决定，不能被代码暗中固定。
- **历史排除**：旧 V1 的实现或文档，只保留作证据与零件候选。

## 1. 一句话产品定义

**已锁定**

这是一个面向普通家庭的 AI 共创设计环境：家庭在同一套真实、可编辑、受规则约束的住宅模型里看、说、改、比较并形成共识；确认后的结构化结果再交给设计师和企业下游深化。

它不是：

- 静态效果图导览或图片热点 Demo；
- 让普通用户从空白开始操作的缩水 CAD；
- 只会聊天、不会改变住宅模型的机器人；
- 替代欧派报价、BOM、生产或施工系统的新后台；
- 六张房间图片拼成的多空间展示站。

## 2. 唯一产品旅程

**已锁定的主干**

```mermaid
flowchart LR
    A["取得结构化户型"] --> B["打开已合理布置的整屋 3D"]
    B --> C["点击房间"]
    C --> D["镜头飞到房间 3D 俯视总览"]
    D --> E["切换入口 / 主功能面 / 自由视角"]
    E --> F["点选家具、表面或功能区"]
    F --> G["用户直接修改或自然语言表达"]
    G --> H["同一 SceneCommand 进入规则校验"]
    H --> I["场景真实改变并解释影响"]
    I --> J["版本、家庭意见与未决项"]
    J --> K["家庭确认"]
    K --> L["设计师复核"]
    L --> M["报价 / BOM / 生产适配层"]
```

### 第一次 30 秒

**已锁定**

1. 默认前提是上游已经提供结构化户型；V1 不把 AI 户型识别当创新点。
2. 用户不先填长问卷，也不面对空房间或产品目录。
3. 第一眼是自己的整屋 3D 剖切 / 透视住宅，里面已有一版遵守基本规则的可讨论方案。
4. 2D 户型只作为同步俯视总览，可随时查看，但不承担主要导航或 CAD 编辑。
5. 用户点击一个房间，镜头先飞到该房间的 3D 俯视总览；随后才出现房间视角按钮。
6. 用户可以先点一个物件或说一句“这里我不喜欢”，而不是先学会使用工具。

**开放**

- 首屏中 2D 总览采用固定小窗、抽屉还是临时切换，留到 Gate 3 的页面体验审阅决定。
- Agent 的具体位置、展开方式和第一句话留到 Gate 8；在此之前不放聊天占位栏。
- 如果只有户型图片而没有结构化数据，V1 只把图片当人工建模参考；是否增加半自动识别属于后续独立范围。

## 3. 2D、3D 与效果图的职责

| 表达层 | 唯一职责 | 可以做 | 不可以做 |
|---|---|---|---|
| 2D 户型 | 展示当前住宅及装修布局的同步俯视总览 | 显示房间、墙体、门窗、家具、尺寸、当前位置和选中对象 | 成为第二套状态、图片热点导航、主要家具编辑器 |
| 实时 3D | 消费者主浏览与主编辑空间 | 整屋浏览、房间飞入、视角切换、对象点选以及后续受约束编辑 | 使用方块假模型、与 2D 使用不同对象 ID、按视角换静态图片 |
| 高质量效果视图 | 同一 3D 场景的高质量呈现结果 | 使用相同场景、版本、材质与相机重新渲染 | 拥有独立家具状态、用预生成图片冒充修改结果 |

### 3D 镜头状态机

```text
whole_home
  └─ click room → room_overhead（每次进入房间的默认状态）
       ├─ 俯视
       ├─ 入口
       ├─ 主功能面（可按房间存在多个）
       └─ 自由视角

任意房间状态 ─ 返回整屋 → whole_home
```

- `room_overhead` 是保留墙体高度、家具高度和空间关系的 3D 俯视镜头，不是 2D 图片。
- 视角按钮只改变同一个场景的相机；当前房间、选中对象和未提交操作不得丢失。
- Home Assistant Floor3D 只提供“数字住宅可浏览、对象可点选”的交互参照，不提供视觉或页面模板。

## 4. 视觉与模型方向

**已锁定**

- 页面和 3D 住宅都采用原创的当代北欧家居目录气质：自然光、真实比例、浅木、暖白、织物和克制层级。
- 参考欧美 IKEA 的生活方式、空间亲和力和工具易用性，但不复制品牌、商品、蓝黄配色或页面结构。
- 家具必须是有真实体积、比例、材质和可选择边界的 3D mesh；禁止把低模方块、白模或老旧仪表盘质感当成最终体验。
- 视觉 polish 不能先于场景同源、镜头和对象命中证明；否则漂亮界面会掩盖错误地基。

## 5. 首个演示住宅

以下全部是 **演示假设**，用于让 Gate 1–10 有同一个可复算测试夹具；它不是用户画像研究结论，也不使用真实客户信息。

### 住宅夹具

| 字段 | 演示值 |
|---|---|
| ID | `demo-home-001` |
| 名称 | 城市三口之家 · 合成住宅 |
| 类型 | 两室两厅的合成户型，所有几何和产品来源标记 `demo` |
| 上游输入 | 已确认的结构化量房数据；不是 AI 识图结果 |
| 核心空间 | 玄关、开放客餐厅、厨房、主卧、儿童房 / 书房、卫生间、阳台 |
| 产品验证空间 | 开放客餐厅 → 主卧 → 儿童房 / 书房 |

### 家庭夹具

| 成员 | 演示偏好 | 可形成的冲突 |
|---|---|---|
| 成员 A · 项目发起人 | 空间松弛、自然光、会客 | 不接受大体量柜体压迫空间 |
| 成员 B · 共同决策者 | 隐藏收纳、预算可控、影音设备 | 希望保留更多储物和设备能力 |
| 成员 C · 7 岁儿童 | 阅读、活动、可成长 | 当前兴趣与未来学习布局需要兼容 |

家庭设计空间默认共享、意见公开。成员身份在 V1 可顺序模拟，不假装已经有真实账号和多人实时协同。

### 三类设计故事

1. **开放客餐厅**：增加收纳与保持开阔、通行和会客之间的取舍。
2. **主卧**：衣柜容量、床侧净距、梳妆 / 储物与视觉轻盈之间的取舍。
3. **儿童房 / 书房**：当下活动、安全、收纳与未来成长调整之间的取舍。

### 各 Gate 使用范围

| Gate | 使用住宅范围 |
|---|---|
| Gate 1 | 玄关 + 开放客餐厅相邻结构，验证 canonical scene 与 2D 投影 |
| Gate 2 | 开放客餐厅局部，验证真实 3D 质量和资产管线 |
| Gate 3 | 整屋建筑壳；至少开放客餐厅、主卧、儿童房 / 书房可完成镜头导航 |
| Gate 4–9 | 只把开放客餐厅做成完整可编辑、规则、版本、Agent 与家庭共识纵向闭环 |
| Gate 10A | 主卧完整化，单独验收 |
| Gate 10B | 儿童房 / 书房完整化，单独验收 |
| Gate 10C | 再决定玄关、厨房、卫生间和阳台是否进入完整范围 |

Gate 3 中尚未完整化的房间只允许展示真实建筑壳和镜头能力，不放不可选择的假家具，也不宣称功能完成。

## 6. 页面结构

### `/lab/scene`：Gate 1–2 技术证明

- 只用于检查 canonical scene、2D 投影、3D、相机、对象 ID、性能和资源来源。
- 不使用最终消费者导航，不放 Agent、版本、家庭面板或营销数据。
- Gate 12 删除或排除出交付构建。

### `/project/demo`：家庭主工作空间

这是一张工作空间，不是六张房间卡片组成的首页。

```text
┌──────────────── 项目与当前状态（轻量） ────────────────┐
│                                                        │
│  [按需 2D 总览]      真实 3D 整屋 / 房间主画布          │
│                      · 返回整屋                        │
│                      · 房间视角按钮                    │
│                      · 对象点选与直接编辑              │
│                                                        │
│  [按需 Agent]        [选中对象的就地工具 / 属性]        │
│  [按需家庭面板]      [按需版本与影响抽屉]               │
└────────────────────────────────────────────────────────┘
```

这只是信息区域关系，不锁定最终左右位置、宽度、色彩和组件样式。具体视觉必须在相应 Gate 用真实页面状态审阅。

### 下游页面

- `/review/:projectId`：设计师只读复核、批准或退回；不作为消费者首屏模式开关。
- `/handoff/:versionId`：输出已确认项、未决项、来源、规则状态和机器可读共识结果。

## 7. 对象能力矩阵

能力必须来自数据，不从 UI 类型或 mesh 名称猜测。未声明能力默认禁止。

| 对象类别 | 选中 | 移动 / 旋转 | 替换 / 删除 | 材质 | 参数尺寸 | 说明 |
|---|---:|---:|---:|---:|---:|---|
| 建筑结构：墙、楼板、柱 | 只读 | 否 | 否 | 仅允许的饰面 | 否 | 几何由 `FloorPlan` 管理 |
| `Surface`：墙面、地面、顶面 | 是 | 否 | 否 | 是 | 否 | 材质变更仍走命令和规则 |
| `Opening`：门、窗 | 只读 | 否 | V1 否 | 允许款式时可替换 | V1 否 | 保留开启方向、净距和墙体引用 |
| 固定定制模块：入墙柜、橱柜 | 是 | 仅规则允许的锚点调整 | 模块可替换，不自由删除关键结构 | 是 | 仅产品允许范围 | 不是自由家具；必须保持墙面 / 模块组合约束 |
| 可移动家具：沙发、桌、床、椅、独立柜 | 是 | 是 | 是 | 产品允许时 | 产品允许时 | Gate 5 的“全部家具可编辑”主要对象 |
| 松散装饰：地毯、落地灯、装饰件 | 是 | 是 | 是 | 产品允许时 | 通常否 | 仍检查碰撞、通行和可达性 |
| 设备：电视、音响、家电 | 是 | 接口允许时 | 可替换 | 通常否 | 否 | 受插座、观看 / 使用净距和安装关系约束 |

“全部家具可编辑”不等于“所有对象都能任意拖动”。每个 `SceneObject` 必须显式声明能力，例如：

```text
selectable · movable · rotatable · replaceable · deletable
duplicable · materialEditable · parameterEditable
```

## 8. 最小数据合同与所有权

Gate 0 只锁定名字、职责和所有权；完整 schema、校验器和测试属于 Gate 1。

| 合同 | 唯一负责 | 不能负责 |
|---|---|---|
| `FloorPlan` | 坐标系、建筑边界和层级入口 | 家具版本或聊天状态 |
| `SceneGraph` | 汇集一套住宅的房间、表面、opening、对象和引用关系 | 绕过命令直接修改对象 |
| `Room` | 房间边界、语义、相邻关系和相机预设引用 | 独立保存另一套家具布局 |
| `Surface` | 墙 / 地 / 顶面、法向、边界、材质和锚点 | 通过图片热点代表墙面 |
| `Opening` | 门窗几何、开启方向、墙面引用和净距 | 作为无归属装饰 mesh |
| `SceneObject` | 家具 / 模块 / 设备的 ID、mesh、transform、尺寸、能力、来源 | 自己决定是否合法 |
| `Material` | 可复用的物理材质参数、贴图和来源 | 另存对象 transform |
| `CameraPreset` | 整屋、房间俯视、入口、主功能面等可复现相机状态 | 成为静态效果图地址 |
| `SceneCommand` | 所有手动与 Agent 场景变更的唯一写入口 | 跳过校验直接改 mesh |
| `DesignRule` | 确定性约束、严重级别、来源和解释 | 依赖 LLM 猜测几何是否合法 |
| `SceneVersion` | 不可变父版本、命令、场景摘要、来源和确认状态 | 只保存一张结果图片 |
| `HouseholdMember` | 家庭身份、表达和决定归属 | 复杂账号权限系统 |
| `Confirmation` | 确认范围、成员、版本和时间 | 覆盖或删除旧版本 |

唯一写入路径：

```text
用户操作 / Agent 工具
        ↓
SceneCommand draft
        ↓
DesignRule validate
   ├─ hard block → 不提交，并解释替代位置
   ├─ warning    → 用户确认后可提交
   └─ allowed    → 提交
        ↓
SceneGraph 更新 → 2D / 3D / 渲染 / 版本 / 导出共同读取
```

## 9. 角色与责任

| 角色 | 负责 | 不负责 |
|---|---|---|
| 家庭成员 | 表达、比较、直接修改、保留分歧、确认 | 学会专业 CAD 或独自证明设计合规 |
| Agent | 自然交流、渐进澄清、调用真实命令、解释取舍、调解冲突 | 直接写原始 scene、替家庭做最终确认、决定安全规则 |
| 确定性规则系统 | 几何、安全、产品和已确认项约束 | 审美聊天或家庭价值判断 |
| 导购 / 顾问 | 按需提供想法与使用帮助 | 默认进入家庭私域、替家庭持续操作 |
| 设计师 | 家庭确认后的专业复核、退回与下游适配 | 作为消费者首页的模式开关 |
| 企业下游 | 精确报价、BOM、生产、施工与交付 | 被 Demo 数据冒充已接通 |

## 10. 旧 V1 隔离边界

**历史排除**

- `app/src/App.jsx` 当前六空间栏、图片热点、提案卡和客户 / 设计师模式切换；
- `app/src/ThreeViewer.jsx` 当前方块式 3D；
- `app/src/domain/catalog.js` 的静态图片热点和六空间产品状态；
- 12 张预生成房间图作为场景或版本真相；
- 旧 `DEMO_SCRIPT.md` 和 `design-qa.md` 作为新体验验收证据。

**候选复用，但必须在对应 Gate 重新验证**

- Node BFF 路由和浏览器不接触凭据的边界；
- 飞书 Base 幂等记录与离线回退思想；
- 来源 `demo` / `estimate` 标记；
- 版本、确认、复核和导出字段中的可用概念；
- Vite、构建、Sites worker 和测试脚本。

Gate 0 审阅期间采用非破坏隔离。用户验收后明确要求丢弃错误版本，因此旧产品代码、资产和展示材料已整体移至 `/Users/dracoglasser/.Trash/oppein-legacy-v1-pre-gate1-20260808/`。它仍可恢复，但新实现不得从中复制旧 product view-model 作为 canonical scene。

## 11. 明确开放问题

以下事项不会阻止 Gate 1，但不得在后续悄悄假定为已确认：

1. 合成住宅的精确尺寸、坐标原点、墙厚和层高，由 Gate 1 在 schema / 2D 证明中提出并单独验收。
2. Gate 2 使用 Three.js、自研资产还是商业 3D SDK，必须先用实际画质、交互、授权、体积和性能比较后决定。
3. 2D 总览在主页面的最终位置和展开方式，Gate 3 用真实页面状态决定。
4. 直接拖动采用地面投影、表面拖动还是 gizmo，Gate 5 通过可用性测试决定。
5. 警告是否允许用户强制保留、由谁确认以及哪些告警永远不可覆盖，Gate 6 决定。
6. 家庭确认按对象、房间还是整屋分层，Gate 7 与 Gate 9 联合决定。
7. Agent 使用 Aily、其他 API 或降级 provider，Gate 8 capability gate 决定；未验证前不显示 Live。
8. 真实欧派 SKU、模块规则、报价、BOM、工期、量房和生产字段仍需用户或 Coach 提供。
9. 多人实时协同、账号权限、公网部署和正式比赛提交均不在当前授权范围。

## 12. Gate 0 验证证据与限制

- 开始状态：本地 `main`，无 `HEAD`、无 commit、无 remote；现有 `app/` 和根目录材料在 Gate 0 开始前已经全部处于 untracked 状态。
- Gate 0 审阅阶段的写入范围只有：本文件、`PLAN.md`、四份旧 V1 文档的历史标识，以及当时 `app/AGENTS.md` 的 Gate 约束。
- 没有编辑 `app/src/`、`app/server/`、`app/public/`、测试、依赖、环境变量或飞书数据。
- 本地校验：所有新增相对链接可解析；上述文档无尾随空白；独立架构批评结果为 `PASS`。
- 证据限制：Gate 0 审阅时仓库没有初始 commit，Git 无法自行区分此前已存在的 untracked 产品文件。用户验收后已授权清理并创建首个本地基线 commit，后续 Gate 将从该基线产生可审计 diff。

## 13. Gate 0 验收清单

- [x] 只有一条从户型到下游的产品旅程。
- [x] 2D、3D、效果视图、Agent 和导出都指向同一个 scene 真相。
- [x] 整屋 → 房间 3D 俯视 → 房间视角按钮的镜头路径已锁定。
- [x] 首个演示住宅、家庭夹具和三类空间故事已定义并明确标记为 `demo`。
- [x] 页面职责、对象能力矩阵和数据合同名称已定义。
- [x] 旧 V1 的排除项与可复用基础设施已分开。
- [x] 未决事项没有被包装成已完成能力。
- [x] Gate 0 没有修改产品代码、生成资产、调用 AI 或改变飞书数据；无初始 commit 的证据限制已明确披露。

用户已验收本文件。首个本地 Git 基线建立后，Gate 1 开始创建 canonical scene 与 2D 户型地基。
