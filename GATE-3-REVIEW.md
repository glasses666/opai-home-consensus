# Gate 3 审阅包：家庭主工作空间与 3D 数字住宅导航

状态：**已实现并完成自动验证，已保存待验收快照；尚未通过用户验收**

日期：2026-08-09

Git 基线：`daf61b3`（夜间后端包 N2）；本 Gate 不混入 Gate 4 资产或编辑能力

## 这一 Gate 构建了什么

- 新增消费者入口 `/project/demo`；`/lab/scene` 继续保留为技术检验台。
- 页面收敛为一屏工作台：实时 3D 主画布、同源 2D 户型总览、明确的返回整屋和当前位置摘要。
- 整屋 3D 中点击房间地面会进入该房间的真实三维俯视，不加载图片或第二份 scene。
- 开放客餐厅、主卧、次卧 / 书房都拥有 canonical `俯视 → 入口 → 主功能面 → 自由` 视角。
- 房间俯视高度按房间最大跨度计算，1024 宽画布也能看清完整空间关系，不再把“俯视”裁成局部家具特写。
- 3D 家具点选、2D 家具点选和当前选择共用同一个 canonical ID；切换镜头不会丢失对象。
- 2D 中选择当前房间的家具只更新选择，不会把入口 / 主功能面镜头强制拉回俯视。
- 房间、镜头预设和对象选择写入 URL；浏览器返回 / 前进可恢复同一房间、镜头和选择。
- 房间级视角胶囊只在进入房间后出现；整屋状态只提示用户点房间。
- 视觉只做 Gate 3 所需的消费者工作台收口：暖白、浅木、陶土与灰绿，不复制 IKEA 品牌或蓝黄配色。

## 如何审阅

1. 打开 `http://127.0.0.1:5173/project/demo`；当前浏览器标签已保留在该页面。
2. 在整屋 3D 中点击开放客餐厅地面，确认镜头先飞到三维俯视。
3. 点击沙发，确认右侧 2D 中同一沙发高亮；依次切换 `入口`、`主功能面`、`自由`。
4. 点 `返回整屋`，再从右侧 2D 户型进入主卧和次卧 / 书房。
5. 使用浏览器返回 / 前进，确认房间、镜头和选择恢复。
6. 本轮只判断“整屋 → 房间 → 视角 → 对象 → 返回”的页面体验；家具编辑与资产写实度分别留在 Gate 5 和 Gate 4。

## 验证证据

- `npm test`：59 项通过，0 项失败。
- `npm run build`：通过；Vite 仍有现有单入口约 934 kB 的 chunk 提示，无构建错误。
- 三房浏览器回归：开放客餐厅、主卧、次卧 / 书房均先到 `俯视 · 同一 scene`，并显示 `俯视 / 入口 / 主功能面 / 自由`。
- 3D 实际点按沙发：URL、3D helper、2D `选择沙发` 同步为 `object-sofa`。
- 主卧双人床与书房书桌在入口、主功能面和自由视角间保持选择；自由视角不再回弹到旧 preset。
- 浏览器后退恢复客餐厅俯视，前进恢复主功能面；两次都保留 `object-sofa`。
- 1024×768：`scrollWidth=1024`、`scrollHeight=768`；主画布、2D 总览、返回整屋和视角胶囊同屏可用。
- 1440×900：`scrollWidth=1440`、`scrollHeight=900`；主画布 1046 px、侧栏 340 px，同屏可用。
- 修复后从 2026-08-09T08:53:20Z 开始的完整浏览器回归：0 warning、0 error。
- 截图：`.omx/audits/gate3/project-demo-1024x768.png`、`.omx/audits/gate3/project-demo-1440x900.png`、`.omx/audits/gate3/living-overhead-1024x768.png`。

## 本 Gate 明确没有做

- 没有家具移动、旋转、替换、删除或材质编辑；这是 Gate 5。
- 没有把 Agent、组件目录、版本、影响卡或飞书状态放进页面。
- 没有继续提升 GLB、灯光、固定构件或客餐厅生活化细节；这是 Gate 4。
- 没有新增路由库、状态库或第二套场景数据；URL 状态使用浏览器原生 History API。
- 没有建立 remote 或 push；当前 commit 仅保存待验收快照，不代表 Gate 3 通过。

## 已知边界与下一步依赖

- 当前九件 GLB 仍是 Gate 2 的原创演示资产；Gate 3 只验证导航信息架构，不能据此宣称商业级材质与模型已经完成。
- Vite chunk 警告暂不为本 Gate 引入分包结构；等 Gate 4 资产加载边界稳定后再按真实加载路径拆分。
- 用户若验收本页，再把 Gate 3 标记为通过；随后进入 Gate 4 完整开放客餐厅真实 3D 垂直切片。

## Git 状态

Gate 3 基于 `daf61b3` 创建本地待验收快照 commit。该提交只用于保存，不代表用户验收；不建 remote，不 push。
