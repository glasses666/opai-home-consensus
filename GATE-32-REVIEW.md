# Gate 32 三重奏黑盒测试记录

状态：`v1.21` 完成 28 风格全量黑盒采集与一次服务失败重试；provider-only 仍未通过，未发布新基线。

## 测试合同

- 28 种设计风格 × 3 个取舍变体 × 4 套家庭布局 = 336 次真实 Aily 目标调用。
- 固定布局：`fixed-baseline-family`、`fixed-family-flex-work`。
- 随机布局：seed `20260814` 生成 `random-1-20260814`、`random-2-20260814`。
- 使用 Gate31 已验证的三段合同：`overview`、`rooms`、`decisions`。
- 每条记录保存完整脱敏 prompt、Aily response、provider trace、合同校验、延迟、错误和重试信息。

## 有效合并统计

主批次：`app/evals/gate32/gate32-20260814041954-20260814/`

- 336 / 336 条记录，`provider` 通过 137 条，provider contract failure 117 条，服务失败 82 条，评测器异常 0 条。
- 失败代码主要为 `SEGMENT_ROOM_SHAPE`（61）、`TRANSPORT`（63）、`SEGMENT_UNSUPPORTED_CLAIM`（27）、`SEGMENT_BRIEF_FACT_UNUSED`（21）和 `TIMEOUT`（13）。

同一 seed 的服务失败重试批次：

- `app/evals/gate32/gate32-20260814081826-20260814/`（82 条）
- 重试后 `provider` 通过 18 条，provider contract failure 35 条，服务失败 29 条，评测器异常 0 条。

以重试结果覆盖主批次中对应的 `provider_error` 后，当前有效口径为：

| 结果 | 数量 |
| --- | ---: |
| 完整 provider 合同通过 | 155 |
| provider 合同失败 | 152 |
| 仍为服务层失败 | 29 |
| 评测器异常 | 0 |
| 总调用 | 336 |

重试转换：82 条服务失败中，18 条恢复为 provider pass，35 条在可用响应后暴露为 provider contract failure，29 条仍为服务失败。服务失败不被计入模型能力判断，契约失败不被重试掩盖。

## 证据目录

- `layouts.json`：固定布局、随机布局、seed 和已知事实。
- `variants.json`：三种取舍变体及原始指令。
- `run.json`：运行合同、调用数量、并发和重试来源。
- `records/*.json`：每次调用的 prompt、provider 返回、trace、合同结果和错误。
- `report.json`：该批次的逐条汇总。

首批使用错误整单合同的尝试也保留在同一目录树中，但不纳入有效统计；这部分用于证明评测器从整单合同切换到 Gate31 三段合同的边界修复。

## 破绽分析

336 条有效合并记录显示三类问题：

1. 真实越权：134 条包含无依据数值、色温、施工、防水、机电或结构/生产承诺。这些错误继续由安全门拒绝，不能靠放宽校验提高通过率。
2. 合同负担：大量失败来自记录分隔、房间字段和决策依据形状。固定布局反而弱于随机布局，说明 Aily 在较长事实与依据 codebook 下更容易漏字段，不是缺少风格答案。
3. 内容缺失：住户事实未使用、四类决定依据不足、房间字段缺失和重复输出。此类失败保留，不由本地适配器补写设计内容。

## 通用调整与证据

- Prompt 从 `v1.16` 迭代到 `oppein-standard-master-plan-v1.21`：缩短依据 codebook，明确四类决定各自必须引用的风格依据、事实依据与代价，并把房间输出改为每房间一行四字段。没有加入风格专用答案。
- 适配器只归一化可证明无损的差异：换行或相邻数字记录、列表分隔符、每房间四字段分组、字段误用 `@@`、决策依据分隔符，以及明确否定语境中的禁用词。缺字段、超额记录、自造依据、真实结构/施工承诺仍失败。
- 北欧同 seed 12 条：`v1.18` 为 5 / 12，`v1.19` 为 4 / 12，`v1.20` 为 9 / 12；`v1.20` 剩余错误均为真实内容缺失。
- `v1.20` 跨风格抽样：北欧 9 / 12、装饰艺术 5 / 12、新中式 6 / 12、巴厘热带 4 / 12。48 条中 24 条完整通过；离线重放到 `v1.21` 后另有 8 个先前失败片段可无损通过，但因原运行在首个失败片段即停止，不能把它们宣称为完整目标通过。
- `v1.21` 全量 fresh 端到端复验为 336 条：首轮 137 条 provider 合同通过；一次服务失败重试后累计 155 条通过，仍有 152 条 provider contract failure 和 29 条服务失败。该结果证明 v1.21 还不能作为全风格发布基线。

关键候选证据：

- `app/evals/gate32/gate32-20260814021326-20260814/`（v1.20 北欧）
- `app/evals/gate32/gate32-20260814022513-20260814/`（v1.20 装饰艺术）
- `app/evals/gate32/gate32-20260814023352-20260814/`（v1.20 新中式）
- `app/evals/gate32/gate32-20260814024527-20260814/`（v1.20 巴厘热带）
- `app/evals/gate32/gate32-20260814032658-20260814/`（v1.21 装饰艺术 fresh）
- `app/evals/gate32/gate32-20260814033608-20260814/`（v1.21 单条服务失败重试）

## 当前边界

`v1.21` 是完成全量采集但 provider-only 未通过的候选 Harness，不是新的 28 风格发布基线。`standard-design-plans-v2.json` 仍是历史 `v1.16` provider 证据；在分析这批真实失败并完成必要的通用调整、再通过新的 provider-only 门槛前，不更新该发布文件，也不宣称所有风格已通过。
