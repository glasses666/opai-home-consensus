# DeepSeek 设计对话：开启思考与对照证据

2026-09-10，本地工作区从 `9330ba77cadcd80beba4c816357240fcaa2b14df` 增量修改；未部署公网、未改用户原项目、未更换产品模型。运行文件 SHA256 见 [原始主矩阵记录](deepseek-reasoning-main-20260910.json)。该指纹绑定本次最终 runtime；后续修改会使相应结论失效。

## 实际接入

- `/api/experience/projects/:id/turn` 默认调用 `callDesignDeepSeek`，请求 `deepseek-v4-flash`、`thinking.type=enabled`、`reasoning_effort=high`、`max_tokens=8192`。真实 API 返回模型名为 `deepseek-flash`，两者均保留在 trace 中。没有使用 Astra/Sol 作为产品回答模型。
- 官方 [Thinking Mode](https://api-docs.deepseek.com/guides/thinking_mode/) 和 [Chat Completions](https://api-docs.deepseek.com/api/create-chat-completion/) 支持上述参数。开启时省略不生效的 temperature；当前工具协议仍是结构化 JSON 观察循环，并未声称已经改为原生 API tools。
- 只记录实际推理证据是否存在、API 报告的推理 token 数和用量，不保留原始 `reasoning_content`。网页“本轮依据与执行记录”展示实际返回证据；历史记录不补填。该栏的 token 数对应 trace 中末次模型请求，逐次调用见 `modelRequests`。
- 仍受 35 秒单次请求、85 秒总回合和 6 步预算约束。未放宽对象能力、几何、碰撞、用户硬约束、预览与保存边界。超时/取消覆盖响应体读取，截断的 JSON 不成为成功预览。
- 旧一次性 Agent/first-plan 接口保留原预算；本次完成的是用户正在使用的主设计对话。回退方式见 [运行手册](../CANDIDATE-RUNBOOK.md)。

## 同输入对照：不是“打开就更好”

只读重放用户项目 `exp-449cdc6a-366b-4115-a146-37b3c9742a0f` 的前两轮历史和当前已保存场景，重试输入“可能是桌子摆放的位置不太对”。两组使用同一 prompt、相同 8192 预算、相同场景和需求；各自重新创建内存副本，不保存。关闭时保留原采样 temperature=0.1；开启时按官方约定不发送。资料库未在此对照重新检索，因此不能用它证明 RAG 效果。

| 提示词 | 思考 | 耗时 | API 推理 tokens | 实际结果 |
|---|---|---:|---:|---|
| 原强制二选一 | 关 | 1.427s | 未返回 | 先列桌子尺寸、位置，再问茶几还是餐桌 |
| 原强制二选一 | high | 6.004s | 774 | 仍替用户补出“够东西别扭/桌椅别着”等原因，表达没有改善 |
| 去掉强制二选一 | 关 | 1.552s | 未返回 | “你说的桌子，是餐桌还是茶几？” |
| 去掉强制二选一 | high | 26.850s | 4964 | 根据餐桌椅偏移给出餐桌平移 300mm 的合法预览；没有先确认是哪张桌子，解释仍有多余坐标 |

第一组初始 prompt SHA256 `c631bd42dd66bcefd9d2c982614e5bbae3c51533adb19547ec4c82266e15244d`；第二组 `04b69cbb2a3269eafc7d9c35b6f79cf3f6774b55c11ec89fac9d83f3d6299b07`。两次对照内部哈希分别一致。原项目文件 SHA256 均维持 `0792737838d48b9ba5200ca2b5b7cc358321aae9c7660d8036b3582917913531`。

实际请求 ID：第一组关闭 `9b24b66c-00fd-4af3-bf19-d57d57a0bfaf`、开启 `58703d69-548f-44bf-9fe0-9cfc51976641`；第二组关闭 `0a16b17d-517a-46b5-bf7e-54d679e7e988`、开启 `a8cee45b-1a66-4b07-9d85-6121db5cee61`。首组记录时间 05:56:00 UTC，第二组 05:58:27 UTC。

结论：已证明思考开关真实生效，未证明开启必然让回复更自然。第二组开启后的动作在几何上合法且可撤销，但目标假设未充分说明，应视作对话质量缺点，不计为“更听话”。本次只取消强制凑二选一和长解释，不为这个样例新增目标正则或固定回复。

复现（快照含私有令牌，只在本机读取，不上传/输出原文件）：

```sh
cd app
node scripts/eval_dialogue_reasoning.mjs --existing-opai-provider --snapshot .data/experience-candidate/projects/exp-449cdc6a-366b-4115-a146-37b3c9742a0f.json
```

脚本对不同初始 prompt、未观察到推理、错误 provider 或源项目变化返回非零。`experimentValid` 仅代表实验成立，不是对话质量达标。一次采样不能证明稳定提升或量化收益；网络/缓存和开启模式会改变耗时、tokens，本次未计算货币费用。

## 最终 runtime 验证

- `npm test`：444/444；包含真实 HTTP 调用方默认配置（mock endpoint，非 live）、参数校验、无推理证据不假标、关闭回退、截断、取消/超时、调用方 deadline、具体单问无需二选一等。
- `npm run build`：通过；保留现有大 chunk、blob-stream util/stream 外置警告，不能当成无警告构建。
- 最终提示词的真实 DeepSeek 刁钻主矩阵：8/8 最终通过、15 次模型调用（含跨轮案例首问）。旧输出错误地只加总 14，原始记录保留，统计代码已修正。4 个案例无校验修复，另有缺失对象边界、未应用材质声明、先观察相对关系等错误被拒绝后模型修正；不是首次全对。单案例总耗时 1.917–44.129 秒，没有本轮单请求超时。完整检查、错误、对象差异及逐次 provider 元数据见 JSON。
- 开启思考、尚未调整话术时还跑过 5 项留出回归，5/5 最终通过、7 次模型请求；这是中间版本证据，不当作最终提示词新增 5 项验收。
- 独立 Sol high 只读审查发现 A/B 实验成功门禁不足，已改为 fail closed；复核通过。没有用审查代理替代 DeepSeek live 结果。

## 后台页面验收

使用原生电脑工具的隐藏 in-app tab，`http://127.0.0.1:5180`，桌面 1280×720，无前台切换。路径：首页 → 我的设计 → 新建项目 → 设计笔记 → 提交 → 展开执行记录 → 后续预览 → 查看真实差异 → 撤销。

隔离项目 `exp-dba5519b-b53a-434c-8b4f-88b6321f7554`：

1. 输入“客厅的桌子看着有点怪，先聊聊，别直接挪。”：6.690 秒、1124 推理 tokens。回复“好，先不动，咱们就看看。客餐厅里有两张桌子——沙发前那张茶几，和旁边那张餐桌，你觉得怪的是哪一张？”没有预览和场景变动。
2. 输入“是茶几。让它离餐桌稍微远一点，其他家具位置不变，距离你来定，先预览。”：34.174 秒、3 次模型调用。第一次缺少相对关系观察被拒绝；模型按反馈观察，再生成合法预览。实际只有茶几 `(1830,7750)→(1800,8048)`，其他家具/表面不变，规则通过，位置硬约束显示在需求摘要。
3. 差异面板显示对象差异 1、表面差异 0。撤销后回到“V1 · 已保存”，未点保留或保存。真实 3D、非空主工作台、回复、推理记录均可见；控制台 error 0，无框架覆盖层。首次关闭差异抽屉时 QA locator 匹配到遮罩和关闭按钮两处，改为抽屉内定位后成功，不将此自动化选择歧义计作业务错误。

页面证据（脱敏 trace、动作、约束）见 [GUI 记录](deepseek-reasoning-gui-20260910.json)，截图见本任务原生电脑工具输出。未验收手机、真机 Safari、公网或飞书后段；这次改动未触及它们，也不借用历史记录声称通过。

## 持久化与边界

本地用户素材和原项目文件保持不变。已有 Obsidian 项目主页 `AgentVault/10_Projects/PRJ-2026-008-oppein-ai-home-consensus/00_Project_Home.md` 已用于历史方向定位，但该页存在其他未提交修改；不覆盖它，本次经验和证据先记在 PLAN 和本文件。经验是“推理开关、对话指令和确定性规则必须分别做受控验证；有推理 token 不等于对话质量改善”。没有新增项目壳、知识条目或修改记忆库。
