# OPAI 本地候选验收账本 · 2026-09-09—10

## 2026-09-10 设计笔记显式材质请求回归

用户现有项目中“地板改成深棕色 → 全屋所有房间 → 是的/允许”曾连续 8 轮澄清、15 次模型请求；根因是旧茶几任务留下的全局 `lock_material` 覆盖全部地面、短确认没有绑定紧邻的是非问题，以及逐面材质工具无法在预算内处理 9 个地面。失败保留，不把它描述成 DeepSeek 偶发不听话。

- 提交 `fc4a676` 让新显式材质意图只替代相交范围内的旧材质锁，保留家具、墙顶、位置和其他范围的约束；否定句、问句、二选一和不兼容材质不会释放锁。
- `set_surface_group_material` 在场景副本中原子应用最多 24 个明确表面；任一对象、材质或规则失败整组不落地。旧项目缺少的深棕胡桃木只允许通过受信任演示材质增量重放，不放宽其他材质表篡改检查。
- 隐藏电脑工具在新隔离项目 `exp-2a4ae921-8c8d-452d-be3d-3ca8e300f2fe` 输入“把全屋地板改成深棕色”。实际来源 `deepseek · deepseek-v4-flash`，请求 `5412d0db-016b-4d07-8fd3-b9b99a12d76e`，一次模型调用、3.786 秒，结果 `legal_preview`；差异抽屉显示 9 个地面变化、规则通过。随后真实点击保留、保存 V2 `version-4ae9aee7`，按项目 ID 在新后台标签重新打开仍显示 V2，持久化场景中 9 个地面均为 `mat-floor-dark-walnut`。
- 脱敏导出：[JSON 证据](evidence/gui-whole-home-material-20260910.json) 与[可读交接](evidence/gui-whole-home-material-20260910.md)。导出绑定 `fc4a676ddd8c1c96df2175be39510abac7099c4f`，不含访问令牌；电脑工具全程使用隐藏标签，没有抢占用户前台。
- 当前全量自动化 421/421、0 fail、0 skip；`npm run build` 成功。此证据证明明确表面、范围和可用材质的真实路径，不证明全部随机表达或任意施工请求都能执行。
- 第一次更新精简候选时错误地把整个 Git 归档覆盖进旧精简基线，虽然干净安装、421 项测试和构建通过，归档却膨胀到 617MB；该包已移入废纸篓并判失败。重组规则已写入 [候选运行手册](CANDIDATE-RUNBOOK.md)：精简包只能按核验过的清单重建或覆盖相对上一候选的精确变更路径，归档体积和禁入路径也是验收条件。

## 继续验收结论（优先于下文历史候选结论）

`26268c2` 归档不是完整目标的最终验收。后台电脑工具继续验收发现并保留了以下失败；相应边界现已接入同一产品并由当前 421 项自动化及真实闭环复测。旧包指纹仅属于当时版本。

- [布局连续回合](evidence/gui-relative-layout-20260909.json)：五次自然输入，首次错误方位追问；第2、3次几何规则通过却让茶几远离餐桌，均由GUI撤销；第4次重复确认需求被去重导致安全失败；第5次实际 DeepSeek 3调用、10.094秒，调用相对移动工具，仅茶几中心 `(1830,7750)`→`(1850,7551)`。与餐桌中心距离 `2663.719`→`2463.717`mm，缩短约200mm，不声称这是通行净宽或能放下垫子的证明。已GUI保留、保存V2 `version-177824e7`、刷新并进入客餐厅看到真实家具。
- 不再把提示词写了“不能猜方向”当保证。观察提供 canonical axes；相对移动通过工具计算并走原 SceneCommand。相同批次先写后读不能绕过相对工具门禁，错误参考对象不能复用其他配对的观察，历史无效约束修复不能缩小原范围。独立定向回归19项通过；随后真实 DeepSeek 从 V4 观察茶几与沙发关系，只将茶几朝沙发移动50mm，GUI差异、保留、保存V5和重开均通过。
- 飞书 slot 以前只返回整表根入口，且邀请称呼易被误当身份。现接入官方只读单记录链接，但不新增共享权限、不声称第二个账号可访问。`updated_by` 只能证明整行最近编辑者，不能证明意见作者。隐藏飞书页面的AX、截图、日志读取均超时；这不能由CLI读取成功代替GUI填写证明。
- 独立审查进一步发现公开意见 source 可伪造、eventId 冲突前已写入业务状态、摘要部分组缺逐条来源、UI失败重试换 eventId 等问题。现已分别修复：来源由服务端绑定，重复事件在业务写前拒绝，摘要每项校验意见 ID，UI 保持稳定动作 ID 并阻止旧响应覆盖；对应回归已进入当前 421 项全量测试。


## 候选与证据边界

本次是同一主工作台的产品改造，不是独立演示页。候选 commit 以包含本文件的本地 Git 提交为准；起始 HEAD 为 `7a120e9d9ec3d811a10219355a9b709a70a904e0`。未 push、未发布生产。所有开发代理已冻结。

当前可移植候选入口：`http://127.0.0.1:5186/project/demo`（独立目录干净安装后实际 dist）；原工作区构建入口 5185，开发入口 5184，隔离后端 127.0.0.1:8794。最终后端进程于 20:54:58 CST 启动（当时 PID 72101）；这不是可跨重启使用的进程身份。用户原有服务、私有项目和原始资产保留。

产品实际 provider：DeepSeek，endpoint `https://api.deepseek.com`，请求及响应模型 `deepseek-v4-flash`。thinking disabled、temperature 0.1、JSON 输出；不是开发 Astra/Sol 的结果。真实 Aily provider 为 `aily_team`。证据中的 workspace 哈希是**导出时**哈希，不能倒推旧回合全部运行于最终源码。以下逐项标出历史失败、最新复测和未测边界。

资产/数据基线：

- 用户家具基线 `design-lab/reference-home/user-furniture-baseline-20260908.json` SHA256 `4dcef50bce7522983a97798ee2ca2dd370d93fcca0faa662ef6a6cf491928a2a`。
- `app/src/domain/reference-home.js` SHA256 `532185880f8bb0feb5b739deee3a8201c5fc24af81b33bf73d481647b434d661`。
- `app/package-lock.json` SHA256 `dc372bb410b276b3796b581998413627a213f363c6b5135ce28ed2fbb7b963e6`。本轮无新依赖。
- 全部来源只证明已确认参考户型，不证明任意上传图片自动建模或任意家具资产操作。

## 已集成的用户行为与实际证据

| 行为 | 观察结果与证据 | 限定 |
| --- | --- | --- |
| 开放表达与纠正 | [独立自由输入记录](evidence/final-independent-free-20260909.json)：新表达引导、纯审美纠正；最终 `6046975e-4a97-4f9b-92d0-9ae401e2f254` 以 2 次模型调用、5.505 秒完成仅沙发亚麻材质预览，其他家具材质/位置及沙发位置锁定 | 6 次输入中有语义失败；最终通过不能覆盖首次失败 |
| 同场景预览、撤销、保留、保存、重开 | [GUI 单人与家庭记录](evidence/gui-solo-family-20260909.json)，项目 `exp-94bcdc7c-be05-4bc3-ba22-64f31ed6698f` 从 V1 至 V4；电脑工具真实点击与刷新，最终炭灰沙发、保存 V4 保持一致 | 非录制回放；只对测试对象/命令范围成立 |
| 资料更新影响设计 | [GUI RAG 记录](evidence/gui-rag-20260909.json)，同 URI 三次修订：浅橡木 → 灰米微水泥 → 用户确认 typed 浅橡木限制；相应真实预览、保留保存至 V4 | 普通文字是参考，typed 规则经确认才确定性执行 |
| 当前资料冲突澄清 | 最终后端 `3225ee48-4082-46c0-8f01-adb7758b04b0`，2.0 秒，引用第三修订，问是否撤销材料限制；没有直接改墙，V4 不变 | 最新源码实测单个问题；不声称任意问题表达都已穷尽 |
| 资料隔离/过期/不可信输入 | [RAG 更新与隔离记录](evidence/rag-update-live-20260909.json) 和 house-knowledge/evidence/experience 回归；项目与房屋过滤、修订失效、未确认资料不取得规则权限 | 历史 live 文字禁令失败保留；确定性拒绝主要由自动化验证 |
| 真实 Base + Aily + 用户选择 + 回流版本 | 最新讨论 `discussion-8793737f01bd4fa5` 绑定V4 `version-bbbb290f`。同一隔离验收本人经 API 向真实 Base 记录 `recvuJGYqcUBcq`、`recvuJGZ5XMoSS` 分别写入“茶几靠近沙发50mm”与“远离沙发100mm”；回读后真实 Aily chat `7683552364045093824` 生成有来源的“意见记录差异”。用户仅选择第一条，DeepSeek 请求 `a8a4a400-b220-44eb-8524-e51fbe59e455` 观察茶几/沙发关系，经3次模型调用和确定性相对移动工具生成合法预览，差异仅茶几 `(1899,0,7545)`→`(1849,0,7551)`，保存V5 `version-a4c6c8d9`。完整导出见[布局与协作闭环](evidence/gui-relative-layout-20260909.json) | 两条记录不是两个成员；Aily 比较意见内容，不证明家庭共识或身份冲突；前两次模型回复分别因缺当前需求、无变化而被拒绝，未伪装成首次成功 |
| 需求简报与交接 | 电脑工具点击导出得到成功反馈；同一实际 handoff API + 前端 formatter 的可读输出：[单人/家庭简报](evidence/gui-solo-family-20260909.md)、[资料约束简报](evidence/gui-rag-20260909.md) | 不捏造浏览器下载文件路径；导出结果可回查真实保存版本、差异与来源 |
| 普通新项目与最终构建 | 隐藏电脑工具 tab 15，`exp-d9e943d9-d2d1-44e9-89da-db48b049b4b3`，实际 5185 dist，无管理员/隐藏录制参数。桌面有真实家具和墙体；375×844 点击进入实时 3D 后实际模型出现；家庭讨论入口可达；error 日志查询为空 | 是桌面浏览器手机视口，不是真机 Safari；此新构建项目未重复整条收费调用 |
| 干净依赖安装后的候选复验 | [额外 GUI 记录](evidence/clean-install-gui-20260909.json)，5186 新项目 `exp-74234b8e-3eb8-487d-9959-3b490fcf9644`。请求 `fc3857e2-a7d7-425b-8140-43a033da056c`，真实 DeepSeek 1 次调用、5359ms，仅沙发变为亚麻材质；真实点击保留、保存至 V2 `version-809ad358`，刷新及手机进入 3D 后保持，error 日志为空 | 这是干净安装重建版本的新 live 回合；未在此版本重跑另一个真实家庭成员 |

本轮实际截图保存在当前任务电脑工具的可视输出中；不把历史 Playwright 图片标作最终截图，也未制作一条剪辑后的完整闭环视频。队友可按同一入口补录。

## 失败分母与修复记录

- 独立自由输入共 6 次：旧候选有一次追问两题、一次正确画面但硬约束种类错误；首次最新纠正又因共享原话验证过严而安全失败。最终第 6 次才成功；中间错误、耗时和重试均在 JSON 中。
- 单人 GUI 纠正曾只改材质，未清掉旧阅读目标；撤销后修复 requirement/hypothesis review 和当前需求引用，再重试。不是“聊天改了就算完成”。
- 第一次 Aily 对单人意见只给继续询问家人的问题；不能供用户采纳，判失败。第二次返回来源明确的本人意向，不捏造共识。
- 首次家庭选择回流报 `REQUIREMENT_REVIEW_REQUIRED`，6 次模型调用、约 16.5 秒，房屋没被破坏。补全工具 schema 与必需 review entries 后，用户普通重试成功，不将该失败隐藏为自动成功。
- 历史普通文字资料禁令曾未被遵守，记录保留；用户确认结构化规则后确定性校验。新修订引用/冲突澄清已复测。
- 最终预览服务初次未设 IPv4 host，导致 127.0.0.1 连接失败；显式 `--host 127.0.0.1` 后解决。这是启动配置问题，不删除记录或混成产品成功。
- 电脑工具一次用过期菜单名称定位失败；重新读取 AX 并点击当前菜单后成功，没有通过页面脚本绕过真实交互。
- 独立候选目录首次默认 `npm ci` 报旧锁文件缺失 Next.js 等自动 peer 项。既有 Vite 工程已用局部 adapter 替代 Next.js，补充项目级 `.npmrc` 保持锁文件原有 legacy-peer 解析，不升级依赖或改全局 npm 配置；安装/复验结果见候选清单。
- 独立归档首次测试因漏带 `app/evals/agent-cases.mjs` 失败（不是业务回归）；补回原文件后当时 374/374 通过。失败日志随候选保留，未静默跳过用例；本轮最终候选从 Git 归档重新生成，不复用该缺文件目录。

## 自动化、构建与当前风险

- 最终源码 `npm test`：421/421，0 fail、0 skip；覆盖确定性规则、候选原子性、持久化/重开、版本冲突、项目令牌隔离、资料和意见过期、意见来源/身份边界、事件幂等、超时/错误、相对关系观察、显式材质范围/短确认与预览修正等。mock/纯函数测试不能替代上方 live 证据。
- `npm run build` 成功；完整 dist 约 106 MiB（约 110 MB）。[隐藏浏览器 Resource Timing 记录](evidence/gui-cold-load-20260909.json)实测首页资源传输1,654,604 bytes；工作台资源5,421,174 bytes，另有1,181 bytes导航与约1,885 bytes未计入 transferSize 的图标请求，约5.43MB。它证明本地候选的页面加载包低于10MB，不等于公网冷网时延或真机流量账单；完整 dist 也不等于一次访问下载量。Vite大chunk提示和BVH弃用warning尚在，控制台 error 为0。
- 旧独立候选曾用现有锁文件重装209 packages并完成当时回归；最终归档改为从本轮提交的 `git archive` 建立新目录，当时执行 `npm ci`、414项测试与构建。最终包路径、SHA256和独立GUI复验在包内 `CANDIDATE.md` 及本次交付回执中记录，不把旧哈希截图当新包证明。
- 手机出现的鼠标操作提示仍有桌面措辞，不影响此次进入 3D/协作入口，但真机触控与可访问性未做完整验收。
- 目前通过的开放动作以真实材质调整、现有家具能力与移动规则为主；不是家具任意生成/施工智能体。不应将有限测试泛化为全部自由需求可靠。
- 用户访问令牌、API key、私有存储和环境文件未放入证据导出。来源的 `authorization` 字段是资料可信状态，不是 OAuth 凭据。此账本与 Base 资源标识仅随本地候选保存，不自动发布到公开库。

## 外部事项与恢复入口

1. 第二名真实家庭成员需本人实际登录参与；当前只证明单人填写、真实 Base/Aily 和版本回流。新增权限和真实群发送仍需确认。
2. 生产尚未发布；要发布须另行授权，并在公网重新验证冷加载、认证回调、模型/API 和资产，不能沿用本地通过结论。
3. Coach 确认 7/8 分钟冲突与企业原始命题；[队友同步说明](TEAM-SYNC.md)、[7 分钟稿](CORE-TALK.md) 已就绪，未伪造真人排练或业务收益。
4. AgentVault 已读取 `HOME.md` 和 `10_Projects/PRJ-2026-008-oppein-ai-home-consensus/00_Project_Home.md`；项目首页原有未提交修改，尚未获 overlap 处理答复，未写入。当前源码与本地 Markdown 权威，不能宣称 Vault 已同步。此次约束纠正/资料边界等复用经验暂收于本账本，待允许安全同步既有项目页。

启动与回退见 [CANDIDATE-RUNBOOK.md](CANDIDATE-RUNBOOK.md)。不使用 reset/clean；不复制凭据、私有项目、受限资产到公开 Git。此候选达到已列明的本地行为证据，不标为未经限定的“全部生产验收完成”。
