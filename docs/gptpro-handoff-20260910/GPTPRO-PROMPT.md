# 交给 GPT Pro 的完整提示词

你现在接管 OPAI AI 家装产品的核心 Agent/Harness。不要只做审计或给建议；请直接在 GitHub 代码上实现、运行、反复试错、修复并提交 Pull Request，直到自然语言需求能稳定转成真实、明显、受约束、可撤销的 3D 空间变化。

仓库：`https://github.com/glasses666/opai-home-consensus`

基线分支：`codex/gptpro-harness-github-20260910`

请从该分支创建你的工作分支。不要从 `main` 开始，不要强推或改写 `main`。先完整阅读根目录 `AGENTS.md`、`GATE-0-PRODUCT-CONTRACT.md`、`PLAN.md`，以及 `docs/gptpro-handoff-20260910/` 下全部文件。`PROBLEM-LEDGER.md` 是当前真实问题和验收合同，`REPRO-TRACE.json` 是一次真实 DeepSeek 失败样本。

开始后先做一个不超过十分钟的能力预检，并如实记录结果：你是否能读取仓库、修改/导出文件、向 GitHub 写入、从执行沙箱访问 `https://api.deepseek.com`，以及是否有安全注入但不会回显的 `DEEPSEEK_API_KEY`。不要把 GitHub 读取权限误认为写权限，也不要把网页浏览能力误认为终端外网出口。绝对不要要求我把 API key 粘贴进对话、文件、补丁或仓库。

如果 GitHub 只能读取，请不要反复尝试授权或因此停工：在我上传的 ZIP 内完成全部修改，最后同时交付 changed-files ZIP、可用 `git apply` 应用的完整二进制补丁、变更清单和建议提交说明。如果沙箱不能调用 DeepSeek，请继续完成实现、mock/确定性回归、对抗用例与 live 观测脚本，但必须把这些结果标成非 live；给出一条最小本机验证命令和预期 trace 字段。我会在保有真实密钥的本机应用你的补丁、跑 DeepSeek 与浏览器验收，再把首次失败和脱敏 trace 回传给你继续修。沙箱限制不是诊断-only 交付的理由。

你的任务不是再做一轮表面 prompt tuning，而是找出模型、系统提示词、状态合同、观察信息、工具能力、候选验证、修正循环和产品调用方之间的真实瓶颈，并直接修通。允许重构这些边界，但必须保留一个 canonical scene、SceneCommand 原子执行、确定性规则、版本/撤销/保留/保存语义和来源追踪。产品运行模型必须仍是 DeepSeek-v4-flash；不要用你自己的输出、固定预设、本地 fallback 或测试 mock 冒充 DeepSeek 的真实效果。

最重要的复现是：用户说“回到家还是像坐在工位，我想放松一点，但书桌要留下，先别加大件。”系统虽然正确识别了保留书桌和不新增大件，却擅自把“保留”解释为“位置不动”，最终只改一面墙和床的颜色。这在规则上合法，在设计上无效。修复后，Agent 应理解：保留对象不等于冻结位置；如果一个澄清会真正改变布局，可以只问一个自然问题，否则应观察当前房间并生成一组明显、有关联、可撤销的空间尝试。普通用户不需要说厘米、坐标、材质 ID 或专业风格术语。

请建立足够强的质量闭环：模型观察当前房屋事实和需求状态，规划候选动作，确定性校验，再检查候选是否真正回应需求、变化是否足够可见、解释是否与命令一致；失败时基于具体新观察修正，而不是反复换安全颜色。不要把设计质量简化成动作数量阈值，也不要为演示句子添加关键词、正则或房间专用 if/else。必要时扩展结构化观察或最小工具，让模型能理解门窗、入口视线、家具关系和工作/休息分区；不能让它猜坐标，也不能编造场景里不存在的家具。

请至少完成以下工作：

1. 复现并定位 `REPRO-TRACE.json` 的退化路径，记录究竟是模型选择、提示词诱导、观察缺失、工具合同还是候选验证放行导致。
2. 实现真正的修复，而不是只改回答文案。
3. 明确定义并测试三种约束：
   - “书桌要留”：对象不能删除，但可在规则允许时移动/旋转。
   - “书桌留在原位”：不能移动/旋转，可在许可下改材质。
   - “书桌什么都别动”：不能对书桌执行任何修改。
4. 让“感觉厨房太挤了”这类已点名房间的表达不再收到“哪个房间/哪个具体场景”的空泛追问；如果仍需澄清，只问会改变下一步设计的生活差异。
5. 用不在现有展示脚本里的刁钻表达建立 development 与 holdout 评测，覆盖纠正、否定心理归因、跨轮硬约束、含糊审美、明确快路径、非法原子回滚和刷新恢复。不要删除首次失败记录。
6. 改善主工作台：差异列表使用用户可读名称，不暴露内部 ID；长请求有阶段反馈、取消、失败与重试；首页新会话冷启动不会永久落入 NOT_FOUND/HTTP_502。
7. 检查家庭信息步骤。个人使用必须完整成立；如果保留该步骤，应是可跳过的轻量居住背景，不要让预算和风格问卷挡在 AI 对话之前。

每次实质修改后做定向测试；稳定后运行完整 `npm test` 和 `npm run build`。在有凭据的环境中，用真实 DeepSeek-v4-flash 做小批、高区分度 A/B 与 holdout 测试，记录 provider、实际模型、thinking/reasoning 参数、请求次数、首次结果、修正结果、耗时、usage 和失败，不把重试后的成功覆盖首次失败。随后通过真实浏览器，从普通新会话按首页路径进入，分别验证桌面和手机视口、3D 对象实际变化、预览/撤销/保留/保存/重开、控制台和关键资源加载。HTTP 200 或 canvas 出现不算验收。

不要提交 `.env.local`、token、真实客户数据、真实飞书成员数据、原始大视频或受限 Fab 二进制。不要做生产发布、真实群聊发送、购买服务或新增权限。对飞书链路只做不产生真实外部写入的回归，除非仓库现有隔离测试凭据明确允许。

最终必须提供：

- 一个可审查的 Pull Request/已推送提交；如果 GitHub 只有读取权限，则交付 changed-files ZIP 与可直接 `git apply` 的完整二进制补丁。
- 变更后的数据合同与关键实现说明。
- 完整测试和构建结果。
- 真实 DeepSeek 首次/重试 trace 与 A/B、holdout 汇总。
- 桌面/手机浏览器证据，证明是真实 3D 命令，不是回答文字变化。
- 仍未解决的问题及其精确阻塞。

请持续实现与验证，不要在给出规划后停下等待，也不要交“第一版/以后再补”的核心缺口。只有 `PROBLEM-LEDGER.md` 的必需行为在同一候选版本中通过，才算完成。
