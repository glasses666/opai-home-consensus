# 2026-09-07 网页与飞书交接发布

## 发布边界

用户授权发布到 https://opai.glasser.top。保留 DeepSeek 主设计链，复用 Base 留痕和 Aily 共识秘书；没有设置群收件人、没有发送群卡片，也没有新增真实欧派数据能力。

## 候选验证

- 本地全量测试 295/295、生产构建、diff 检查通过；大型 3D chunk 警告保留。
- Chrome 1440×1000 和 390×844：主页场景切换→项目详情→真实 3D→版本确认→提交→复核→交接→返回同一版本，无 JS 错误/横向溢出。
- Browser skill 未提供，按 frontend-testing-debugging 使用已安装 Playwright/Chrome；自动化脚本与截图在 /tmp，不作为运行依赖。
- 阿里云候选 `/opt/opai/releases/20260907-feishu-web`，独立 8792 服务和 `release-qa-20260907.json` 测试存储。线上原存储不覆盖。
- 六条 DeepSeek live golden 全通过，全部 provider、零 fallback。实际 Base 事件 `evt-handoff-mtr3k4z6-dvdquk`，记录 `recvuwRqGzP2Qr`；回读 Event ID/Result JSON 确认版本摘要、提醒、边界及复核链接。Aily summary 为 ready。演示审批明确写测试备注，不是专业审批证据。

## 运维方法

本机构建并上传 dist、server、src、evals、scripts、package 文件；不上传 .env、凭据、node_modules、原始视频或 Blender 工程。服务器 Node 20.18 只运行后端，不在服务器构建 Vite 8。

Nginx 根目录 `/opt/opai/current/app/dist`，API 转发 8791。发布前旧链接 `/opt/opai/releases/15f6d51`，该目录曾热更新，其名称不是源码版本证明。使用新 release 目录、原子切换 current、重启 opai；保留旧目录与上线前数据备份。

`opai-feishu-delivery.conf` 作为 systemd drop-in 配置公开站点与已核验 Base URL；不改原有受限环境文件和授权目录。授权维护 timer 保持启用。

回滚：将 current 原子指回上述旧目录并重启 opai。不要默认恢复数据备份，以免覆盖上线后的用户写入；数据回滚需另外判断。发布不更改 Worker/Funnel/其他站点配置。

## 已知边界

这是共享演示产品，不是多租户生产系统；保留既有权限和专业复核边界。Safari/真机 GPU、负载/长时间并发、真实户型识别质量、全量建模脚本重新渲染不在此次验收内。样式检查和模型浏览不等于重做所有历史渲染。

首次打包携带 macOS provenance 扩展头，Linux tar 忽略后正确解包；后续建议用 `tar --no-xattrs`。生成 CAD/原生模型作为资产保持原样；Git diff 标记为 binary，未伪装成经逐行人工审核的源代码。

## 上线结果

- 发布源码本地提交 `8706a2a`，current 已原子切到 `20260907-feishu-web`；opai/nginx/tailscaled/授权 timer 全部 active。
- 公网首页 SHA-256 `6ef1ce94b2917fab3db26c9e61ed4ae3fe2cdcd3e22b241ac76b2478ce5bec04` 与本机构建一致；三个后端关键文件本地/远程 SHA-256 一致。
- 公网 Chrome 六条主路由与 390px 首页无 JS/HTTP 错误和横向溢出；真实 3D canvas 存在，主视频实际解码宽 1920 且时间推进。新会话开场结束后主视频从头播放，早段为空场是已批准视频内容，不是加载失败。
- 线上原存储仍保留初始版本与零交接快照，未用发布测试数据替换。故新服务进程 health 的 Aily/Base 为 `real_*_not_verified`；它表示该进程尚未执行首次真实业务，不是授权失效。服务器独立候选的实际 Base/Aily 验证与线上进程状态分开报告。
- 回滚目录及 `/opt/opai/data/project-demo.pre-release-20260907.json` 保留；候选测试存储单独保留。发布未推送 Git remote，仓库剩余未跟踪项仅另一任务的路演材料。
