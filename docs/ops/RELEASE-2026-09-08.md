# 2026-09-08 current-version publication

User authorized pushing the current version and updating `https://opai.glasser.top`.

## Source and boundary

- Local implementation commit `29e2313`; public release snapshot `942fb52833a5ffd601849da1b836e65cb101b9c6`, fast-forwarded remote main from `7e8587a`. Local historical commits remain intact. Snapshot differs from local source only by omitting the 196,678,234-byte raw `欧派.mp4` that exceeds GitHub's file limit. No force push.
- Licensed Fab runtime GLBs deploy with the application but remain excluded from public Git. Generated UE project configuration was excluded because it contains a generated connection credential. Original files remain local. Unrelated untracked submission materials were not staged.
- Local main and publication branch intentionally have different history; do not blindly pull/merge or force-push the raw-video history later. Reuse the public snapshot parent for subsequent releases until history cleanup is separately agreed.

## Deployment and rollback

- Release `/opt/opai/releases/20260908-reference`; previous `/opt/opai/releases/20260907-feishu-web` preserved. Atomically replaced `current` and restarted `opai` successfully. Isolated candidate on port 8793 passed liveness and DeepSeek/catalog startup checks, then was stopped.
- Existing shared environment/auth files unchanged. Data backup `/opt/opai/data/project-demo.pre-release-20260908.json`. Post-restart JSON comparison changed only `/project/updatedAt`, not business content.
- Rollback: atomically restore `current` to the previous release and restart `opai`; do not restore data over new user activity without separate review.
- Public, server and local index SHA-256: `2502ed1402fd79e702e30832117b63ed85c7124244e21cf524b42bcc1d8f83b7`.

## Verification and known limits

- 319 tests, production build and scoped diff checks passed. Browser plugin unavailable; used installed Playwright/Chrome according to frontend-testing-debugging. Desktop 1440×1000: homepage, independent reference route, room selector and 2D/3D switch; mobile 390×844 homepage has no document overflow. Follow-up run had no page errors or failed HTTP responses.
- First public run had transient 520 responses for several JS chunks; subsequent requests returned 200. Origin served the files correctly. Do not claim the tunnel's cold-load reliability fixed. A trial bundler option was rejected as ineffective and fully reverted; deployed build remains the tested source.
- Furniture downloads are slow on the observed public route: double-bed GLB 527,496 bytes took 13.9 seconds, SHA-256 matched local. Early screenshots still contained loading placeholders; canvas presence alone is not complete model proof.
- Extended 120-second observation confirmed all ten distinct furniture GLB requests finished and the final screenshot displayed real furniture rather than placeholders. This is an observation window, not a measured exact load time. Evidence `/tmp/opai-public-model-loaded.png`; public interaction script `/tmp/opai-public-release-qa.mjs` and download log `/tmp/opai-public-model-check.log`.
- Existing production Feishu Aily/Base status was `auth_failed/user_token_invalid` before and after deployment. This release does not restore authorization. DeepSeek and demo catalog remain available. No test messages, new business submissions or external approvals were sent.
- Safari, physical mobile devices, new Feishu authorization and sustained cold-network reliability remain unverified.

## Persistence

Consulted AgentVault `HOME.md` and `10_Projects/PRJ-2026-008-oppein-ai-home-consensus/00_Project_Home.md`. The owner note still contains overlapping uncommitted work, so it was not overwritten or committed. This source-local Markdown records the release and reusable findings pending that existing gate.
